# 已闭环后的剩余 bugfix 需求（2026-09-24）

主流程 #397 已通过；下表是独立后续需求，不把它们写成已修复。唯一执行台账仍为 TASKS.yaml。

| 编号 | 实证 / 影响 | 修复范围与验收 |
|---|---|---|
| BF15 | 旧流程恢复正式执行时会遍历无关私人历史，造成选择/轮询干扰 | 服务端/前端按明确TASK作用域只读过滤；保留未知原请求，禁止恢复触发新POST |
| BF16 | 同身份SSE重同步时办理说明/固定输入勾选曾被清空 | 同身份/任务/会话暂存输入；切身份/任务仍清空授权，权限恢复后重新验证；不自动执行 |
| BF17 | 悬赏状态筛选迟到响应可能覆盖新筛选 | 请求generation/作用域核验，旧响应不回写；真实竞态回归 |
| BF18 | runtime ACK约3.8万条多次同步全扫描，执行拾取慢 | 在保留顺序/幂等/归属下增量索引与测量；不得删历史、用超时中止或跳过派发 |
| BF24 | Web28 #397详情已完成/归档，但“执行和成果”仍显示“尚未确认本悬赏有正式交付”；独立正式交付页已明确r2 accepted | 详情使用本taskId的权威formal-deliveries摘要；读取失败显示未能核验并提供入口，不把缺少当前workspace快照当暂无交付；覆盖刷新/归档/跨任务/旧响应 |
| BF25 | 前序实际 `/agent/tasks/397/workspace` 多次503，API reason=snapshot_unavailable；最终验收页不调用该端点，不能据此称已恢复 | 只读定位快照生成/重建/旧任务兼容，记录具体故障关联；保留锁、身份及一致性，恢复后同#397读取200再关闭此项 |

证据：BF15–18见tasks.md历史及 `deliverables/verification/juyiting-workbench-integration-20260924/`；BF24见最终竖屏任务详情观察及 `mobile-portrait-web28-final-20260924/task397-responses-private.jsonl` 的权威formal-deliveries；BF25见前序bf27网络/服务端日志记录。末轮无该workspace请求，结论为未复测而非仍然503。

另外未覆盖：实体手机软键盘/刘海、全产品全部边界、所有状态异常分支的线上回放、资金结算、多Agent协作；本次不恢复独立Reviewer，不以原候选固定环境清单阻止已授权主流程交付。
