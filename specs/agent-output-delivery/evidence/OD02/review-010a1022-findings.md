# Independent OD02 repair review — REQUEST_CHANGES

Candidate: `010a102217d105f6e6f5dd94dc3d01a8a4e5ffcc`. Reviewer: independent read-only architect `/root/output_design_review`; root transcribed the verdict. Review applies to the OD02 development gate only. Preferred cross-model review remains unavailable; this is the documented fallback.

The final associated regression is valid: **64 tests, no failure/error/skip**, with all 17 changed candidate paths matched. It does not cover the residual counterexamples below. Original writer owns further repairs; OD03 is not unlocked.

| Finding | Verdict | Residual and minimum acceptance |
| --- | --- | --- |
| R01 | OPEN, P0 | `OutputRunAuthorizationServiceImpl.authorizePersistedMutation` lines 332–367 catches `OutputAuthorizationException`, but production `AgentIdentityServiceImpl.lockActiveCanonicalAgentIdsInScope` (229–264) and `requireActiveIdentityForBinding` (308–322) can throw `AgentServiceImpl.AgentBizException` for missing/suspended/retired/inconsistent identity. These methods are transactional with rollback for exceptions; catching outside the proxy can still leave rollback-only. The exception escapes verification's final transaction (`OutputUploadServiceImpl` 180–188), so expected denial never commits REJECTED and remains VERIFYING. Add a non-rollback-poisoning, exact identity authorization result under the accepted lock order. Expected identity denial commits `OUTPUT_AUTH_REVOKED`, no stored transfer, and cleanup releases reserved bytes. Unexpected database/lock failures remain retryable. Verify actual MySQL + Spring-proxied production identity lifecycle scan barriers (suspend/retire/missing/inactive binding), and unchanged ACTIVE → READY. The previous permissive identity mock does not prove this boundary. |
| R04 | OPEN, P1 | `OutputContentInspector.inspectZipFile` lines 81–109 trusts `ZipFile.entries()`, which enumerates only the central directory. The exact-candidate harmless probe in `zip-structure-010a1022/` demonstrates acceptance of an archive with two local records but only one central entry, bypassing the member budget for the hidden member. Prove local/central one-to-one correspondence: counts, offsets, names, flags/methods, size/CRC, no unreferenced or overlapping local records, and consistent EOCD/central directory. Add hidden-local, local/name mismatch and EOCD/count/offset negative cases while preserving valid nested ZIPs and shared actual-byte budgets. This is an inspector-level counterexample, not a claim of malicious READY in a deployed system. |
| R06 | OPEN, P1 | `OutputUploadSecurityConfiguration.OutputTicketFilter` lines 54–59 catches every authorization runtime failure and emits permanent 401, including database/lock outages. Missing/malformed/expired/revoked credentials require 401/nonretryable; authenticated ACL/operation denial requires 403/nonretryable; unexpected authentication infrastructure faults require 503/retryable. Preserve the output error envelope, no-store and stable request ID in actual filter-chain tests. Envelope shape itself is closed. |
| R02 | CLOSED | Current successfully settled epoch and exact scanned storage identity fence complete/final READY. |
| R03 | CLOSED | RETAINED now counts toward projected physical-key occupancy. |
| R05 | CLOSED | Exact schema metadata checks and real MySQL drift tests satisfy this gate. |
| R07 | CLOSED | Dedicated strict JSON parsing rejects trailing input. |
| R08 | CLOSED | Deterministic PUT size mismatch is a permanent integrity error with cleanup accounting retained. |

No unrelated refactor, Rabbit fixture repair or full rerun is required by this verdict. Repair the three residual boundaries, run meaningful affected tests, freeze a new candidate, and obtain independent re-review. The original full-module Rabbit prerequisite failure and earlier schema-debug failures remain recorded separately. Owner downloads, client/UI, integration and production release remain later gates.
