# Intermediate lease-token log redaction check

Root independently parsed the retained LogServiceImplTest XML:27 tests,0 failures/errors/skips; exit0. Original stdout, exit file, operator hash list and XML were copied byte-for-byte from /tmp/cyf-od07-api-evidence. archive.json records the hashes. No test was rerun and no raw report was reformatted.

The current API working-tree change adds normalized leasetoken to LogServiceImpl's sensitive parameter names. Its new regression exercises direct leaseToken, nested lease_token and array LEASE-TOKEN keys while keeping ordinary run/status/operation metadata. This is the complete scoped logging suite result, not27 new lease cases or a final API candidate approval.

The exact execution command and frozen source association remain due in the writer handoff. HTTP/filter integration, policy1 guards, dispatch, real transactions and client behavior are not established by this suite. Keep the separate lease-core25/25 result and earlier failed six-filter run distinct.
