# OD07 current checkpoint

2026-09-13: active, not accepted. Sole product writer `/root/od01_source_auth` owns API worktree. OD06 development ACCEPT remains in ../OD06/accepted-r1-development-review.md; R1 release gaps stay open. Web212bfe4 and client714b4aa are unchanged.

- API3343342b committed M003A strict schema/entity/mapper preparation. Later uncommitted work corrects feature-off migration and adds run-bound lease core. Do not reset or cherry-pick over the writer's working tree.
- Writer reported the first targeted `:agent:jia-agent-service:compileJava` passed for lease-core work. This is compile evidence reported by the operator, not business tests or a final-candidate association; actual command/log will accompany the frozen handoff. No broad suite is counted as passing.
- Current working-tree implementation includes HTTP lease request/public-response DTOs, transaction adapter/receipt, Controller/security, dispatch binding/wire, policy1 admission and legacy/direct/aggregation/collaboration/funded guards. Product-source compilation reached compileTestJava in policy-guards-compile-test-r3; its new test matcher import failed. Earlier compile-r1/r2 failures are retained by the writer. Stable targeted tests, actual MySQL adversarial/feature-off policy0 regression and independent final API review remain due. Client lease lifecycle follows frozen API acceptance.
- Root prepared lease-fixtures.json, preserving R1 fixture bytes. Root helper8a908ae is independently accepted for synthetic controls only (seven methods); see accepted-lease-probe-8a908ae.md. No live lease request or new API service startup has occurred.
- Intermediate tests are retained in intermediate-lease-core-r3/README.md: test compilation repaired to exit0; the six-filter run had48 tests/20 failures, followed by a scoped lease-core fixture repair with25/25 passing. Only the latter suite has independently checked retained XML. Remaining suites and final source association are still due; do not combine these runs into an all-pass total.
- LogServiceImplTest27/27 is independently parsed and archived in intermediate-log-redaction-r1/README.md. This includes leaseToken key normalization/redaction regression; exact command and final source association remain due. It does not replace HTTP/filter or other lease tests.
- Root's dispatch/client check found TASK_INVITE supplied null workItemId and canonical outputContext validation allowed only R1 capabilities. Writer added the missing wire/codec connections, then corrected an overbroad bidirectional check that would reject existing R1 Hall commands with explicit workItemId. Root also observed receipt retention now uses run recoveryUntil+7days and version conflicts expose authorized currentVersion. These source observations still require final tests; they are not a frozen-candidate review verdict. Client handoff details are appended to implementation-handoff.md.
- Cross-run authorization finding: the adapter previously passed body runId to core without comparing it to current.runId. Writer added the comparison before receipt lookup and injects the ticket run into the command. Admission r2's retained adapter XML includes the four-action mismatch/no-core-or-receipt-interaction regression. Real MySQL cross-run coverage and final source association still remain due; this is not full finding closure.
- Admission/dispatch r1 reached tests:141 tests/1 failure, retained in intermediate-policy-admission-r1/README.md with all three original XMLs and earlier compile logs. Writer identified an outdated five-argument captureTaskInvites stub after the sixth workItemIds argument was added. Its scoped repair is verified in r2 below; the original r1 batch remains recorded as failed.
- Admission/dispatch r2 repair passed145/145 across four suites; see intermediate-policy-admission-r2/README.md. This supersedes the r1 failure only for the covered scope. Response leaseToken/leaseUntil null omission was identified afterward and its new test is not in this XML; it remains due alongside broader transaction/HTTP gates.
- Bypass guards r1:114 tests/3 failures across nine suites, archived in intermediate-policy-bypass-r1/README.md. The funded refusal code, legacy exception type and scope-miss NOT_FOUND behavior need repair/verification. Its separately retained adapter suite5/5 includes the release-response null-omission repair; no full-batch pass or real MySQL proof is inferred.

Existing API10018 still runs the immutable accepted R1 package; the previous synthetic Agent and fault proxy are stopped, its persona intentionally unbound. New trusted live operations require one explicitly designated operator and a new disposable binding; no user Agent or original checkout is touched. Every Gradle invocation holds /tmp/cyf-gradle.lock and uses the existing OD development wrapper. No push, deploy, pipeline trigger or gitlink pin is authorized by these local checks.
