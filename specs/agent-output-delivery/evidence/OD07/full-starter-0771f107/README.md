# Frozen OD07 full starter and retained downloads

Source `0771f1072d3d79eabde0757f254634427a7e0746`, isolated port 10018. Development bootJar passed (198 tasks); unchanged frozen source, private plugin-path rebinding and development OpenCV substitution. This is not canonical release evidence.

The application started in 146.259 seconds; Tomcat and the Rabbit connection initialized successfully. The original startup-log prefix is preserved byte-for-byte through the final startup warning. Production deployment d49a9b22 failed separately and is not described as repaired here.

Real login and OAuth authorization-code flow passed. TASK3 lists all three retained versions and downloads report.md, evidence.png and changes.zip with exact expected hashes, while the prior synthetic Agent remains offline/unbound and its workspace absent. Observation records source and historical-byte comparisons. No new binding was created. Global actuator health is not a readiness assertion because isolated ES/LDAP are absent.

JWT/session/private runtime configuration and credentials are excluded. Immutable jar remains at the absolute path in package metadata. No product push or release is implied.
