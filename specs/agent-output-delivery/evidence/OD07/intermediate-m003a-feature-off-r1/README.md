# Intermediate M003A feature-off checks

Root independently parsed two original XML reports: OutputDeliveryLeaseSchemaInitializerMySqlTest4/4 with no skips; OutputDeliverySchemaConfigurationTest3 methods with2 failures. Combined invocation7/2 failed. Original stdout/exit and frozen XMLs from m003a-feature-off-mysql-r1-xml-1789247562 were copied byte-for-byte; archive.json records hashes. No test rerun or report rewriting.

The MySQL suite includes featureOffSpringInitializerInstallsM003AAndSharedPolicy0DaosRemainWritable. The observed source populates baseline db/schema.sql, starts the real Agent/M003A Spring initialization components with output delivery disabled, verifies business output schema beans remain absent, and exercises actual MyBatis task/work-item DAO create/read/update/delete on MySQL. This is component bootstrap and DAO compatibility, not a full starter/network startup. Root requested an explicit pre-start assertion that all seven M003A columns are absent, so future baseline schema changes cannot silently weaken old-schema coverage; that assertion is not established by this report.

The configuration tests fail on an obsolete disabled-schema sentinel assertion and accessing beans from a context that failed to start. Fix their setup/expectations while preserving unconditional M003A prerequisites and disabled business schema behavior. Do not change the product back to optional M003A to satisfy an old fixture. Combined invocation acceptance, exact command/final source association and remaining real MySQL entry/lease/receipt/bypass concurrency proofs remain due.
