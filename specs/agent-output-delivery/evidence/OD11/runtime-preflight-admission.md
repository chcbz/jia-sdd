# Proposed fixed runtime-layout follow-up

The earlier narrow probes did not identify a Java jar. A repository historical lifecycle source at Root commit be41be7 provides a concrete `/opt/cyf/service/api` layout, so this bounded observation checks that specific deployment instead of guessing a new launch mechanism. Existing authorized Flow deployment can be reused without demanding a new provenance audit of every existing helper, but actual configuration location and running-artifact identity still need verification.

Source `../../tools/preflight-output-runtime.py` SHA256 `a0275cf1797356fd56b7a2b6ab0e022fba82e1006ace97f750f89ae0adac6722`; flow `runtime-preflight-flow.yaml` SHA256 `6220a41fc9033e4478906f5d614055a82fcce3f8e0c3e9b40d06cb8f3f3acb2c`. The historical `ops/release/host/cyf-api-kit` source SHA256 is b333df940a58640a59b46ebd29d301fe2a82e22b3745598693179a004e74d525. Syntax passed locally; no remote execution yet.

Scope: metadata of seven exact /opt runtime and /run paths; hash the fixed lifecycle helper up to1MiB and fixed deployed JAR up to300MiB with no-follow open and inode/size/mtime consistency. Inspect four fixed Spring properties paths up to64KiB only if regular/non-symlink, exporting solely known Agent/spring.config property names (never values). Existing app secrets may be encountered in memory but are never exported; no secret/key file or process environment is read. Numeric PID comm/Java cmdline inspection stays bounded, matches only the exact /opt jar argument, and exports only PID/startTicks, cwd equality and prod-profile-argument booleans. No network, SQL, subprocess, helper invocation, install, signal, application write or service restart.

After independent review, reuse the prior controller with only unique name `cyf-output-runtime-preflight-20260913-a0275cf1`, new hashes/YAML and new private local intent directory. Preserve singleton ECS/region checks immediately before create/start, all name-match pages, record-before-write, zero existing runs, full readback equality and reviewed default settings, final complete config digest recheck, one empty-param start and unknown-result read-only reconciliation. Do not alter either prior preflight pipeline or production release pipelines. Any target/Flow permission denial stops this channel, without retrying the denied ECS/RAM API or changing permissions.

Bind official run/job/order/machine log to ECS i-wz9j3ip2unzhwij0bs30, cn-shenzhen, host group28833/machineGroup yjctjhskhk1ti9t4. Missing/changing/symlink/oversize data remains unknown. A historical helper match is source correspondence only; installation, backup, rollback and business release gates remain separate.
