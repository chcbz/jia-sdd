# Proposed capacity/layout follow-up probe

The first host metadata run5264702/1 established only 4.5 GiB disk remaining and no response on the known storage/scanner ports. The next bounded read-only observation is necessary to choose an install/deployment plan compatible with actual memory and the existing runtime layout. It does not install anything or change a service. The existing ECS/RAM API denial remains untouched.

Frozen source `../../tools/preflight-output-capacity.py` SHA256 `9a67c935056f268b5ea49721b4c76d82540135ae1b08e0556fb9c7f0eeb65a7a`; flow `capacity-preflight-flow.yaml` SHA256 `766523c3bc4ae07d987f30a95e49c162f03e6a841064aaa0b87b0772133f8b16`. Local syntax and five path predicate cases passed; no production invocation yet.

Exact read scope: architecture; /etc/os-release at most16KiB exporting only ID/VERSION_ID/ID_LIKE; /proc/meminfo at most16KiB exporting only four capacity numbers; the two known deploy helpers at most1MiB each, requiring the previously observed hashes before extracting at most60 restricted literal paths and six fixed keyword booleans. Helper raw content is not exported. Inspect numeric PID comm and only matching Java cmdline (up to64KiB), optional cwd symlink for a relative jar and bounded stat; export only canonical CYF/ISP jar path, PID/startTicks and file size. Bounded unrelated Java arguments can be read in memory but are never exported. No application configuration or process environment is read. No TCP connections, subprocess, service commands, install, file write, SQL or signal is performed by the remote script. Read errors are reported as unknown or omitted, not success.

Use the same reviewed no-source/single-job/no-download root Flow channel, fixed machineGroup yjctjhskhk1ti9t4. Create a separate task-owned deterministic pipeline `cyf-output-capacity-preflight-20260913-9a67c935` only after independent review; preserve the first pipeline/run. Reuse the prior guarded controller sequence with new fixed name and hashes and a new exclusive local request directory. Check exact singleton host group28833, ECS i-wz9j3ip2unzhwij0bs30, cn-shenzhen immediately before create and before start. Read all name-match pages before creating, record intent before each remote write, read back parsed flow and zero sources, accept only previously reviewed default outer settings, and pin the entire returned pipelineConfig digest before a fresh reread at start. Require no existing runs; start once with empty params. Unknown writes lead only to read-only reconciliation, never blind retry. Reject any actual target/Flow permission denial without changing permissions or retrying ECS.

Bind result to official run/job/order and machine log metadata for the exact ECS/region; archive only safe results. This does not close memory sufficiency, storage/scanner readiness, migration, backup, rollback or feature smoke until the actual install and release plan is executed and verified.
