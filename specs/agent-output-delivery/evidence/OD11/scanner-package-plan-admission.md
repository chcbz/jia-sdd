# Revised ClamAV cached package resolution

The initial f6624ff0 candidate was rejected before create/start; only a read-only Flow name/host precheck ran. Review findings were inherited environment, unrestricted repositories/plugins/cache growth, post-capture output limit and permissive partial-plan status. No installation or production mutation occurred.

Revised source `../../tools/plan-output-scanner-packages.py` SHA256 `dc8f9d0f901a772e17c1ce0a0954b3706fc7651ba4156ea90e16f2f82441a77e`; YAML `scanner-package-plan-flow.yaml` SHA256 `3bf4b059c9f5d0adc17c1d73a9650f3ed5f9a21cbbd816c8ca1875619595f416`.

The revised command uses `--noplugins --cacheonly --assumeno --disablerepo=*`. Only exact IDs from the fixed alinux3-os/updates/plus/powertools and epel allowlist that appear in bounded local .repo files are enabled. Cache-only DNF forbids network metadata refresh, including for expired cached metadata. Missing/incomplete caches stay UNKNOWN and are not retried with network flags. Repository inventory exports only safe IDs, no URL or configuration value. A fixed minimal PATH/HOME/locale environment replaces the Flow environment. DNF logs/persist state use a private temporary directory with 1MiB log size/no rotation, removed on exit. Existing system package caches are not refreshed; no package installation is confirmed, and no service command is invoked.

A 4GiB available-space prerequisite protects the current approximately4.84GiB filesystem from starting even the bounded plan when headroom falls; before/after available space is recorded. Stdout is read incrementally up to2MiB, with120s wall timeout; a dedicated process group is killed and parent reaped on completion, timeout or output excess. Complete cached plan requires DNF exit1, automatic `Operation aborted.`, all three requested packages and no recognized error marker. Other statuses remain UNKNOWN. This does not verify RPM signatures or prove production readiness.

After independent ACCEPT, create one new task-owned pipeline `cyf-output-scanner-package-plan-20260913-dc8f9d0f`. Reuse the guarded controller changing only name, public YAML/hash and exclusive local intent directory. Preserve pre-create/pre-start singleton host checks, name pagination, intent-before-write, full flow/settings readback, complete config digest locking, no existing runs and a single empty-param start. Unknown responses use read-only reconciliation, no blind retry. Original API/Web/legacy pipelines remain unchanged. Target: group28833, machineGroup yjctjhskhk1ti9t4, ECS i-wz9j3ip2unzhwij0bs30/cn-shenzhen. Bind results to official run/job/order/machine metadata.

Any later metadata download or installation requires its own concrete candidate using observed repo IDs and verified package provenance. This cache-only step avoids a separate remote repository-enumeration run when a complete cached plan is available.

The cache-only revision9a9eed3 was also NO-GO before create/start. Final repairs bind DNF to a generated private minimal config, system caches `/var/cache/dnf`, actual repo directory `/etc/yum.repos.d`, private logs/state under fixed `/var/tmp`, and mandatory `skip_if_unavailable=False` for every repo. Repository files/directory must be root-owned, not symlinks and not writable by group/others; duplicate allowlisted IDs are rejected. Both cache and temporary filesystems require4GiB available, and current MemAvailable must be at least1.5GiB. Unknown output now exits1. The parser requires a complete transaction table, exactly one automatic abort and all three targets, rejecting unparsed/unsupported/duplicate rows and problem markers. Six synthetic controls reproduced the former ambiguous cases and passed; they are helper controls, not host/package verification. No new host run has yet occurred.
