# Proposed ClamAV package resolution

The production host is Alibaba Cloud Linux3/x86_64. The earlier fixed scanner paths/port are absent. Root tested public upstream ClamAV GitHub release metadata and the official RPM endpoint once; both returned403, and neither was retried. Use the host's configured vendor package repositories to resolve a concrete install transaction before choosing package versions, consistent with isp-install's supported distro package-manager approach.

Source `../../tools/plan-output-scanner-packages.py` SHA256 `e8e65147eaf9a0711972397613c6e754c57a35ee027cfa788ccd7598e8a2d48d`; flow `scanner-package-plan-flow.yaml` SHA256 `6912dbd532ed073898e8ed63a37ff4871b3995a66af9c1f61b73b54baaf76c73`. Syntax passes. This proposal runs exactly `/usr/bin/dnf --assumeno --setopt=install_weak_deps=False --setopt=timeout=15 --setopt=retries=0 install clamav clamd clamav-update`, with closed stdin and120s timeout. It refuses transaction confirmation, installs no package and runs no service command. DNF may refresh existing repository metadata/cache, which is explicitly within this reversible preparation scope. It neither changes repository configuration nor disables signatures or GPG checking. Only bounded parsed package names/architectures/versions/repository IDs and fixed status/size lines are exported; all raw output, including possible repository credentials, remains in memory. Missing package/command/timeouts remain unresolved, never readiness.

After independent review, create a new deterministic task-owned Flow pipeline `cyf-output-scanner-package-plan-20260913-e8e65147` using the same no-source/single-job/no-artifact-download root host channel. Reuse the prior guarded controller changing only name, public YAML/hash and exclusive local request directory. Preserve both pre-create/pre-start singleton host checks, name pagination, intent-before-write, full flow/settings readback, complete config digest locking, no existing runs and a single empty-param start. Unknown responses are reconciled by read-only queries only. No original API/Web/legacy pipeline or prior probe is edited. Stop on any Flow/target denial without altering permissions or retrying the denied ECS API.

Expected sole target remains host group28833, machineGroup yjctjhskhk1ti9t4, ECS i-wz9j3ip2unzhwij0bs30 in cn-shenzhen. Bind results to official run/job/order/machine metadata. This is package planning, not installation or release acceptance. Actual install needs a separate concrete reviewed script with fixed resolved packages, signature verification, resource checks, private loopback services and unchanged existing API/client runtime.
