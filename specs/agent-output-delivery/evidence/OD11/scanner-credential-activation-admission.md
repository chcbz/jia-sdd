# Scanner credential-transition activation candidate

Sourceefa84230010f23121f86500f2c4cc15075a6db73fd525b522c4384f00d14d068; YAMLe2a578eca9e41dbf07bdeab8420d7d6b5b71188d85b683cf45f684d823982235. Controller `/tmp/od11-scanner-credential-activation-efa84230.cjs`, SHAb92bd3e08c14b498520b84116875a39aaf683bc702d524bf3b650b8ca37745a3. Unique name `cyf-output-scanner-credentials-20260913-efa84230`; no create/start yet. Fixed organization and sole Shenzhen host remain unchanged.

Identity diagnosis5264938/run2 proves effective/account UID/GID986 and actual clamd journal UID/GID986 reaching database loading before compensation. The prior rejected UID tuple is unknown. This candidate retains the exact installed engine/units/CVD signatures and logging configa005; verifies the earlier logging backup and failed observed intent1789301970; writes only a new exclusive credential-activation intent, then enables/starts the scanner. No repeated install, download, logging replacement, updater or API mutation.

For the Type=simple initial child, print numeric UID/GID telemetry before rejection and allow each UID/GID tuple to be whole0 or986 during the first15seconds, including sequential `(uid0,gid986)` and `(uid986,gid0)` transitions; all other tuples, subsequent root credentials, PID/startTick changes or any NRestarts fail. Readiness still requires strict all986 UID/GID and exact clamd executable inode; root PONG cannot qualify. Initial PID/startTicks remains captured before first PING; checks remain before each PING, at progress, after readiness and after the four existing clean/EICAR/member61MiB/aggregate110MiB scans. Memory/disk reservations and600second startup deadline unchanged. Failure compensates only the scanner. Eight mocked credential/restart controls pass; they do not establish real readiness.

Admission requires independent review, fresh API/Web deployment conflict check, exact-name zero and singleton host before create, complete Flow readback/settings equality, zero prior runs and one exclusive {} start. Unknown writes permit read-only reconciliation only. Fresh resource checks also execute inside the host script. Latest disk2717790208bytes barely passes scanner reserve2684354560 but does not pass MinIO initial2862452920; storage remains uncreated and capacity investigation is separate.

The15second clock now starts immediately before systemctl, and the predicate compares unrounded monotonic elapsed time. Sequential cross credential pairs are explicitly admitted only in that window and covered by controls; readiness remains strict986/986. Original candidate4ce6 was never created or executed. Capacity source was committed separately atbf9479a and task-created Python cache removed; this candidate will be committed before use.
