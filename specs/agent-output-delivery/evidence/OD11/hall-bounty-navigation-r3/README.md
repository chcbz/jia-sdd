# Failed real Hall bounty navigation

This is an isolated R1 probe, not production or final R2 acceptance. Web `212bfe4`, R1 API JAR and process identity are recorded in archive.json. The fixture owner logged in through the existing local HTTP login/OAuth helper; the browser then used that real token from isolated storage. This is not OAuth UI/WeChat evidence. The old synthetic Agent remains stopped and unbound; no task creation/assignment or model generation occurred.

Command: `node specs/agent-output-delivery/tools/smoke-output-browser.mjs /tmp/od11-browser-hall-bounty-r3-config.json`. The JSON selects the existing task3 text artifact, entryMode hall-bounty and completed status. It contains file paths and fixture metadata, not credentials. Output remained under `/tmp/od11-browser-hall-bounty-r3`; observation.json and failure.png are archived byte-for-byte with hashes. Earlier probe attempts r1/r2 diagnosed an incorrect tool assumption that desktop uses the portrait navigation button; r3 instead read the canonical bounty-board geometry and dispatched real pointer clicks.

Observed: desktop1440x900 → actual bounty-board canvas hit target → completed status filter → input task ID3 → click the matching task card → real GET `/api/agent/tasks/3/artifacts`200, three output cards displayed. The nested close control could not be hit within25seconds; the probe failed and cleaned up Chromium with no cleanup error. No completed preview/download or successful viewport is claimed.

The screenshot shows the nested detail dialog cropped by the outer floating panel. Current `BountyPanel.vue` constrains `.bounty-modal` by `min(90vh,720px)` while its outer `.floating-panel` has smaller actual height, overflow hidden and paint containment. The detail includes three published files and a non-scrolling task-info section. The sole Web writer must confirm/repair container-relative height, header reachability and scrollable output content after integrating the latest Web changes, then repeat actual pointer navigation and byte download. Root has not modified product Web code to bypass the failure.
