# Pinned isolated MinIO installation candidate

Source `../../tools/install-output-storage.py` SHA256 `3089dc3914264eefb5282fed220f3b4eb522182bacdcf885d3defd87fd102ffc`; YAML `storage-install-flow.yaml` SHA256 `5f53ba8e73743e4d01d0a89ac1d0b943df842670cf37faf391b1efa50e46f954`. This is a concrete proposed installation, not yet run. User authorization to continue through release includes required base components. Independent review precedes execution.

The official upstream binary direct download was gone (410), so preparation uses the same tested MinIO release's official Quay image. `minio-provenance/` preserves original manifest/config/provenance bytes. Root independently validated all nine image layers, their recorded sizes/digests, and the sole AMD64 MinIO regular-file member; the development host is ARM and did not execute that binary. Public anonymous HEAD on the pinned layer returned200/39450432bytes. The installer downloads only that exact layer with no proxy or Flow credentials, checks its fixed length/SHA, streams only the unique regular `usr/bin/minio` file, checks110989496bytes and binary SHA `7c5bd8512c6e966455b1d198209358b2d191c77a83ab377c4073281065fb855f`.

Scope is new paths `/opt/cyf/output-storage`, `/etc/cyf-output-storage`, `/var/lib/cyf-output-storage`, a new `cyf-output-store` system user/group, and new `cyf-output-storage.service`. Every target must be absent; trusted ancestor directories must be root-owned and not writable by group/others. Existing path/user or occupied19000/19001 fails before mutation. Require current disk for3.5GiB reserve plus328,538,288bytes peak, and1.5GiB MemAvailable. Temporary download files are removed on exit. Fixed minimum child environment; no raw subprocess output or credentials enter Flow logs. Root-only credentials/environment are generated on-host and never transferred to developer/Flow. Service data is writable only by its dedicated user.

The new unit binds loopback only; console is disabled; MemoryMax384M/GOMEMLIMIT256MiB, at most8 API requests, TasksMax128, systemd filesystem/user isolation and a restart burst limit apply. Installation invokes only useradd and systemctl daemon-reload/enable--now for the new unit. No existing API/client/service is restarted, no existing configuration is overwritten. Readiness requires the actual MinIO health endpoint200. This still does not create a bucket, configure the application, verify bucket versioning/quota/retention/backup or enable end-user writes; those are subsequent release gates.

An exclusive install-intent receipt precedes user/service changes. Unknown/partial installation returns nonzero and preserves new paths/receipt for read-only diagnosis; it must not be blindly retried or delete data. Recovery after partial installation is a new inspected repair candidate. Stopping this new unit is a bounded recovery option; never run unscoped cleanup or remove storage containing outputs. Full production rollback/backup is required before application activation, not inferred from service readiness.

Proposed pipeline `cyf-output-storage-install-20260913-3089dc39`: zero sources, one root VMDeploy job, no artifact download. Retain the guarded controller's unique-name pagination, one ECS pre-create/pre-start checks, intent-before-write, full YAML/settings readback and complete config hash, zero prior runs and single empty-parameter start. Host group28833/machineGroup yjctjhskhk1ti9t4/ECS i-wz9j3ip2unzhwij0bs30/cn-shenzhen remain fixed. Official order/machine logs determine actual outcome. No original product pipeline is edited.

## Revised resource and failure model

Initial19a0e3f was NO-GO before any create/start. This revision makes the preflight budget explicit using reserved space plus the complete allocation peak. This limited empty-storage installation reserves3.5GiB throughout download/start and1GiB MemAvailable after startup. Known peak layer+candidate+installed+64MiB initialization budget totals328,538,288bytes. Admission requires3.5GiB+known peak, and max(1.5GiB,1GiB+384MiB). Latest4,333,858,816bytes/1,693,245,440bytes satisfy those concrete sums; any drift below them rejects. This is not the unknown existing API helper's start/restart threshold, and authorizes no API restart. Archive/scanner/package/bucket capacity must be separately allocated before enabling writes; the empty service alone may not consume that future capacity unchecked.

Pre-activation checks re-evaluate memory and disk; post-readiness must retain3.5GiB disk and1GiB available RAM or the new service is stopped and disabled. The final binary is opened no-follow, checked for root UID/GID/0755/size and stable inode/mtime, rehashed and fsynced; results contain measured identity. New group absence and created UID/GID/home/nologin identity are verified. systemd MainPID is bound to stable /proc startTicks, exact executable device/inode/path, all real/effective/saved/fs UID/GID values, and measured unit memory. Failures after activation perform bounded disable--now and independently read back inactive/failed+disabled, preserving files, identity, credentials and data for diagnosis. An unsuccessful compensation stays explicit UNKNOWN without blind retry.

A420s total installer alarm plus at most55s compensation leaves at least125s under the Flow600s host timeout for result output. This closes the earlier300s mismatch. No installation has been attempted yet.

Latest preflight5264762/1 measured4,188,438,528bytes, below the old rounded4GiB floor but above3,758,096,384+328,538,288=4,086,634,672bytes required by the preserved reserve and peak allocation. The revised threshold removes only that extra rounding:3.5GiB reserve,64MiB initialization allowance, all activation/readiness checks, memory budget and compensation are unchanged. There is101,803,856bytes additional peak headroom at the latest observation. The old storage pipeline5264761 was never started and will not be edited/reused; the revised immutable name is89cf0709. This does not waive any later API/bucket/scanner/backup budget.

## Current cleanup observation required

Network5264773/1 reached a failed terminal result after namespace creation, without exception-path cleanup fields. This revision therefore does not rely on those missing fields or rerun DNF. Before any install mutation, a bounded read-only check requires no `/var/tmp/cyf-scanner-plan-*` paths and no reference to that exact dedicated prefix in any current process command line or mountinfo. It checks up to8192 current numeric process directories, at most64KiB cmdline/512KiB mountinfo each and15s overall; disappearing processes are tolerated, unreadable/oversized/remaining references fail closed. Only aggregate counts and zero-reference assertions are exported, never unrelated arguments/mounts/configuration. This is new live cleanup evidence, not a retrospective claim about the failed run. All previously reviewed allocation/identity/compensation gates are unchanged. Use the new immutable c22e74f3 pipeline only after independent review; no old storage pipeline is started or edited.

The c54360d cleanup review required the15s deadline to start before directory enumeration. This final revision uses streaming os.scandir, checks that deadline per entry and fails on the first matching prefix without collecting/sorting; the same remaining deadline bounds /proc. No DNF or installation attempt was repeated.
