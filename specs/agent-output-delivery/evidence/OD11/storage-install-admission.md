# Current isolated MinIO installation admission

The sole new proposed identity is `cyf-output-storage-install-20260913-145adaee`. Review pending. Previous pipeline5264789/run1 was rejected before download: disk short1,193,976bytes and memory short10,829,824bytes. Do not retry it. This new allocation implementation must receive independent review. Source `../../tools/install-output-storage.py` SHA256 `145adaee3346ebb091163a6240b403650ee29cbea68a899a6e6ffc876e49599c`; flow `storage-install-flow.yaml` SHA256 `5942de2491d25a1ed4c994f9a62739ba77c7b0d4d20b4f2ff9d4912d1239ed25`. Do not start/reuse older pipelines. The earlier direct Quay attempt5264784/run1 failed during download before installation mutations; its original observation is in storage-install-5264784-run1/. Failure subtype was unavailable, so the exact network cause remains unknown.

This candidate addresses download transport and bounded streaming allocation. Public DaoCloud mirror `/v2/quay.io/minio/minio/blobs/sha256:…` serves the exact official Quay layer. Local full streaming GET on1789284747–1789284760 verified39,450,432bytes and original SHA256 f9c0805c25ee5c0d375a9c16810eafb57cf658d7b6814c179d52496272bec8a4; evidence minio-provenance/mirror-layer-verification.json. Anonymous pull token comes only from fixed HTTPS m.daocloud.io/auth/token/service/scope/client_id; no user, Flow or registry credential is used or exported. Fixed docker/27.0 User-Agent and no-cache header match the successful full fetch. Token response<=64KiB/token<=16KiB; request20s then layer30s and120s total download. Initial local HEAD succeeded while a default full fetch returned403; this full GET verifies the concrete transport, not production reachability. Production repeats complete size/hash and exact binary-member checks before installation.

Unique regular binary remains110,989,496bytes, SHA2567c5bd8512c6e966455b1d198209358b2d191c77a83ab377c4073281065fb855f, MinIO RELEASE.2025-09-07T16-13-09Z AMD64. Instead of copying110,989,496bytes twice, assert same filesystem, exclusively hard-link the root-only candidate to the new install path then unlink its temporary name. This preserves one allocation; no existing destination replacement. Installed root UID/GID,0755,size,stable inode/mtime,full hash and fsync checks are unchanged.

Disk reserve remains3,758,096,384bytes (3.5GiB). Stream the response directly through a compressed-byte hashing reader into the unique candidate, then drain through EOF and verify the exact39,450,432byte layer hash before any installation mutation. No compressed layer file is retained. Bound read chunks1MiB and tar entries2048, retain unique regular110,989,496byte member+binary hash checks, and fsync candidate. Peak is now178,098,360bytes (single binary+64MiB initialization), admission3,936,194,744bytes. The immediately preceding resource rejection5264789/run1 recorded3,974,451,200bytes, giving38,256,456bytes margin for this new implementation, always rechecked live.

The immediately preceding streaming attempt5264797/run1 failed with MemoryError in preflight after96MiB RLIMIT_AS and before cleanup/download/mutations. Local RSS28MiB did not establish the production interpreter's virtual-address layout; the limit was unsuitable. This candidate removes that address-space limit and restores the previously accepted initial-memory admission max(1536MiB,retained1GiB+service384MiB)=1,610,612,736bytes. It does not reduce the original admission or final1GiB reserve. Before cleanup it exports only numeric self VmSize/VmRSS/VmData to distinguish address layout from resident use. No daemon/user process is changed by this observation.

Streaming implementation and integrity controls are byte-identical at the helper-function level to24cba35: local exact extraction measured28,225,536byte peakRSS; truncated/modified-tail tests rejected. Those reports remain attributed to24cba35, not regenerated or relabelled as full current-source tests. Current Python AST and embedded YAML parse pass. Keep same-device exclusive hardlink transfer, runtime384MiB cgroup limit, activation/final resource checks and MemoryError compensation. The parent interpreter is not artificially constrained by virtual-address size. This separate empty-storage step still does not admit combined scanner/API/workload resources.

Retain previously approved live read-only scanner-plan cleanup: streaming /var/tmp and bounded /proc share15s from function entry, reject any residual dedicated path/process/mount reference before mutation. All new paths/identity/ports must be absent; trusted ancestors root-owned/unwritable to others. Creates only /opt/cyf/output-storage, /etc/cyf-output-storage, /var/lib/cyf-output-storage, cyf-output-store user/group and its new systemd unit. Credentials generated on host stay root-only. Existing API/client/service is not restarted or configured.

Unit remains loopback19000/19001/browseroff, MemoryMax384M/GOMEMLIMIT256MiB/maxrequests8/TasksMax128/restartburst3 per300s with user/filesystem isolation. Verify readiness200 against actual systemd MainPID/startTicks/UID/GID/exe path+device+inode and measured memory. Recheck live disk/memory at activation/readiness. Exclusive intent precedes user/service changes. Post-activation failure disables/stops only new unit, independently verifies inactive/failed+disabled, retains paths/data, reports uncertainty. Alarm420s +<=55s compensation within Flow600s. New error output is safe type/code/HTTP status and downloaded byte count, never raw exception text or token.

Guarded controller uses exact frozen source/YAML, zero sources, one root VMDeploy, unique-name pagination, singleton target before create/start, intent-before-write, complete parsed YAML/settings readback+config digest, zero prior runs and one empty-parameter start. Fixed org5fb7d76ee6f9d07f148529c7/group28833/machineGroupyjctjhskhk1ti9t4/sole ECSi-wz9j3ip2unzhwij0bs30/cn-shenzhen. Ambiguous writes reconcile read-only. No DNF or product pipeline edits.

Success covers only isolated empty storage health and identity. Bucket/quota/retention/backup, scanner/signatures, API configuration/migration, real user download and formal release remain separate gates.

Previous candidate51ee9620 create returned WAF_CLIENT_UNCLASSIFIED; paginated exact-name reconciliation at2026-09-13T08:04:29Z found zero matching pipelines, so no pipeline ID/start or installation exists for that request. Do not resend that intent. This revision resolves self process telemetry through numeric getpid under /proc rather than the self alias; identical bounded numeric fields and all storage/runtime checks. The suspected gateway pattern is unproven; this is a harmless equivalent explicit-PID path, not a change in authorization or exported data. New identity requires independent delta review.
