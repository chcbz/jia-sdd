# Pinned isolated MinIO installation candidate

Source `../../tools/install-output-storage.py` SHA256 `37e82d6f16fc3bda14beef981802de70c483fc1dbfbc5c5d150ea8a8576d1ec1`; YAML `storage-install-flow.yaml` SHA256 `6350521079a96a32f359316e7ec349b93eb5936e21f7edefca42a97a351d765a`. This is a concrete proposed installation, not yet run. User authorization to continue through release includes required base components. Independent review precedes execution.

The official upstream binary direct download was gone (410), so preparation uses the same tested MinIO release's official Quay image. `minio-provenance/` preserves original manifest/config/provenance bytes. Root independently validated all nine image layers, their recorded sizes/digests, and the sole AMD64 MinIO regular-file member; the development host is ARM and did not execute that binary. Public anonymous HEAD on the pinned layer returned200/39450432bytes. The installer downloads only that exact layer with no proxy or Flow credentials, checks its fixed length/SHA, streams only the unique regular `usr/bin/minio` file, checks110989496bytes and binary SHA `7c5bd8512c6e966455b1d198209358b2d191c77a83ab377c4073281065fb855f`.

Scope is new paths `/opt/cyf/output-storage`, `/etc/cyf-output-storage`, `/var/lib/cyf-output-storage`, a new `cyf-output-store` system user/group, and new `cyf-output-storage.service`. Every target must be absent; trusted ancestor directories must be root-owned and not writable by group/others. Existing path/user or occupied19000/19001 fails before mutation. Require current4GiB disk and1.5GiB MemAvailable. Temporary download files are removed on exit. Fixed minimum child environment; no raw subprocess output or credentials enter Flow logs. Root-only credentials/environment are generated on-host and never transferred to developer/Flow. Service data is writable only by its dedicated user.

The new unit binds loopback only; console is disabled; MemoryMax384M/GOMEMLIMIT256MiB, at most8 API requests, TasksMax128, systemd filesystem/user isolation and a restart burst limit apply. Installation invokes only useradd and systemctl daemon-reload/enable--now for the new unit. No existing API/client/service is restarted, no existing configuration is overwritten. Readiness requires the actual MinIO health endpoint200. This still does not create a bucket, configure the application, verify bucket versioning/quota/retention/backup or enable end-user writes; those are subsequent release gates.

An exclusive install-intent receipt precedes user/service changes. Unknown/partial installation returns nonzero and preserves new paths/receipt for read-only diagnosis; it must not be blindly retried or delete data. Recovery after partial installation is a new inspected repair candidate. Stopping this new unit is a bounded recovery option; never run unscoped cleanup or remove storage containing outputs. Full production rollback/backup is required before application activation, not inferred from service readiness.

Proposed pipeline `cyf-output-storage-install-20260913-37e82d6f`: zero sources, one root VMDeploy job, no artifact download. Retain the guarded controller's unique-name pagination, one ECS pre-create/pre-start checks, intent-before-write, full YAML/settings readback and complete config hash, zero prior runs and single empty-parameter start. Host group28833/machineGroup yjctjhskhk1ti9t4/ECS i-wz9j3ip2unzhwij0bs30/cn-shenzhen remain fixed. Official order/machine logs determine actual outcome. No original product pipeline is edited.
