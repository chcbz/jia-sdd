# Fixed official-repository scanner transaction plan

The only prior scanner query5264758/1 ended UNKNOWN without a package plan. It was not retried. A separate development-host HTTPS query of the official Alibaba mirror found exact ClamAV1.4.6-1.el8 RPM metadata. The concrete updater package is `clamav-freshclam`, providing legacy virtual name `clamav-update`; the new request uses the concrete pinned NEVRA. Public metadata and its checked primary checksum are recorded in scanner-public-package-metadata.json; this is not RPM signature or host dependency proof.

Candidate source `../../tools/plan-output-scanner-network.py` SHA256 `b01815f05df89acf5213bf2ee45741e798a04c36dd4f0144d8f41c62f3ba4e75`; YAML `scanner-network-plan-flow.yaml` SHA256 `3b3baba43b3b44513f2cc7ab85cfce7a0de1dafef783747f8b211be45ee6f7dd`. Five pure parser controls passed; no DNF/mount/host call was involved in those controls.

This new candidate performs one non-confirmed DNF transaction plan against three literal HTTPS base URLs on mirrors.aliyun.com: alinux/3/os/x86_64, alinux/3/updates/x86_64 and epel/8/Everything/x86_64. All three public repomd endpoints were successfully read from the development host. No host repository file/global DNF config is consumed. Fixed root-owned distro metadata must identify Alibaba Cloud Linux3/x86_64. A generated private config and repo file fix all URL/repo/cache/state/log/variable directories; noplugins, disablerepo-all and exact three enables, gpgcheck=1 and sslverify=1 apply. No GPG key is imported. RPM signatures remain an actual-installation gate.

The Python process first creates a private mount namespace, makes inherited mounts recursively private, then mounts one new256MiB tmpfs with nodev/nosuid/noexec/0700 at a private /var/tmp child. Every DNF writable cache/log/state path is inside it. It is unmounted after the DNF process group is killed/reaped; the underlying temporary directory is removed. Namespace or cleanup failure returns UNKNOWN, never readiness. There is no host-visible persistent mount or repository change. A current4GiB disk gate and1.5GiB MemAvailable gate cover this transient preparation; startup fails before DNF if either fails. This step should precede the approved empty MinIO installation to avoid overlapping resource budgets.

DNF uses only fixed PATH/HOME/C locales, closed stdin, --assumeno and exact clamav/clamd/clamav-freshclam1.4.6-1.el8.x86_64 requests. Output is incrementally limited to2MiB/120s, exported only as parsed package identities/status. Flow timeout300s leaves cleanup margin. Plan completion requires exit1, exactly one automatic abort, complete parseable transaction table with all three concrete names and no known problem/unparsed/duplicate row. Private cache must contain no RPM. Disk drift over16MiB causes UNKNOWN. All failures emit safe JSON and nonzero exit; no raw repository output or credentials are exported.

Proposed new pipeline `cyf-output-scanner-network-plan-20260913-b01815f0` uses the same guarded unique-name, zero-source, single-host Flow channel. Preserve singleton pre-create/pre-start checks, exact readback and config digest, empty prior runs, intent-before-write and one empty-param start. Org5fb7d76ee6f9d07f148529c7/group28833/machineGroupyjctjhskhk1ti9t4/ECSi-wz9j3ip2unzhwij0bs30/cn-shenzhen are unchanged. No original product pipeline is modified. Unknown API responses are reconciled read-only with no blind repetition.

Allowed side effects: private tmpfs metadata/cache, temporary mountpoint and transient DNF/rpmdb lock activity. Prohibited: RPM transaction confirmation, scriptlet execution, service changes, system repository changes, persisted package/cache installation, API changes. Actual fixed-package installation will be separately reviewed after this plan.
