# Real recovery and unbind retention

Candidate API f1978b09ac7d567a53923f7c7e5c1bc3667e6a13, Web212bfe4f9441cc82262d98e7b0723d6c0800e3d9, client714b4aa73b474b37ec6348d045b653a3ca273cf2. Existing isolated API10018 and actual MySQL/MinIO/ClamAV were used. Writer operated the live requests; Root archived original safe reports/helpers and independently checked local bytes without replaying requests.

- TASK4: authentic task create/assign and real WS dispatch; only output HTTP passed through the loopback fault proxy. It forwarded the first complete to API10018, received202, discarded that response and held subsequent transport failures until release. Pre-restart queue UPLOADING/retry5, Agent absent, counter1; after a new Agent process recovered the durable queue, PUBLISHED/terminalNotified, counter still1. Three list/versions/detail/download paths succeeded; database has three uploads/objects/artifacts, only v1, three active owner shares and ordered events1–10.
- TASK3: authenticated disposable persona unbind returned200/E0. Binding and identity are SUSPENDED, runtime offline, TASK3/4 sources and three owner shares each remain active. After refreshing the same owner's expired JWT, all three retained files remained readable through list/versions/detail/download. Root read the three saved post-unbind downloads and compared their bytes with previously archived TASK3 files: all equal.
- Root verified TASK4 snapshot bytes against the recovery report and checked the external append/fsync invocation counter is exactly one line. TASK4 recovery download bytes were compared with snapshots by the writer helper in memory and were not retained; Root does not claim independently reading those HTTP response bytes.
- The safe index records two Agent exec sessions and registration/SIGINT boundaries. Agent OS PID/startTicks were not persisted; generator child PID is not an Agent PID. Proxy PID/startTicks were retained; cleanup report confirms proxy and synthetic Agent stopped, API unchanged. No user Agent was stopped.

Preserved failures: post-unbind r3 used an expired JWT and returned401; database r1 selected a nonexistent identity_state column. Corrected read-only r3 database observation uses actual schema fields; no schema/fixture mutation repaired the result. Earlier TASK1 WORKSPACE_POLICY_REQUIRED and TASK2 OUTPUT_SIZE_MISMATCH terminal states remain intact; generic overwritten create/assign evidence is not reconstructed.

Scope: tiny synthetic files, real application transport/storage/scanning and durable recovery. The proxy buffers Content-Length requests and is not a large-file/streaming interruption load test. Model invocation means the counted deterministic generator, not a quality test of a real model. Legacy task completed is R1 OWNER_SHARE behavior, not R2 formal submission/acceptance. These results do not by themselves pass the full R1 matrix, canonical packaging, physical WeChat, deployment or R2 gates.

See root-observation.json for independent byte checks and original archive digests; od06-o04-o20-safe-index-r1.observation.json connects operation reports, process boundaries and source paths. Archived launchers reference local secret files but contain no secret values. Do not execute them to refresh timestamps.
