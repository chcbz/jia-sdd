# Binary upload mismatch investigation

Writer reported real task1 dispatch failed without workspacePolicy; task2 used an isolated Git workspace and reached trusted TASK run753feab8e4634be488797c37d9b3a912. Markdown published; PNG upload returned OUTPUT_SIZE_MISMATCH; ZIP remained snapshotted. These are partial observations, not bounty acceptance.

Root read the PNG109B and ZIP377B snapshots. A UTF8 string roundtrip changes both byte counts/hashes (see root-snapshot-roundtrip.json). Static API43e6bb94 source shows base UriAccessLogFilter wraps requests in common EsRequestWrapper, which reads bytes via InputStreamReader into String then returns body.getBytes(). The sole writer subsequently confirmed source/snapshot/Node PUT all109B with matching hash, and identified the actual wrapper conversion as the corruption source. A bounded streaming/byte-preservation repair is under targeted test; candidate/report evidence will be archived after freeze. The Python illustration alone is not Java server byte-count evidence.

A repair must preserve streamed upload bytes and bounded memory without disabling authentication. Tests should include the real filter/wrapper path that mock controller-only tests can miss. Preserve binary snapshots for supported retry. If SIZE_MISMATCH is already contractually terminal, retain that failure and use a new real dispatch to verify the fix; do not manually rewrite queue/run/upload state and label it recovery. Test supported restart/retry separately.
