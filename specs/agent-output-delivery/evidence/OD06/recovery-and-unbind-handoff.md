# Remaining bounded R1 integration checks

Current accepted API f1978b09, Web212bfe4, client714b4aa. Real conversation text and TASK3 text/PNG/ZIP publication and offline downloads passed. Root browser4 runs/8 viewports passed with real exact-route previews/downloads; see browser-resource-pass-f1978b09. Do not rerun those passing flows merely to refresh evidence.

The sole operator is `/root/od01_source_auth`; Root has returned live ownership and only writes control evidence. Keep the original user Agent untouched. No further Gradle run is needed unless a new product change/failure requires it. If one is needed, use the existing development helper exactly, with `/tmp/cyf-gradle.lock`, cached substitutions and targeted tasks.

1. Reuse accepted cross-process client7/7, full305/305, and real dependency epoch/CAS/GC evidence for their respective layers. They do not by composition prove the entire authenticated server/client restart path.
2. Add one real trusted TASK dispatch with controlled loss of a real complete HTTP response, followed by an actual restart of the task-owned synthetic Agent. Forward actual business requests/responses to the accepted API; only inject transport failure. Verify durable snapshot and operation identity resume, no additional model command execution, and exactly one published version. Do not edit queue/run/upload state or replace business responses with mocks to manufacture recovery. Report any genuine limitation before changing the contract.
3. After recovery, unbind only the disposable `od06_persona_2d7db03a9a` through the authenticated API, verifying its fixture owner/Agent identity first. Confirm runtime/binding revocation and re-read/download TASK3's three OWNER_SHARE files with unchanged hashes. Losing the synthetic API key is expected; OD07 may create a new isolated binding. No further routine approval is required for this authorized isolated verification.
4. Preserve task1 workspace-policy refusal and task2 terminal size-mismatch failure through read-only state observations. Never re-create old HTTP evidence after its original helper file was overwritten.
5. Freeze any actual product repair for independent read-only review, then rerun only affected checks. Archive unique task/run/attempt-named sanitized results and exact byte/code/package associations.

Canonical original dependency access, physical WeChat and actual production migration/deployment remain separate external release gates. Broader Hall/history navigation and any unexecuted live failure case must remain accurately attributed to component evidence or pending, not declared live PASS. After these bounded checks, request an independent R1 development-gate assessment and state precisely which gaps remain before deciding OD07 entry; do not label full R1 release accepted.
