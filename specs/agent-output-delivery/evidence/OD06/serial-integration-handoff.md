# OD06 serial integration handoff

## Current checkpoint (2026-09-13)

Accepted API f1978b09 (binary log-wrapper repair30/30 + bootJar, independent APPROVE), Web212bfe4, client714b4aa. Runtime PID478651/startTicks56397314 uses immutable jar1438fa8d59e8de9e1ab8ab65c5c19698725d5b59e1031b7654f8b232e6a71f6d at127.0.0.1:10018. The task-only properties now include exact Vite15173 CORS plus the starter's25 JWT resource URI patterns. Root independently verified process/package/config correspondence.

Real CONVERSATION3 Markdown and TASK3 text/PNG/ZIP publication plus Agent-offline downloads passed. Four source/snapshot/online/offline byte copies matched per TASK output. Browser4 runs/8 viewports all succeeded with actual hit-tested clicks, preview checks and SHA-matched downloads; all screenshots were inspected and task Chromium cleaned up. See live-bounty-task3-f1978b09 and browser-resource-pass-f1978b09. Earlier r3–r6 failures remain retained, not relabeled.

The sole live operator is now the existing writer, executing recovery-and-unbind-handoff.md: one true complete-response-loss/Agent restart seam, followed by disposable persona unbind and retained-file reads. Root does not send live requests during this ownership. Full R1, OD07–OD11 and release are still unfinished. Do not restart an API merely to obtain a log, overwrite old attempt evidence, rerun canonical dependency probes or manually repair terminal fixture state.

## Earlier integration lessons and build shortcut

Real generic conversation creation exposed tenant0 while exact output ownership requires the authenticated owner tenant; the reviewed43e6bb94 repair fixed new creation without widening legacy output access. Later real TASK binary upload exposed text transcoding in global logging; the reviewedf1978b09 repair preserves the stream and passed30 affected tests. These lessons are retained to avoid relying only on seeded/controller-only fixtures. Use the existing development wrapper for every local Gradle repair/build, with task-specific --tests and :starter:bootJar in the same call when possible:

```bash
# cwd=/home/chc/wsps/cyf-worktrees/output-api; helper owns flock/JDK/substitutions
python3 /tmp/cyf-od02-evidence/run-gradle-with-od-env.py <affected-test-task> --tests '<affected-class>' :starter:bootJar
```

Use --offline only when that task's dependencies are already cached. Do not run canonical first as a trial. The fixed original dependency gate is already blocked by recorded old-host TLS/DNS and canonical-host403 observations; no new credential/provenance evidence has arrived. Root alone coordinates that external gate. Do not re-read settings.xml, transfer credentials across hosts, probe private endpoints or rerun unaffected verifier tests on each local fix.

Local startup is part of implementation verification and may run before the final repair commit/review, without production traffic or user Agent changes. Freeze a coherent startup repair after the relevant tests and actual launch attempts, then obtain independent review before trusted-run acceptance. This avoids treating every safe local boot attempt as a separate approval step. Current isolated schema/bucket persist; do not recreate them blindly.

Start from a task-owned immutable jar copy, or stop the task API before replacing its running bootJar path. Repacking a live jar can break lazy class loading; r7 shutdown showed missing classes from unrelated packaged libraries after repacking, whereas r8 stopped cleanly. This is an execution precaution, not evidence of missing production dependencies. Diagnostic error details belong only in the isolated loopback configuration and must be removed after diagnosis; do not archive credentials, cookies or authentication headers.

OD06 starts after OD05 independent approval. OD05 development approval was recorded on2026-09-12; step1 client retention is accepted at714b4aa and step2 API reconciliation/build/preview is now active. Later steps remain queued and no R1 integration acceptance is claimed. OD06 includes bounded implementation repairs before final integration verification; the current checkpoint above supersedes historical active-step notes. Keep one active product writer throughout; root owns this sequence, evidence and pins.

1. **Client retention repair** — sole critical writer in output-client, based on OD04 `29aa70da` (or its accepted descendant). Implement and independently review the complete R03 gate in `client-retention-gate.md`. Preserve immutable queue commandEvidence and terminal recovery. Freeze this candidate before changing another repository.
2. **API integration and canonical build repair** — sole critical writer in output-api. Re-observe accepted committed concurrent development tip, reconcile output candidate `4c292cc3` with history/session/ACL changes using `integration-baseline-watch.md`. Ten previously forecast conflicts require combined behavior and regressions, not whole-side replacement. Repair canonical dependency provenance/configuration if still necessary; read `public-image-dependency-probe/README.md`. The catalog lives in the tracked API `plugin/jia-gradle-plugin/src/main/java/cn/jia/plugin/gradle/SharedDependencyManagementPlugin.java`; the production aggregate is `:starter:bootJar`, with agent mapper/service and chat starter dependencies. The existing external init's absolute-path includeBuild and OpenCV/JAI substitutions are development-only. A reproducible canonical build must resolve its checked-in plugin/catalog without that init, preserve target native loading/scopes and run affected image probes/regressions. Every Gradle invocation holds `/tmp/cyf-gradle.lock`. Independently review and freeze the integrated API candidate.
3. **Live R1 integration** — one verification operator, no concurrent writer. Start isolated API and synthetic client instances, leave the user's existing Agent process untouched. Exercise actual trusted conversation and bounty dispatch, ticket exchange, manifest snapshot, HTTP bytes/scanner/publication, all required user read routes. Collect a text file, bitmap/PDF, and manifest-designated changes.zip containing change.patch/README.md under the frozen allowlist. Actually stop only the synthetic Agent and move its synthetic execution directory; verify byte-identical user download afterward. Exercise transfer interruption/restart, other-user denial, pagination, expiry/configuration and storage/scanner failure scenarios. Existing `tools/smoke-output-http.py` assists tiny-file routes but does not create trusted runs or prove lifecycle/UI by itself.
4. **Browser and packaging verification** — use accepted Web with the actual API; cover Hall, standalone chat and bounty, identity/history switches, workspace disabled/no selectable Agent, desktop/mobile sizing and download/preview. Chromium is available; simulated mobile sizing does not prove WeChat device behavior. Verify token-free external browser same-account route; record any physical-device evidence still missing without relabeling it. Audit strict migration fresh/upgrade/partial-state behavior, aggregate datasource/transaction-manager topology, proxy size/timeout and private bucket/scanner configuration. Local service listeners were reachable on 2026-09-12 (MySQL13306, MinIO19000, ClamAV13310), which is only readiness, not application health or production proof.
5. **Release candidate and pins** — release_guard performs independent R1 gate assessment. Archive exact API/Web/client commits, actual package hashes, commands/reports, fixture correspondence and acceptance matrix. Publish only authorized feature candidates as appropriate; never infer production deployment permission from development tests. Create detached worktrees at empty root api/web gitlink paths only after verifying they are empty and selecting reproducible pushed candidates. Check each git --show-toplevel and HEAD before sddw pin/verify, avoiding the upward repository lookup trap. Keep actual deployment and physical-device gaps explicit.

If live verification exposes a defect, hand it to the sole relevant writer, review its frozen fix, then rerun only affected checks. Do not relabel previously passed mock/slice results as live evidence. Development progress on R2 must not falsely mark unresolved R1 release gates complete.

## Browser harness reuse

Installed Web has `ws` and `@vue/test-utils`; Playwright/Puppeteer are absent. Existing `tests/juyiting-public-beta-ui-smoke.mjs` already drives `/usr/bin/chromium` via CDP, with isolated browser profiles, origin/token checks and tracked process cleanup. Prefer adapting/reusing that established mechanism for the output acceptance probe instead of installing another browser stack. Its current map/snapshot/event assertions are not output acceptance; add actual output route/UI/download observations in the serialized verification phase. The Vite configuration supports `VITE_API_PROXY_TARGET` and `VITE_OAUTH_PROXY_TARGET` for isolated same-origin development integration. Avoid using production defaults inadvertently.

Root prepared `tools/smoke-output-browser.mjs`, reusing the existing CdpSession/Chrome cleanup for live resource-route, preview and click-download/hash checks at desktop and simulated mobile sizes. It accepts a local JSON config containing webRoot, tokenFile, outputDirectory, resourceRoute, title, expectedSha256 and optional expectedText. Only the isolated Vite15173 origin is accepted; API responses are not mocked. It has passed Node syntax checking only and must run after real publication. Its real OAuth token bootstrap does not prove browser OAuth login, and simulated mobile is not WeChat device evidence. Keep the unique outputDirectory outside the source trees until its screenshots/report are reviewed for archive.

## API/UI compatibility seam discovered during OD05

Accepted API `OutputDeliveryServiceImpl.summary` currently derives TEXT only from inline row.content; an uploaded text/Markdown file has previewKind NONE. The design promises bounded text-file previews. Verify this path in the serialized API integration repair and align the authenticated summary/detail/download behavior (without allowing arbitrary active formats or unbounded inline reads), then add a real text-file preview example to the integration matrix. Do not ask Web to ignore the server's NONE declaration or assume every text file is inline content. Download remains application/octet-stream; image previews must use validated summary/detail MIME and decoded image bounds on the Web side.

## Integration execution preparation (read-only)

Rechecked on2026-09-12: concurrent API remainsa9d3e741, Web baselineb31565f8. Root free space1.9GiB and /tmp2.0GiB; recheck before aggregate packaging. Existing services and JDK/Gradle tooling are reusable. The aggregate dev configuration also declares Redis/LDAP/RabbitMQ/Elasticsearch integrations: isolated startup must explicitly supply development overrides or disable unused integrations, never inherit production destinations. Do not archive credential-bearing property files or command arguments.

The existing OutputDeliveryRealDependenciesHttpIntegrationTest uses MockMvc and seeded runs. It remains valuable component evidence, but a live probe must separately observe the actual server dispatch and WS ticket exchange. Run the synthetic client against the isolated server, record only nonsecret run/output identifiers, and associate expected bytes with its actual manifest snapshot. Browser tests must use that same frozen candidate and published data; a mocked browser response cannot prove the API/UI text preview seam.

For uploaded bounded text previews, the current Web TEXT branch reads detail.content; changing summary.previewKind alone is insufficient. API repair must populate authorized bounded detail content using the same read authorization/object protection as download, with strict supported MIME/size/UTF-8 handling and an oversized-file download fallback.

Root provisioned the aggregate's missing isolated Redis dependency on2026-09-12:127.0.0.1:16379, authenticated readiness passed. See [redis-readiness/README.md](redis-readiness/README.md) for actual version8.0.2 and the documented isp-install7.4.8 download deviation. Read the task-owned credentials file only into the launch environment; do not print or archive it. RabbitMQ/Elasticsearch listeners remain absent, so unused integrations still require explicit local disable/loopback overrides.

Headless HTTP navigation on this host hung before Fetch.requestPaused with both native WebSocket and ws. A credential-free probe loaded data URLs but stalled HTTP; adding --password-store=basic to the disposable Chromium profile allowed HTTP navigation. This is local test-runner configuration, not a product authentication change. Keep that flag in the output browser probe; do not enable verbose/net logs in credentialed runs. The real user token bootstrap still does not prove browser OAuth.

Browser r5 output GETs succeeded, but conversation-list/phrase POSTs returned403. Static configuration check found global highest-precedence CorsConfig enabled with an empty default origin list and no local override in the task properties. A browser POST through Vite changeOrigin can carry Origin15173 against Host10018; this is a hypothesis until the sole operator compares with/without Origin and observes the rejection body. If confirmed, configure only the exact isolated Web origin and combine with the next necessary API restart. Do not change product ACL or allow wildcard credentialed CORS to make the fixture pass.
