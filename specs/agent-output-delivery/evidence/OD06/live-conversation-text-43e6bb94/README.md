# First trusted conversation publication and offline retrieval

API `43e6bb94`, Web `212bfe4`, client `714b4aa`. One isolated synthetic Linux Agent generated one Markdown file through real `/chat/stream` dispatch, trusted run/ticket, client snapshot, upload, scan and publication. Owner HTTP list/versions/detail/download succeeded; foreign-user list/detail/download returned 404 `OUTPUT_NOT_FOUND`.

Root independently read the moved source, retained snapshot, online download and offline download: all 103 bytes, SHA-256 `53b73c6b7b3a87bea403f2d822fdd40ec281ecfedd7329fa869b3a696bdaa64e`. The synthetic process was absent and its original workspace was moved. See `observation.json` for exact source/run/version and evidence hashes.

HTTP summaries were created by the writer. The older owner-read helper incorrectly projected `filename/mimeType` and root detail fields; its null fields are not evidence of missing wire metadata (`name/mime/detail.item`). Source report text is a synthetic fixture, not proof of real model execution.

Root subsequently observed startup log lines and PID2302882 using the same immutable jar; PID2031242 was no longer running. `startup-observation.json` preserves observed metadata and the writer's restart explanation separately.

This is a partial R1 result. Browser/OAuth, bounty, other formats, recovery/retention matrix, R2 and release are not accepted by this evidence.
