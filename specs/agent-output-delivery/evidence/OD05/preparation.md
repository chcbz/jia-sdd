# OD05 frontend preparation (not started)

Read-only map at Web `77666e8fc1b2060ff3db8374b5de4539a5e5e53d`, worktree `/home/chc/wsps/cyf-worktrees/output-web`. OD05 remains dependent on accepted OD04; no frontend source or tests changed here.

- Hall composition is `src/components/world/JuyiHall.vue`; conversation identity/lifecycle is `src/composables/juyiting/useHallConversation.js`. Pass explicit conversation/source identity to shared output UI rather than deriving authorization from the selected map/roster Agent. Its existing abort/generation lifecycle is a useful model for discarding stale output responses on user/source switches.
- `src/components/juyiting/ChatPanel.vue` is shared by `BountyDiscussionPanel.vue`; it currently receives messages and selectedTask but no explicit conversationId. Wire the output source explicitly through composition instead of guessing it from a message or task label.
- The standalone `src/components/chat/Chat.vue` also handles `conversationType=juyiting` and Agent-delivered events. Check this routed conversation surface as well as the Hall overlay so the feature does not disappear when the same conversation opens outside Hall.
- Bounty detail lives in `src/components/juyiting/BountyPanel.vue` (modalTask/detailTask), including assignment controls. The unrelated `src/components/task/TaskDetailDialog.vue` displays the general scheduled/financial task model; do not mistake it for the bounty detail entrypoint.
- Legacy `TaskWorkspacePanel.vue` is guarded by taskWorkspaceEnabled plus an actorAgentId subject. Reuse the shared output list there where useful, but keep owner file retrieval available when the workspace gate is off or no selectable Agent exists.
- `src/composables/useHttp.js` supports JSON/text/stream, not a documented Blob response mode at this baseline. Downloads need a bounded binary response path that preserves UserJwt acquisition, combined abort signals and identity-generation checks (`src/stores/api.js` and identityLifecycle helpers). Do not send an authenticated binary response through JSON-envelope parsing or bypass the identity lifecycle with an unrelated fetch helper. Revoke temporary object URLs and discard stale responses on identity/source changes.
- Frozen output errors use `message/retryable/requestId`, while successes contain only `code:"E0"/data`. `useHttp.js:168` already reads `message` as a fallback but retains only error `code/status/response`; output recovery needs the declared retryability and request ID without inferring them from a legacy body `status`. Its returned wrapper has HTTP status and a nested JSON body, so keep these distinct. OD02 review R06 requires the API to fix its legacy envelope; do not compensate by freezing that incorrect shape into the client/UI. Root `tools/verify-http-contract.py` accepts saved `{operationId,status,body}` observations for an offline shape check, separate from runtime or authorization evidence.
- Relevant regression seams include `tests/juyiting-hall-conversation.test.js`, `juyiting-task-workspace-wiring.test.js`, `juyiting-task-workspace-feature-flag.test.js`, `juyiting-task-workspace-panel.test.js` and `juyiting-task-workspace-integration.test.js`. Add the shared output behavior tests defined by OD05, then perform the frontend build required by the user's workspace instructions; a local build is not production-release evidence.

Preserve separate map/roster data flows and explicit target Agent assignment. Initial R1 labels distinguish shared output from formal acceptance; R2 delivery/review actions remain OD10.

## Installed development dependencies

Preparation completed on the unchanged web candidate `77666e8fc1b2060ff3db8374b5de4539a5e5e53d`: `npm ci --no-audit --no-fund` in `/home/chc/wsps/cyf-worktrees/output-web` exited 0 and installed 678 packages (Node v22.22.0, npm 11.17.0). The worktree remains clean and Vite is available. `package-lock.json` SHA-256 is `c69c07a9b41b963b78f7d8bbc033b7e029ae5df3e3d82054990bb2cd7df1d7e3`. This is dependency preparation only; no frontend feature test or build has run.

## Baseline update before OD05

Read-only follow-up on 2026-09-12: local remote-tracking `origin/develop` now points to `b31565f8741fdc6f986763dd86442a2c2a4345b0`, the committed conversation-history/deletion branch observed in the shared checkout. It descends from the untouched output-web baseline. Before OD05 edits, verify the still-clean feature worktree and choose the current committed development baseline, preserving these changes; record the actual base and do not switch the shared checkout. Local `origin/master` currently points to `e88425b` (reader fix), so neither branch name alone proves feature release.

At `b31565f`, ChatPanel already declares `conversationId` and JuyiHall passes it through its panel variants. Reuse that explicit prop instead of duplicating the earlier baseline's missing wiring. Preserve history selection/deletion/busy behavior and ensure switching/deleting a conversation cancels and clears output requests. Include the history regression tests when attaching OutputList. This reduces duplicated wiring and later merge work.

The user-provided AGENTS explicitly requires `npm run build` for frontend edits; follow that instruction even though the older runbook describes CI builds as the default. A local production build still does not establish deployment.
