# OD05 repair review — REQUEST_CHANGES (R02 remaining)

Independent read-only reviewer `/root/od05_review_fallback` rechecked candidate `188948293d97c69a97df55a37d09ecd10c245ad1`.

Closed: R01 identity-clear-before-save side effects; R03 explicit nonretryable error suppresses syncing polling; R04 same-component reactive v1→v2 navigation; versions request defensive fence. Ordinary-list empty refresh and refetching loaded page depth now work.

Remaining P2 R02: `OutputList.vue:218` returns when a refreshed page lacks the requested target, preserving requestedItem; `OutputList.vue:109` reinserts that cached item into displayItems. With the current requestedResource interface: open a precise resource, revoke its authority (list empty, detail404), then refresh. outputs.items becomes empty while the old title and downloadable card remain alongside the no-outputs message.

Repair requires revalidating the exact detail on authoritative refresh and clearing the card plus preview when the detail is inaccessible. Absence from the latest first page alone cannot delete an otherwise valid historical version. Cover both retained historical authorization and subsequent denial, while preserving cancellation/source/identity/version fences.

Counterexample source/log/exit are archived under `review-1889482-counterexamples/`; exit0 is observation, not product PASS. Controlled component/HTTP evidence is not real API E2E. Product and root stayed unchanged during the review. Root resumed the same sole writer for this bounded repair; all OD06 gates remain later.
