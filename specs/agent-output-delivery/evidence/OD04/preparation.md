# OD04 client preparation (not started)

Read-only map of `isp-install` at `a100a5007621dbb66717544f6332108fb7b4e5e7`, worktree `/home/chc/wsps/cyf-worktrees/output-client`. OD04 implementation still depends on accepted OD03; this preparation starts no second product-code task.

- `conf/codex-ws-agent/agent-client.mjs`: `runCodex` handles command and chat, but only the command branch currently acquires a trusted workspace lease. Its `finish` currently releases that lease before producing the final reply. Insert manifest validation, copy/fsync snapshot and durable queue persistence **before** release; keep HTTP publication after release. Chat needs its separate per-run output-root lock rather than assuming the command workspace lease exists.
- `runManagedCommand` routes `SKILL_INSTALL` separately. Add ordinary output handling at the command/chat execution seam; do not treat installation output as a user-manifest export by accident. Trusted `outputContext` must survive the message adapters unchanged and be validated before selecting the run's fixed `outputs/<runId>/manifest.json` root.
- `workspace-manager.mjs` already owns trusted command worktree acquisition, no-follow root checks and lease release. Reuse those boundaries; client output code must not derive authority or arbitrary filesystem roots from a model-written manifest. Snapshot only listed files within the run output root, with private durable queue state outside model-writable directories.
- **Installer packaging seam:** `shell/codex_ws_agent_install.sh:stage_application_files` explicitly copies each runtime module. Adding `output-*.mjs` to the source directory alone will leave installed releases missing imports. Extend the explicit staging list and any required CLI compatibility entrypoint for the manifest helper, then verify the staged release. Preserve immutable releases and private persistent `.env`/profiles/state handling.
- Existing test seams: `test/agent-client.test.mjs`, `test/workspace-manager.test.mjs`, `test/managed-host.test.mjs`, `test/config-runtime.test.mjs`; package script `npm test` invokes `node --test`. New snapshot/queue/HTTP tests must verify restart without model execution, and both command and chat finish order.

Installer repository rules require Bash syntax checking for changed scripts, template/path checks and targeted install validation. Do not invoke broad installation on this development host: existing local MySQL/MinIO/ClamAV are already installed and evidenced under OD00/OD02. `install.sh` has no MinIO/ClamAV component mapping at this baseline, and `shell/mysql_install.sh` has an x86_64 binary URL; these are deployment planning facts, not reasons to rerun or replace working ARM development services. Effective production storage/scanner configuration remains OD06's gate, including the paired archive limits in OD02/clamav-limit-review.md.

No client source was edited or test/build executed by this preparation.
