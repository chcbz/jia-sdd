# Independent OD03 review — REQUEST_CHANGES

Candidate: `683e8007dbbde98895d82a391fc7d2a4228f46fa`. Reviewer: `/root/od03_independent_review`, independent read-only architect fallback after the preferred provider failed. No cross-model approval is claimed. No files were changed by the reviewer.

## Blocking findings

1. **R01 — snapshot pagination omission.** `agent/jia-agent-mapper/src/main/java/cn/jia/agent/mapper/AgentTaskOutputMapper.java:40` and `chat/jia-chat-mapper/src/main/java/cn/jia/chat/dao/impl/ChatOutputDaoImpl.java:76`: latestOnly NOT EXISTS does not constrain newer rows to the cursor snapshot. A post-snapshot v2 excludes v1, while v2 is itself outside the snapshot; later pages lose that output. Bind the same snapshot in both subqueries and prove both sources with real MySQL cross-page tests.
2. **R02 — unauthenticated Output read errors.** `oauth/jia-oauth-resource/src/main/java/cn/jia/oauth/config/ResourceServerConfig.java:102`: missing/expired/invalid JWT fails before controllers, returning default errors instead of the Output envelope/requestId/X-Request-ID/no-store. Match only Output GET/capabilities routes, retain non-Output behavior, and test missing/expired JWT through the real filter chain. The current HTTP test asserts only 401.
3. **R03 — deterministic publication request-hash collision.** `agent/jia-agent-service/src/main/java/cn/jia/agent/output/service/OutputDeliveryServiceImpl.java:519`: NUL-delimited fields and `<null>` as a null sentinel collide for A(content="<null>", objectId=null) versus B(content=null, objectId="<null>"). Receipt return at line 105 precedes material validation, so B wrongly replays A. Use the frozen RFC8785 canonical JSON contract; test exact replay and same-key/different-body 409 with this concrete pair.

## Other reviewed areas

No new bypass found in exact Task/Conversation owner/share ACL, tenant/case isolation, READ_PIN-before-stream and GC lock order, receipt replay during write pause, terminal receipt miss or transaction authorization. ASYNC permitAll is currently acceptable because initial REQUEST passes JWT, business ACL and pin creation; no cross-path AsyncContext.dispatch(path) was found. Future cross-path async dispatch would require new review.

MockMvc real-dependency tests are not live TCP/proxy deployment or actual Agent-offline lifecycle evidence. Those gates remain OD06.

## Repair ownership

Original sole writer `/root/od01_source_auth` resumes in `/home/chc/wsps/cyf-worktrees/output-api`. Repair only R01–R03 and directly affected regressions, preserve actual execution artifacts before later tests overwrite them, freeze a new candidate, then return to the same independent reviewer. OD04 remains pending until development review passes.
