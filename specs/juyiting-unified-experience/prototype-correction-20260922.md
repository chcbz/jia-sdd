# 1.13.10 原型纠偏版验收（2026-09-22）

**已本机发布 `1.13.10-uxfix3`，本轮相关回归通过，等待用户验收。** 不等于已完成付费生成闭环或业务推广试点。

## 交付内容

- 常驻聚义厅／悬赏榜／厅内议事／百宝箱导航；地图与办事概览切换、方向入口保留。地图仍是真实 melonJS，不替换成原型示意图。
- 概览合并为轻列表，保留来源失败、真实状态、权限和按来源分页，不再一来源一大卡片。
- 百宝箱：全部／资料／成果／回收站；列表→预览→更多管理，上传独立进入，回收内联确认。只有一个活动窗口。
- 需求→临时选材→固定版本预览→返回草稿→独立交办确认。取消选材不改原引用；换好汉可见保存；费用未知明确说明，显式授权才提交。
- 修复确认层关闭保存、旧资料版本取消、历史边界测试遗漏；人工检查线上截图发现的竖屏地图下方空白，也已修复并新增回归。

## 精确来源和测试

- Web `6726cc0583e992d11ad38e00f358dd9125bf27bb`，tree `d384e43aeefbdbfa45075c674f35ff81010a5e0e`，冻结 `release/1.13.10-uxfix3`，已合入组件 develop；旧 release 分支均未移动。
- API仍 `787ae631264d0ae323fcce48957cb71c1cae52c8`；本次没有重建／重启 API、Nginx 或 Agent。
- 本机正式生产构建 exit0，`build_origin=local_user_authorized`，Flow Run=null；未恢复 Flow 自动触发，未使用 Eva。
- 最终同树：300项单元／组件回归通过，31项真实Vue＋模拟API浏览器流程通过，零跳过、零失败；fixture检查无外网请求或未捕获JS异常。
- 发布后真实授权账号：只读HTTP6/6，线上UI27/27，16张截图；1440×900、844×390、390×844、320×700；浏览器已关闭，临时身份profile已删除。
- Web制品 SHA-256 `523499f9eed93475698543e6d034c8b5deb878423e24ea6a82b251840dce8b71`；安装树 `b0132512af2432eff13ff54cd84ca8689d2f0a24b2ffa3f2aff8227f877f26ff`。
- 安装记录：`/opt/cyf/service/api/release-records/jyt-ux-1.13.10/JYT-UXFIX3-20260922/installed.json`。锁保护、可恢复目录、API进程身份不变、实际健康、入口JS/CSS字节与制品绑定均通过。
- 完整证据：`deliverables/verification/juyiting-prototype-correction-20260922/final-result.json`；便携摘要 `verification-prototype-correction-20260922.json`。

## 用户验收操作

1. 刷新聚义厅（若仍显示旧版，强制刷新一次），确认顶部四个主入口、地图／概览切换及方向入口。
2. 打开百宝箱：检查分类、轻列表，进入文件预览→更多管理→逐层返回；不应出现多张功能大卡片或“回聚义厅”。
3. 提出需求→添加资料→预览→取消，原引用应不变；再次选择并使用后，显示固定版本。
4. 下一步进入独立确认，换好汉→保存更改，才可授权；也可返回修改或保存草稿后刷新恢复。
5. 竖屏首页地图应连接工具栏和底部主动作区；横屏窗口应接近全幅，内容可滚动，不应挤成窄抽屉。

**边界：** 本轮线上不保存／上传／执行付费Provider；保存、提交、回执及异常流程由隔离fixture和组件测试验证。真实付费生成、下载修改两版闭环，真机／微信宿主方向及键盘仍需另行验收，不把Chromium视口仿真冒充真机。业务转化提升需要试点数据，不从测试数量推断。

## 原型差异与历史证据

本次对齐原型的信息结构和分步流程，不宣称逐像素相同：真实地图、服务端可用格式、费用提示和明确保存状态取代Demo的示意数据。初版b8bd0de原型对照FAIL、uxfix2通过DOM但人工发现竖屏空白的证据均保留，没有改成PASS或覆盖冻结分支。
