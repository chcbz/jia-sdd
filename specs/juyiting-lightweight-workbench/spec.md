# 聚义厅轻量工作台 · 真实功能接入

目标：把 V3.1 轻量工作台的默认首页、纸面字体/排版、移动底栏（典籍阁直达）和扩大的厅内议事区落到正式 `/juyiting`；复用现有 Vue 组件和权威接口，保留全部已存在的聚义厅业务能力及沉浸地图，不把静态 Demo 的假数据/假回复接入生产。

基线：`docs/ui-workbench/feature-coverage.md` 63组核对、`docs/ui-workbench/ui-detail.md` 原型规格。功能账以实际 JuyiHall.vue 挂载链为准；全站其他路由保持原样。

不可退化：地图/点将册独立，正式TASK_CREATE未指派、私人交办显式授权/固定版本，话头新建与历史、典籍阁真正阅读与检索，成果已查看/归档/正式验收不同意图，退出设备保留原路由，条件功能原开关原权限。

非目标：启用默认关闭的语音/经济/协作开关；新增后端API/迁移；把静态地图图片和假回复导入正式页；修改其他全站模块的业务逻辑。
