# 任务

- [x] 独立 root/web/API worktree 建立、保留其他分支用户脏改；原型与审计文档复制进工作树。
- [x] 实现工作台首页、侧栏/移动底栏、地图来回切换、原面板导航；本地空API浏览器四视口和实例检查已采样，真实服务待验。
- [x] 收紧顶部/弹层视觉与字体token；厅内议事空会话区域四视口测量与底栏安全距离已核；实际长内容/软键盘待测。
- [x] 逐项核查63组既有业务入口和条件能力的组件挂载/入口，结果在 `feature-integration-audit.md`；真实授权、写入及失败恢复仍需联调。
- [ ] 新增行为/视觉回归；当前候选前端 build 已过、七份相关测试 146 通过；早前全量 2331 过/47 失败（含本机指定浏览器缺失、TMX 超时和旧断言/桩等不同原因，详见 acceptance）；授权测试账号已通过完整生产 OAuth，并用进程内只读代理让候选读取真实服务的概览/典籍/会话目录/实景快照；写操作及候选正式部署仍待授权验收（详见 `evidence/real-readonly-browser-20260924.md`）。
- [ ] 独立只读评审完成（无P0/P1，P2三项已修，菜单首次卸载焦点也已修）与截图/测量已存档；仍待真实服务联调、受控浏览器环境全量回归和最终验收。web 当前候选 `8f47a12` 已推送到非发布特性分支（远端 SHA 已核对）；API 无需变动、无 API 迁移。**没有触发候选 CI 或部署**；不得把推送当发布或业务验收。

- [x] 修复短屏展开菜单的内容盒超宽和溢出：六视口实际 Vue 空 API 检查、320×320 滚动与末项聚焦、七份相关组件测试 145 passing（单份 89 passing 为上一 SHA）、前序 a935440 构建通过；不代替真实账号/固定 Chrome 133 全量门禁。

- [x] 极窄/极短复排：200%-等效的半 CSS 视口 + DPR2（160×370/195×422/422×195/720×450）在真实 Vue 空 API 浏览器测得页头与底栏不越界、议事输入可达、过矮屏的工作窗可滚动；底栏前三项保留 aria-label，右下仍显示“典籍阁”。七份相关组件测试 146 passing、构建通过；**不等于**实际浏览器 200% 缩放/系统软键盘/服务端验收。见 `evidence/compact-reflow-review.md`。

- [x] 系统 Chromium 实际页签 200% 缩放：隔离临时扩展 `tabs.setZoom(2)`，四个原始视口 + 28 条合成消息真实 Vue 检查通过；所有 XHR/fetch 在浏览器内拦截，未访问真实后端。固定浏览器/实体键盘/候选服务流程仍待验；证据见 `evidence/actual-zoom-review.md`。

## 2026-09-24 开发交付与服务器验证交接

- [x] 轻量工作台正式 Vue 实现收口；七个主入口、右下“典籍阁”、扩大的厅内议事区、地图保持同实例与未改动的业务组件均有对应实现。Web 候选 `8f47a12`。
- [x] 将 Web 候选提交 pin 到 root 的 `web` gitlink，连同规格文档推送 root 的同名特性分支；服务器合并该 root 分支时须递归更新子模块，否则页面仍是旧版。API 保持原 SHA。
- [ ] 服务器按固定环境构建/测试、合并并发布后，按 `real-service-qa.md` 验证真实写入、SSE、手机键盘及权限等；这些不是本地已完成的开发回归，也不是本次分支推送自动触发的验收。
