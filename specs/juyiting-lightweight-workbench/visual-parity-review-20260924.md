# 轻量工作台 · 全面视觉复核（2026-09-24）

## 范围与结论

以 `deliverables/ui-workbench-demo/` V3.1 为**视觉基线**，对当前 Vue 候选的 7 个真实顶层入口（办事概览、我的事项、厅内议事、点将册、百宝箱、典籍阁、消息通知）在 320×740、390×844、844×390、1440×900 四视口进行浏览器截图/DOM 对照，共 28 个表面。保持实际 API、权限、地图 Stage、服务端状态和草稿/案卷/阅读器，不用 Demo 本地假数据替代业务。详细属性/动作继续参考 `ui-detail.md` / `ui-computed-attributes.md`。

| 对照项 | 本次核对和处理 |
|---|---|
| 导航图标 | 桌面侧栏、手机底栏、全部入口、消息、账户与首页两个动作分别用 Varlet 的 `home-outline`、`format-list-checkbox`、`chat-processing-outline`、`notebook`、`bell-outline`、`account-circle-outline`、`plus` 等；校验相关 9 个字形在安装的 Varlet CSS 中存在。工作台活动面板关闭按钮改用 Demo 的 `window-close`，地图原有关闭图标不动。右下角仍是“典籍阁”，指向原阅读/检索双页签。 |
| 首页内容和字体 | 补齐 Demo 的“聚义厅 · 轻量工作台”、暖白需求卡、“事”印章与 01–03 步骤、工作空间面包屑、侧栏账户块；小屏收起步骤但保留业务动作。空 API 截图不同于 Demo 的示例任务列表，不伪造真实事项。 |
| 排布/色彩 | 沿用暖白 `#FFFEFA`、地色 `#F5F4F0`、正文 `#242E2B`、朱砂 `#923F30` 的工作台 token；工作台内真实面板维持内容/错误提示结构。百宝箱搜索按钮固定最低宽 68px 且不换行，搜索输入可以收缩；上传、分类和焦点态跟随工作台色系。 |
| 聊天空间 | 工作台在桌面用全宽面板、手机避开 62px 底栏；不缩小消息字号换取面积。原工具条、引用、消息历史和发送组件仍在；现有长消息/短屏、200% 缩放证据见 `acceptance.md` 与 `evidence/`。 |
| 地图与功能 | 地图作为可切换的同一真实 Stage；“我的事项”仍是原悬赏/草稿办理面板；“典籍阁”仍是真实阅读/检索（不是 Demo 的帮助页）。真实榜文按钮/点将册/百宝箱/消息会比静态 Demo 有额外入口与状态；这是业务保留，不应删除以追求截图相等。 |

## 可复核证据和边界

- 空 API 的真实 Vue shell：`python3 specs/juyiting-lightweight-workbench/tools/check-visual-shell.py --url https://127.0.0.1:<dev-port>`；本次 28/28 的页面宽度、页头、底栏碰撞、百宝箱搜索按钮宽度/换行断言通过；截图在 `evidence/parity-20260924/`（示例：首页桌面/手机、桌面事项和典籍、手机议事和百宝箱）。测试使用受限的本地假 token/空响应，仅评估页面结构与样式；空数据带来的错误提示不能视为生产故障。
- 2026-09-24 通过 Chromium 访问线上 `https://kit.chaoyoufan.cn/juyiting`，匿名首跳跳至 `https://api.chaoyoufan.cn/oauth2/authorize` 后返回 **502 Bad Gateway**；本轮未进入已发布版登录后页面，不能宣称已核验线上最终 UI，也不能把本地候选视作已发布。请服务器合并、发布后使用测试账号重验：分别核对四视口导航、首页有数据和空/失败、菜单可滚动、事项目录、长议事、阅读/检索、上传搜索、角色状态、地图来回同实例，并优先确认线上 OAuth 可用。
- Demo 含模拟任务/文档/回复；正式版的数据、费用、权限、会话和文件流不通过本次空 API 截图验收。`acceptance.md` 中 A1–A6 的真实后端读写、实际手机软键盘与发布后复验仍应保留未完成标记。不要声称“一次性解决”了尚未取得线上身份/真实状态截图的所有差异。
