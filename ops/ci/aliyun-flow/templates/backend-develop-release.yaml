name: cyf-api-kit-ci
variableGroups:
  - q37x7n8snxqq744z
sources:
  repo_0:
    type: gitee
    name: cyf-api-kit-ci
    endpoint: https://gitee.com/chcbz/jia.git
    branch: develop
    triggerEvents:
      - push
    branchesFilter: ^develop$
    certificate:
      type: serviceConnection
      serviceConnection: vyz9pea3uni64twx
defaultWorkspace: repo_0
stages:
  cloud_ci:
    name: Backend develop CI
    jobs:
      api_ci:
        name: Changed-scope tests and bootJar
        runsOn: public/cn-beijing
        timeoutMinutes: 60
        steps:
          targeted_build:
            name: Changed-scope tests, layering, and bootJar
            step: JavaBuild
            with:
              jdkVersion: "21"
              mavenVersion: 3.5.2
              run: |-
                set -eu
                test "${CI_COMMIT_REF_NAME:-}" = develop
                test -n "${CI_COMMIT_SHA:-}"
                test "$(git rev-parse HEAD)" = "$CI_COMMIT_SHA"
                export CYF_FLOW_GRADLE_ACTIVE=1
                export CYF_FLOW_BUILD_ROOT="$PWD/_cyf_flow_build"
                test ! -e "$CYF_FLOW_BUILD_ROOT"
                mkdir -p "$CYF_FLOW_BUILD_ROOT"
                git diff --exit-code HEAD --
                export CYF_SOURCE_CLEAN_BEFORE=1
                sh ./gradlew --no-daemon --stacktrace --console=plain \
                  --init-script ops/ci/aliyun-flow/cold-init.gradle \
                  --project-cache-dir "$PWD/_cyf_project_cache" \
                  :agent:jia-agent-mapper:test \
                  --tests cn.jia.agent.cache.AgentPersonaCatalogCacheTest \
                  --tests cn.jia.agent.dao.AgentPersonaDaoCacheInvalidationTest \
                  --tests cn.jia.agent.dao.AgentRuntimeBatchMapperContractTest \
                  --tests cn.jia.agent.dao.AgentRuntimeMapperContractTest \
                  --tests cn.jia.agent.dao.AgentCommandOperationsMapperContractTest \
                  --tests cn.jia.agent.dao.AgentSceneScopedDaoTest \
                  --tests cn.jia.agent.dao.AgentTaskMetaScopedDaoTest \
                  --tests cn.jia.agent.dao.AgentTaskWorkItemDependencyDaoTest \
                  --tests cn.jia.agent.dao.AgentTaskArtifactOutcomeMapperContractTest \
                  --tests cn.jia.agent.dao.AgentCommandRecoveryMapperContractTest \
                  :agent:jia-agent-service:test \
                  --tests cn.jia.agent.api.AgentControllerTest \
                  --tests cn.jia.agent.api.AgentWorkItemPlanControllerTest \
                  --tests cn.jia.agent.api.AgentTaskArtifactControllerTest \
                  --tests cn.jia.agent.service.impl.AgentWorkItemPlanServiceImplTest \
                  --tests cn.jia.agent.api.AgentCommandOperationsControllerTest \
                  --tests cn.jia.agent.service.impl.AgentCommandOperationV1ProjectorTest \
                  --tests cn.jia.agent.service.impl.AgentCommandOperationsServiceImplTest \
                  --tests cn.jia.agent.api.AgentTaskArtifactOutcomeEventProjectionTest \
                  --tests cn.jia.agent.service.impl.AgentTaskArtifactOutcomeServiceImplTest \
                  --tests cn.jia.agent.service.impl.AgentTaskArtifactOutcomeRealTransactionTest \
                  --tests cn.jia.agent.service.impl.AgentCommandRecoveryPolicyTest \
                  --tests cn.jia.agent.service.impl.AgentCommandRecoveryServiceTest \
                  --tests cn.jia.agent.service.impl.AgentCommandRecoveryRealTransactionTest \
                  --tests cn.jia.agent.service.impl.AgentCommandReissueCoordinatorTest \
                  --tests cn.jia.agent.service.impl.AgentCommandOperationsRealTransactionTest \
                  --tests cn.jia.agent.service.impl.AgentCommandOperationsMySqlTest \
                  --tests cn.jia.agent.config.AgentCommandOperationsConfigurationTest \
                  --tests cn.jia.agent.service.impl.AgentSceneServiceImplTest \
                  --tests cn.jia.agent.service.impl.AgentServiceImplTest \
                  --tests cn.jia.agent.service.impl.AgentTaskTeamRecommendationGreedyTest \
                  --tests cn.jia.agent.service.impl.AgentWorkItemDependencyServiceImplTest \
                  --tests cn.jia.agent.service.impl.AgentTaskStateServiceImplTest \
                  --tests cn.jia.agent.service.impl.AgentTaskStateServiceRealTransactionTest \
                  --tests cn.jia.agent.service.impl.AgentWorkItemLeaseRealDatabaseTest \
                  --tests cn.jia.agent.service.impl.AgentTaskArtifactStorageF02Test \
                  --tests cn.jia.agent.config.AgentTaskArtifactStorageConfigurationF02Test \
                  --tests cn.jia.agent.service.impl.AgentTaskCollaborationServiceImplTest \
                  --tests cn.jia.agent.service.impl.AgentTaskCollaborationServiceRealTransactionTest \
                  --tests cn.jia.agent.service.impl.AgentWorkItemResultCommitServiceImplTest \
                  :base:jia-base-service:test \
                  --tests cn.jia.base.config.UriAccessLogConfigTest \
                  --tests cn.jia.base.filter.UriAccessLogFilterTest \
                  --tests cn.jia.base.service.impl.LogServiceImplTest \
                  :chat:jia-chat-service:test \
                  --tests cn.jia.chat.ai.AiProviderConfigurationTest \
                  --tests cn.jia.chat.ai.BudgetedChatModelTest \
                  --tests cn.jia.chat.api.ChatControllerTest \
                  --tests cn.jia.chat.service.ChatConversationEventBrokerTest \
                  --tests cn.jia.chat.service.ChatStreamPolicyTest \
                  :common:jia-common-core:test \
                  --tests cn.jia.core.deadline.RequestDeadlineContextTest \
                  --tests cn.jia.core.deadline.RequestDeadlinePropagationTest \
                  --tests cn.jia.core.deadline.RequestDeadlineTest \
                  --tests cn.jia.core.entity.JsonRequestPageTest \
                  :common:jia-common-service:test \
                  --tests cn.jia.core.audit.BoundedAuditDispatcherTest \
                  --tests cn.jia.core.config.CorsConfigTest --tests cn.jia.core.config.RestTemplateConfigTest \
                  --tests cn.jia.core.deadline.RequestDeadlineClientHttpRequestInterceptorTest \
                  --tests cn.jia.core.config.RedisCacheConfigTest \
                  --tests cn.jia.core.elasticsearch.ElasticsearchServiceTest \
                  --tests cn.jia.core.elasticsearch.ElasticsearchTimeoutsTest \
                  --tests cn.jia.core.filter.RequestIdFilterTest \
                  --tests cn.jia.core.interceptor.HttpRequestLogInterceptorTest \
                  --tests cn.jia.core.ldap.LdapFailureClassifierTest \
                  --tests cn.jia.core.ldap.LdapRequestBudgetContextTest \
                  --tests cn.jia.core.redis.RedisRequestBudgetTest \
                  --tests cn.jia.core.redis.RedisServiceDeadlineTest \
                  :common:jia-common-starter:test \
                  --tests cn.jia.core.amqp.RabbitMqBudgetConfigurationTest \
                  --tests cn.jia.core.deadline.RequestDeadlineConfigTest \
                  --tests cn.jia.core.deadline.RequestDeadlineExceptionHandlerTest \
                  --tests cn.jia.core.deadline.RequestDeadlineFilterTest \
                  :isp:jia-isp-service:test \
                  --tests cn.jia.isp.ldap.LdapTimeoutsTest \
                  :oauth:jia-oauth-service:test \
                  --tests cn.jia.oauth.config.OauthConsentTemplateTest \
                  --tests cn.jia.oauth.config.OauthControllerAccountSecurityTest \
                  --tests cn.jia.oauth.config.OauthExternalHttpTimeoutsTest \
                  :sms:jia-sms-service:test \
                  --tests cn.jia.sms.api.SmsControllerReplyCallbackTest \
                  --tests cn.jia.sms.config.SmsExternalEmailClientTest \
                  --tests cn.jia.sms.config.SmsExternalHttpTimeoutsTest \
                  --tests cn.jia.sms.service.impl.SmsReplyCallbackServiceTest \
                  --tests cn.jia.sms.service.impl.SmsServiceImplTest \
                  :starter:test \
                  --tests cn.jia.starter.AuthFormTransportTest --tests cn.jia.config.ApiPerformanceMetricsConfigTest \
                  --tests cn.jia.config.SmsEmailRuntimeClasspathTest \
                  --tests cn.jia.config.ElasticsearchProductionHealthIsolationTest \
                  :user:jia-user-mapper:test \
                  --tests cn.jia.user.dao.impl.UserInfoDaoImplTest \
                  --tests cn.jia.user.dao.impl.UserRelationMapperContractTest \
                  :user:jia-user-service:test \
                  --tests cn.jia.user.api.UserControllerListTest \
                  --tests cn.jia.user.service.impl.UserServiceImplTest \
                  :wx:jia-wx-service:test \
                  --tests cn.jia.wx.config.WxExternalHttpClientBuilderTest \
                  --tests cn.jia.wx.service.impl.MpInfoServiceImplTest \
                  --tests cn.jia.wx.service.impl.PayInfoServiceImplTest \
                  validateLayering \
                  :starter:bootJar
                python3 - <<'PYARTIFACT'
                #!/usr/bin/env python3
                """Package this successful Flow build for the existing transactional installer.

                Call only after the required Gradle command exits zero. ticket_sha256 is a
                legacy wire-field for a deterministic run identity, not an approval ticket.
                """
                import hashlib
                import io
                import json
                import os
                from pathlib import Path
                import re
                import shutil
                import subprocess
                import zipfile


                def canonical(value):
                    return (json.dumps(value, sort_keys=True, separators=(',', ':')) + '\n').encode()


                def digest(path):
                    h = hashlib.sha256()
                    with path.open('rb') as handle:
                        for chunk in iter(lambda: handle.read(1024 * 1024), b''):
                            h.update(chunk)
                    return h.hexdigest()


                REQUIRED_MAIL_RUNTIME_CLASSES = frozenset((
                    'javax/mail/MessagingException.class',
                    'javax/mail/Session.class',
                    'javax/mail/Transport.class',
                    'com/sun/mail/smtp/SMTPTransport.class',
                    'javax/activation/DataHandler.class',
                ))


                def verify_mail_runtime(jar):
                    """Check the shipped bootJar, not the broader Gradle test classpath."""
                    missing = set(REQUIRED_MAIL_RUNTIME_CLASSES)
                    try:
                        with zipfile.ZipFile(str(jar)) as boot:
                            for member in boot.infolist():
                                if not member.filename.startswith('BOOT-INF/lib/') or not member.filename.endswith('.jar'):
                                    continue
                                with zipfile.ZipFile(io.BytesIO(boot.read(member))) as dependency:
                                    missing.difference_update(dependency.namelist())
                                if not missing:
                                    break
                    except (OSError, zipfile.BadZipFile, RuntimeError) as exc:
                        raise SystemExit('invalid bootJar runtime archive: ' + type(exc).__name__)
                    if missing:
                        raise SystemExit('bootJar missing required mail runtime classes: ' + ', '.join(sorted(missing)))
                    return sorted(REQUIRED_MAIL_RUNTIME_CLASSES)


                def main():
                    root = Path.cwd()
                    env = os.environ
                    commit = subprocess.check_output(['git', 'rev-parse', 'HEAD']).decode().strip()
                    tree = subprocess.check_output(['git', 'rev-parse', 'HEAD^{tree}']).decode().strip()
                    if (env.get('PIPELINE_ID') != '5260799' or env.get('CI_COMMIT_REF_NAME') != 'develop'
                            or commit != env.get('CI_COMMIT_SHA') or not re.fullmatch('[0-9a-f]{40,64}', commit)
                            or not re.fullmatch('[1-9][0-9]*', env.get('BUILD_NUMBER', ''))):
                        raise SystemExit('unexpected Flow source/run identity')
                    # Both checks are enforced in the shell, before and after Gradle; repeat the
                    # latter here. Untracked build outputs are not part of the tracked source.
                    if env.get('CYF_SOURCE_CLEAN_BEFORE') != '1':
                        raise SystemExit('missing successful pre-build source check')
                    subprocess.check_call(['git', 'diff', '--exit-code', 'HEAD', '--'])
                    build = root / '_cyf_flow_build'
                    jars = [p for p in (build / 'jia/starter/libs').glob('*.jar') if not p.name.endswith('-plain.jar')]
                    provenance = build / 'opencv-resolved.json'
                    if len(jars) != 1 or any(p.is_symlink() or not p.is_file() for p in jars + [provenance]):
                        raise SystemExit('expected one bootJar and OpenCV provenance')
                    prov = json.loads(provenance.read_text())
                    if (prov.get('jar_sha256') != '323d40119548134b0966d3735e97a78d1edbc79bd91e7fb1074e5152392095f4'
                            or prov.get('jar_size') != 722802):
                        raise SystemExit('unexpected private OpenCV artifact')
                    mail_runtime = verify_mail_runtime(jars[0])
                    print('CYF_BOOTJAR_MAIL_RUNTIME ' + json.dumps(mail_runtime))
                    out = root / '_cyf_api_flow_export'
                    out.mkdir(mode=0o700)  # fail rather than reuse stale output
                    flow = dict(organization_id='5fb7d76ee6f9d07f148529c7', pipeline_id='5260799',
                                job_id='cloud_ci.api_ci', run_id=env['BUILD_NUMBER'], source_tip_sha=commit)
                    source = dict(branch='develop', commit_sha=commit, tree_sha=tree, clean_before=True, clean_after=True)
                    records = []
                    for kind, src, name in [('application_jar', jars[0], 'application.jar'),
                                             ('dependency_provenance', provenance, 'dependency.provenance.json')]:
                        target = out / name
                        shutil.copyfile(str(src), str(target))
                        records.append(dict(kind=kind, path=name, sha256=digest(target), size=target.stat().st_size))
                    identity = hashlib.sha256(canonical(dict(flow=flow, source=source))).hexdigest()
                    receipt = dict(schema_version=1, status='success', gradle_exit_code=0, bridge_exit_code=0,
                                   ticket_sha256=identity, flow=flow, source=source, files=records)
                    data = canonical(receipt)
                    (out / 'receipt.json').write_bytes(data)
                    receipt_sha = hashlib.sha256(data).hexdigest()
                    (out / 'application.metadata.json').write_bytes(canonical(dict(
                        schema_version=1, receipt_sha256=receipt_sha, flow=flow, source=source,
                        application_jar=records[0], dependency_provenance=records[1])))
                    (out / 'application.sidecar.json').write_bytes(canonical(dict(
                        schema_version=1, receipt_sha256=receipt_sha, flow=flow,
                        sha256=records[0]['sha256'], size=records[0]['size'])))
                    print('CYF_API_ARTIFACT ' + json.dumps(dict(flow=flow, source=source,
                          jar_sha256=records[0]['sha256'], receipt_sha256=receipt_sha), sort_keys=True))


                if __name__ == '__main__':
                    main()
                PYARTIFACT
          upload_artifact:
            name: Upload same-run release artifact
            step: ArtifactUpload
            with:
              uploadType: flowPublic
              artifact: cyf_api_flow_5260799
              filePath:
                - _cyf_api_flow_export/
              includePathInArtifact: false
  deploy:
    name: Automatic backend deployment
    jobs:
      deploy_api:
        name: Install same-run artifact and check health
        component: VMDeploy
        with:
          artifact: $[stages.cloud_ci.api_ci.upload_artifact.artifacts.cyf_api_flow_5260799]
          downloadArtifact: true
          artifactDownloadPath: /var/lib/cyf-api-flow/downloads/${BUILD_NUMBER}/package.tgz
          executeUser: root
          run: |-
            set -eu
            /usr/local/sbin/cyf-api-flow-deploy "$PIPELINE_ID" "$BUILD_NUMBER" "$CI_COMMIT_SHA"
          useEncodeBase64: false
          machineGroup: yjctjhskhk1ti9t4
          batchNumber: 1
          batchWaitTime: 0
