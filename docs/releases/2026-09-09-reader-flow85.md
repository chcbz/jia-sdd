# Reader capsule overlap — released

- Released Web commit: `77666e8fc1b2060ff3db8374b5de4539a5e5e53d`, pushed to both `develop` and `master`.
- Original reader fix: `d82b152ff6df523e78a8270be1d1483813e1366f` (`web/src/components/juyiting/archive/ArchiveReader.vue`). Reserve capsule space on the right normally, and on the logical left for virtual 90-degree landscape; retain safe-area mapping and ordinary browser behavior.
- Alibaba Flow organization `5fb7d76ee6f9d07f148529c7`, frontend pipeline `4403172`, run **85: SUCCESS**. Code scan, full unit tests, production build and host deployment all succeeded. Deploy order `69254729`, group `28833`: one batch, one healthy successful host. Backend pipeline was not run.
- Full original Mocha suite: **2082 passing, 0 failing, 2 pending**; HTML/JSON reports uploaded. The two pending checks require the separate Mini Program project (`web/tests/public-entry-navigation.test.js`). No new test skips or relaxed thresholds were added.
- Fresh production E14 on actual x64 Chrome 133: 10 s warmup + 60 s sample, p95 ≈ **0.7 ms**, p99 ≈ **0.8 ms**; all five gates passed under the unchanged 2/4 ms limits.
- Online verification at **2026-09-09 00:26:55 Asia/Shanghai**: `https://kit.chaoyoufan.cn` index and all seven referenced entry/Juyi Hall JS/CSS assets matched the actual Flow artifact byte-for-byte. The artifact MD5 also matched the build log. Detailed SHA-256 evidence: [verification JSON](2026-09-09-reader-flow85.json).
- Reader CSS is in the lazy `JuyiHallEntry-DfJb07QC.css`, not the main index stylesheet. Verified capsule class, normal `padding-right:112px`, virtual-landscape `padding-left:112px`, and safe-area rules in the deployed bytes.
- Local reader geometry validation covered 60 Chromium/production-CSS cases. **No physical WeChat device test was performed.**

## Pipeline blockers resolved

CI now installs signed OS dependencies without weak dependencies, caches metadata, verifies pinned Node20/Chrome133/WebP1.2, forces a fresh E14 before the full original suite, and installs the real stable Chrome launcher path used in frozen atlas provenance. Test fixes restore current component contracts, wait for real UI readiness and clean browser mocks/fixture mutations even on failure. Independent read-only reviews approved the release candidates.

Runs 83 and 84 failed before deployment (OS installation timeout, then historical test-harness/provenance failures). Run 85 is the successful release evidence; neither earlier local checks nor historical benchmark reports substitute for it. Existing onboarding and Mini Program public-entry changes remain ancestors of the released Web commit.
