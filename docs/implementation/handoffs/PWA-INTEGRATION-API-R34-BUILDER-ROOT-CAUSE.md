> Historical stop record: this matrix was authoritative until the user explicitly reauthorized bounded R34 continuation on September 1, 2026 after an external cleanup. The final result is recorded in `PWA-INTEGRATION-API-R34-STATIC-PACKAGE.md`.

# PWA API R34 builder root-cause matrix — 2026-09-01

Task: `PWA-INTEGRATION-API-RC`  
Writer: `sol-pwa-integration-api-r34-20260901` / `critical_worker` / `writer`  
Terminal gate: **`blocked_root_cause`; owner released; no R34 final package, unit, activation, runtime evidence root, or Review candidate**

## Authority and immutable product

- R34 was explicitly authorized from R33 attribution `/var/tmp/cyf-pwa-api-r19-static-verifier-r33-builder-0kRJT8pE/control/second-failure-attribution.txt`, SHA-256 `85d00236b563b5f597459daf0ca48ce5e088ede47f027d02e80210144b31d56d`.
- Product worktree `/home/isp/wsps/cyf/.worktrees/pwa-integration-api-rc-r19` remained clean and exact at commit/tree `2599de3c2bdf5f1c3cf6e4faa8dcbb4048dffa4a` / `bad28da5c05d6448d805f88c4e88457f0f657a5e`.
- R33 and every predecessor remained read-only. R21 was not rerun.
- No runtime/unit start, stop, or restart; Gradle; daemon reload; network hydration; DB; RabbitMQ; Chromium; production access; deployment; or final package publication occurred.

## Frozen R34 contracts

Before generator editing, the fresh retry builder froze:

1. unchanged product API/event and identity/ACL/transaction/migration semantics;
2. lock order and fail-closed publication/acceptance transaction boundaries;
3. sensitive-payload allowlist and denial list;
4. acceptance-to-test coverage for all R30 Review `4 P1 / 1 P2` findings;
5. exact `61019:61019`, mode `0700`, empty mutable runtime children;
6. provisional evidence → accepted receipts/candidate → `ExecStopPost` final canonical publication;
7. package exact-orchestrator canonical NUL-separated keys, nine ordered online keys and 17 total keys;
8. real Gradle/Maven tree-manifest regeneration from actual R34 copies;
9. real forbidden-operation observations rather than hardcoded Boolean claims;
10. explicit `external_xml_dir is None` / `isinstance(str)` branching with other types rejected; and
11. Python 3.6 parsing/compilation with `subprocess.run(text=...)` and `capture_output=...` forbidden.

Freeze SHA-256: `05904bc26649804045d4d2147b5558d55673bb30c31af1a3adcbe17bfa31b845`.  
Acceptance matrix SHA-256: `01ae17bebbc9291d0275dc500d344ccd10659866f2df7d9762f6b89f50e6bee1`.

## Successful fresh retry preparation

Fresh retained builder:

`/var/tmp/cyf-pwa-api-r19-static-verifier-r34-builder-oq1inx6b`

- Exact metadata/content projection: `410` rows, SHA-256 `f52d5af7de44c53519dbf6e42634adb868dfa716af554c689262bb3f5856d4a4`.
- Setup terminal SHA-256: `3680d3dccd4d98393f49b524417cbd33895428e59c3da21885fa5e10f3c87912`.
- Python-3.6-compiled generator source SHA-256: `702d53e0926e0c8c01f02cc66cc18167966b870ed909e5a277af6e921e093e2e`.
- External generator cfile SHA-256: `94d317c1b65f2ae7b875eda54094162748a935801c6457dc84fc7388bf68ac1c`.
- Exact packaged orchestrator donor SHA-256: `5d8bc25c3530fec84d6a8ebdc89dfedfccfef4cf6b1cff52c3d1c8c6d1d25cd2`.
- The generator contains the explicit nullable coverage branch, Python 3.6 AST guard, actual Gradle/Maven manifest regeneration, five-child adversarial test fixture, provisional/accepted transaction fixture, canonical 9/17-key checks, and final `5368709120`-byte prepublication gate.
- The generator was **not executed**, because final publication was not permitted while the disk gate remained below threshold.

## R34 failure matrix

| Attempt | Result | Root cause | Safety result |
| --- | --- | --- | --- |
| Fresh R34 setup | Failed before process execution | A nested triple-single-quoted static-test body terminated the outer setup literal, producing a Python parse error. | Attributed before retry. The R34 pointer, builder, and all eight final paths remained absent. |
| Sole R34 retry | Builder and generator preparation succeeded; then stopped on capability probe | A non-required `cp --reflink=always` probe was attempted to explore lowering disk usage. The ext filesystem returned `Operation not supported`. | This was the second consecutive R34 failure. The only probe by-product is the preserved zero-length builder-owned `control/reflink-capability-probe.copy`; stderr SHA-256 is `ce5e7718537edcc4ab22d987ea799352c66da11bfe595b7914d8d0104a073e20`. No final path was created. |

The disk observation after terminal preservation was `4806258688` free bytes, below the frozen `5368709120` minimum. No unknown or predecessor path was cleaned to change that result.

## Exact final-path absence

All R34 final paths remain absent:

1. `/var/tmp/cyf-pwa-api-r19-static-verifier-r34-preparation`
2. `/usr/local/libexec/cyf-pwa-api-r19-static-verifier-r34`
3. `/usr/local/libexec/cyf-pwa-api-r19-static-verifier-r34-seal`
4. `/etc/systemd/system/cyf-pwa-api-r19-static-verifier-r34.service`
5. `/var/tmp/cyf-pwa-api-r19-static-verifier-r34-evidence`
6. `/var/tmp/cyf-pwa-api-r19-static-verifier-r34-workspace`
7. `/var/tmp/cyf-pwa-api-r19-static-verifier-r34-incidents`
8. `/var/tmp/cyf-pwa-api-r19-static-verifier-r34.activation.json`

Therefore R34 has no package-root SHA, activation SHA, unit SHA, runtime/static evidence key, or reviewable manifest. It must not enter Review.

## Successor boundary

R34 must not be retried or overwritten. A separately authorized fresh non-overwriting R35 may use the R34 builder only as read-only design evidence. It must:

1. preserve R33, R34, and all predecessors exactly;
2. use ordinary independent copies only—no reflink and no hardlink probe;
3. wait for an externally provided fresh free-byte observation `>= 5368709120` before constructing or publishing any final package;
4. retain the explicit nullable coverage branch, Python 3.6 contract, R30 Review closure design, actual Gradle/Maven manifest regeneration, real forbidden-operation observations, and transactional evidence design; and
5. perform no runtime, Gradle, daemon reload, network, DB, RabbitMQ, Chromium, production, deployment, R21, unknown cleanup, or predecessor mutation before a fresh static Review candidate is sealed.
