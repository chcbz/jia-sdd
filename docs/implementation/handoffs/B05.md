
## 首轮独立 Review（2026-07-23）

- Reviewer：Erdos。
- 结论：**REJECT**（P1 两项、P2 一项）。
- P1：聚合只锁 task 并读取 child snapshot，不能阻止等待父行锁的 required child 在 task 写成 completed 后成功插入；MySQL 8.0.21 已复现最终 completed task 仍有 active required item。
- P1：snapshot 未完整校验 B04 lease 状态组合，claimed/running 缺 lease 或 terminal 残留 lease 可被接受。
- P2：required 为空、optional running 时 task 未提升到 running。
- 已退回 Owner：child 创建必须与 parent 非终态检查原子序列化；lease 不变量与 B04 对齐；修复 optional-only active 聚合并补真实并发测试。

## 最终独立 Review 与集成（2026-07-23）

- 修复 commits：`4f539e0`、`f581fb7`、`872b20f`、`f06ed0e`。
- Reviewer：Erdos，第五轮结论 **ACCEPT**。
- MySQL 8.0.21 验证：parent gate 使用 `uk_task_scope`（`type=const`），byte-exact scope/status 拒绝大小写、截断和 NUL padding 碰撞。
- 并发验证：terminal 聚合与迟到 required child 串行化；锁住 task-1 时 task-2 `FOR UPDATE` 约 1ms，不扩大锁范围。
- H2/Mapper：聚合真实库 17 项、parent gate DAO 2 项均通过。
- 已按 commits 顺序集成到 `api/develop`：`d230e00..3d9b7e4`，并随 HEAD `9a160ca` 推送。
