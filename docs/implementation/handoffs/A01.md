# Task Handoff: A01 定义稳定 Agent ID 规则和历史映射

## 1. 结论

- 状态：accepted
- Owner：`main-gpt-5.6-sol`
- Reviewer：`Lagrange (019f8a43-2c6b-7ee3-8e49-838e46fd56aa)`
- Branch：`docs/a01-stable-agent-id`
- Worktree：`/home/isp/wsps/cyf`
- Commit：当前 workspace 根目录不是 Git 仓库；本任务为根目录文档交付

## 2. 实现内容

- 封闭定义 canonical ID 合法集合：新 opaque ID、registry 显式 legacy-canonical、`builtin-songjiang`。
- 冻结 immutable owner scope `(client_id, owner_jiacn)` 及 `tenant_id = owner_jiacn` 映射。
- 将 active persona 唯一范围定义为 `(client_id, owner_jiacn, persona_code)`。
- 指定 binding/identity registry 为持久身份事实源，runtime 为可重建 projection。
- 区分 stable agentId、runtimeInstanceId 和 webSocketSessionId 生命周期。
- 定义 `PROVISIONED / ACTIVE / SUSPENDED / RETIRED` 状态机及 restore/retire 边界。
- 限定在线 alias 只能使用 `legacy_agent_id`；persona/profile/displayName 仅作迁移证据。
- 明确 A02 负责 identity registry/alias Schema、Initializer、测试、dry-run 和部署说明。

## 3. 修改文件

- `/home/isp/wsps/cyf/docs/implementation/agent-identity-v1.md`
- `/home/isp/wsps/cyf/docs/implementation/DECISIONS.md`
- `/home/isp/wsps/cyf/docs/implementation/TASKS.yaml`
- `/home/isp/wsps/cyf/docs/implementation/handoffs/A01.md`

## 4. 首轮 Review 处理结果

| 必改项 | 修订结果 |
| --- | --- |
| Canonical ID 合法集合 | 已封闭定义，不靠前缀推断 |
| Owner scope | 已冻结 `(client_id, owner_jiacn)` 和 tenant 映射，禁止 owner 转移 |
| Durable identity source | 已指定 binding/registry，禁止物理删除历史，runtime 仅为 projection |
| runtimeInstanceId | 已明确进程级；WS session 为连接级 |
| 生命周期状态机 | 已拆分四态，unbind/restore/retire 语义明确 |
| Legacy/alias | 在线只解析 legacy_agent_id；冲突与 scope 缺失一律阻断 |
| Alias Schema 归属 | 已扩大 A02，B01 不再临时决定 |
| active 唯一约束 | 明确使用 generated active key 或语义等价方案，不依赖 nullable valid_to |

## 5. 验证证据

```text
只读代码检查：AgentServiceImpl.register/bindPersona/generateAgentId/requireApiKeyOwnedAgent
只读协议检查：ApiKeyHandshakeInterceptor、AgentWebSocketHandler
只读配置检查：codex-profiles（敏感字段未输出）
只读数据库检查：agent_runtime、agent_persona_binding、agent_task_meta
结果：确认存在短 ID、拼接 ID、scope 缺失、错误前缀、跨 scope 历史和任务短 alias。
```

## 6. 验收条件逐项核对

- [x] canonical 合法集合和禁止推权规则明确。
- [x] owner scope、tenant 映射和 persona 唯一范围明确。
- [x] 持久身份事实源与 runtime projection 边界明确。
- [x] runtimeInstance/session 生命周期明确。
- [x] 生命周期和恢复规则明确。
- [x] legacy alias 在线解析范围和迁移阻断规则明确。
- [x] A02/B01 Schema 职责无重叠。
- [x] 未直接修改生产数据。

## 7. 剩余风险

- 现有 active binding 唯一索引比新规则更窄，A02 迁移前需检查历史冲突并采用可重跑 DDL。
- scope 缺失、跨 owner 冲突和多候选记录必须人工确认，不能用 personaCode 自动修复。
- 新生命周期状态落库前，旧 inactive/删除语义需要 A02 输出映射规则。

## 8. 对后续任务的接口

- A02：实现 identity registry/alias Schema、Initializer、测试和 dry-run。
- A03：协议携带 canonical agentId 与 runtimeInstanceId，不能混用 WS session ID。
- B01：协作表使用 canonical agentId 和显式 scope，不创建身份 alias 表。
- B09：只消费 A02 已确认映射。

## 9. Reviewer 结论

- 第一轮：REJECT。
- 第二轮：ACCEPT。首轮 8 项必改问题全部关闭。
