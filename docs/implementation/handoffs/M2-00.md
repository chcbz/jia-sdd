# Task Handoff: M2-00 M2 基线收敛与安全启动门禁

## 1. 结论

- 状态：accepted
- Owner：`main-gpt-5.6-sol / main_orchestrator`
- Disk worker：Beauvoir / `deepseek-v4-pro high`
- Branch：控制面无根仓库分支；累计 base 分支见下文
- Worktree：`/home/isp/wsps/cyf`
- Commit：N/A（仓库根目录不是 Git repo）
- 生产变更：无；未部署、未重启、未连接生产 MySQL、未执行 B09/C01H DML

## 2. 实现内容

1. 锁定 API/Web `develop` 与远端同步的精确 SHA。
2. 冻结 API 主工作树 44 项 dirty 的路径、字节和 SHA-256；复核全部内容未变化。
3. 按对象 SHA 冻结两个 stash，未 apply/pop/drop。
4. 确认 B07/B08/B09 源码已在 API 基线，B09 生产回填仍未执行。
5. 扫描 live Agent 日志，未发现 `MESSAGE_TYPE_REQUIRED`。
6. 由 DeepSeek V4 Pro 三轮有界安全清理磁盘，在创建 worktree/构建前达到 5 GiB 门禁，并在验证后恢复退出门禁。
7. 从锁定 commit 创建 API/Web 累计 clean base，不接触脏 `develop` 文件系统。
8. 完成 API 399 tests、Web 497 tests 和 Web production build。
9. 恢复被全局 Gradle 缓存清理一并移除的私有仓库 init 配置；`/root/.gradle/init.gradle` 最终为 mode `0600`，秘密内容未写入项目文档。

## 3. 锁定基线与累计 clean base

| Repo | Locked develop/origin SHA | Cumulative branch | Worktree | Final HEAD | Clean |
| --- | --- | --- | --- | --- | --- |
| api | `c49d148da40bc1f2998954cef27feee7c6b77b57` | `feat/m2-api-base-20260803` | `/home/isp/wsps/cyf/.worktrees/m2-api-base` | `c49d148da40bc1f2998954cef27feee7c6b77b57` | Yes |
| web | `2424f51f375814f403ca70a9a6e9948728e595b1` | `feat/m2-web-base-20260803` | `/home/isp/wsps/cyf/.worktrees/m2-web-base` | `2424f51f375814f403ca70a9a6e9948728e595b1` | Yes |

创建后和最终清理后均断言 HEAD 精确相等、工作树 clean、branch/worktree 唯一。

## 4. 用户工作与 stash 保全证据

- API dirty 数量：`44`。
- 44 项逐文件 size/SHA-256 复核：`manifest_failures=0`。
- 最终 status manifest SHA-256：`e629f39533ec835b9e6ed5711737af735857bf28273be6fab5ad28c3858b1c6e`。
- 最终 dirty content manifest SHA-256：`a6d3df3cc3c3f079187c2ac78a5d994df157e2b87ac657e9cca4af4c4f434db0`。
- `stash@{0}` object：`75a3458d8a25faa9d70deb01c53ae185d7d9f7cc`。
- `stash@{1}` object：`12f0b4e6149e904e92082bb8642d3086cad033ec`。
- 两个对象类型均为 `commit`；未执行 apply/pop/drop。

完整 44 项清单见 `docs/implementation/preflight/M2-00.md`。

## 5. 资源与清理证据

### 5.1 初始门禁

- 初始可用：`3,138,584,576` bytes（约 2.92 GiB），不满足门禁。
- DeepSeek V4 Pro 第一轮清理后：`5,615,607,808` bytes（约 5.23 GiB）。
- 第一轮主要删除：DNF/Gradle/npm/Codex 临时缓存、API/Web 生成物、过期 `/tmp` 探针、已轮转 journal、最旧 archived session；未触碰源码、worktree、数据库、部署、备份、active sessions 或 memories。

### 5.2 验证后收尾

测试和构建重新生成依赖与输出后，可用空间降至：

```text
3,765,633,024 bytes (3.507 GiB)
```

第二轮清理删除前占用归因合计 `1,616,306,176` bytes：

| 类别 | 删除内容 | 删除前占用 |
| --- | --- | ---: |
| API base | `.gradle` + 15 个 `build/` | 11,653,120 bytes |
| Web base | `node_modules`、`dist`、`mochawesome-report` | 359,301,120 bytes |
| Gradle | caches/wrapper/daemon/native/tmp，保留 `init.gradle` | 752,058,368 bytes |
| npm | `_cacache`、`_logs` | 58,531,840 bytes |
| Codex archive | 最旧 66 个非活跃历史归档（2026-05-31～2026-06-30） | 434,761,728 bytes |

Codex archive 第二轮分批删除清单 SHA-256：
`6537ae8d7d3e21f314d2886e751edeb93c05d776082beb288209e5f392bf10da`。该值属于执行过程证据；三轮删除的最终可复现合并清单见 5.3。

第二轮清理后一度仅高于 5 GiB 约 10 MiB。为避免 handoff 写入和系统日志使门禁再次跌破，DeepSeek worker 又执行了 72.65 MiB 的窄范围余量清理：

- 已轮转/压缩 `/var/log`：27,209,728 bytes；清单 SHA-256 `b32b5f148ae3a6ad263e66fcc61bebd3c90854ece2a79a151f3c5a3910897f9e`。
- 剩余最旧 archived sessions 45 个：41,095,168 bytes；清单 SHA-256 `1900b89b757f78d00c3d98fe264fcdfb9c39ef870bef605568a99497a89df529`。
- DNF 可再生 metadata：6,688,768 bytes。
- 超过 7 天的明确 `/tmp` 探针/编译临时文件：1,187,840 bytes；清单 SHA-256 `82532d086b331d0c98f2978149d27577cefe6cfe130894897b636c55b4a72057`。

最终 review 前由主控在 2026-08-03T22:20:58+08:00 复核：

```text
root available:   5,433,647,104 bytes (about 5.060 GiB)
inodes free:      1,970,259
memory available: 2,060,005,376 bytes
swap free:        1,491,300,352 bytes
```

`/root/.gradle/init.gradle`：mode `0600`，819 bytes，SHA-256
`632c00a4acd92511a26cb9dd57861c6af56ff255c76635519eff9ec3245e088b`。


### 5.3 Codex archived sessions 最终合并清单

- 路径：`docs/implementation/preflight/M2-00-archived-sessions-deleted.txt`
- 条目：112 行
- 文件大小：7,952 bytes
- SHA-256：`c77a301af74666de6950ddc1047b383cfdebe5b0c3634321f383403c467028be`
- 语义：这是三轮删除的最终可复现合并清单；5.2 中各分轮 SHA 继续保留为过程证据。
- 边界：仅记录已删除的 `/root/.codex/archived_sessions` 非活跃归档；`/root/.codex/sessions` 与 `memories` 未触碰。删除不可恢复。

## 6. 测试证据

### API

所有 Gradle 命令持有 `/tmp/cyf-gradle.lock`，使用 `--no-daemon --max-workers=1` 和低内存 JVM。

```text
:agent:jia-agent-mapper:test
66 tests, 0 failures, 0 errors, 0 skipped

:agent:jia-agent-service:test
333 tests, 0 failures, 0 errors, 15 skipped

combined
399 tests, 0 failures, 0 errors, 15 skipped
BUILD SUCCESSFUL
```

首次 clean-host 验证暴露环境依赖：全局 Gradle init 配置被缓存清理移除，导致 plugin/private artifact 无法解析。主控从归档运行证据恢复 init 配置至临时文件，验证 private repository 可解析 `org.opencv:opencv:4.5.5` 后完成全部测试，并最终恢复 `/root/.gradle/init.gradle` 为 `0600`。秘密值未写入仓库。

### Web

```text
npm ci
677 packages installed
npm audit: 3 vulnerabilities (1 low, 2 high) — baseline dependency debt, not introduced by M2-00

npm run test
497 passing

npm run build
1104 modules transformed
PASS
```

Build 仅报告既有的 `src/stores/api.js` dynamic/static import chunk warning。

## 7. 验收条件逐项核对

- [x] API/Web 基线 SHA 与远端同步状态锁定。
- [x] API 44 项 dirty 路径/字节/SHA-256 冻结并复核不变。
- [x] 两个 stash object SHA 冻结且未操作。
- [x] Agent 日志无 `MESSAGE_TYPE_REQUIRED`；无需新增兼容任务。
- [x] B07/B08/B09 源码/生产双状态明确；未执行生产迁移。
- [x] 两个累计 clean base 从精确 SHA 创建，HEAD clean。
- [x] API/Web 基线测试和 Web build 通过。
- [x] 用户未提交修改、stash、数据库、部署和备份未触碰。
- [x] 退出前根分区达到至少 5 GiB。
- [x] 独立 DeepSeek V4 Pro Reviewer ACCEPT。
- [x] 冻结 reviewer-accepted 最终控制面文件集合；detached SHA-256 manifest 按本 handoff 冻结状态生成，供 C01 首个 commit trailer 引用。

## 8. 未解决问题与风险

1. **P1 磁盘余量薄**：当前仅略高于 5 GiB；下一次 Gradle/npm 首次构建会重新下载缓存。C01 claim 前和每次构建前必须重新检查门禁，必要时建立独立资源任务，禁止借机删除源码/worktree/stash。
2. **P2 clean-host reproducibility**：Gradle 私有仓库依赖依赖 host-level `init.gradle`。本次已恢复并验证，但该配置不是 Git 追踪产物；后续应单独设计秘密管理和可重复 bootstrap，不夹带进 C01。
3. **P2 baseline dependency debt**：`npm ci` 报告 3 个已存在漏洞（1 low、2 high）；M2-00 未执行无边界 `npm audit fix`。应另建依赖升级任务。
4. **P2 archived history loss**：为满足用户指定由 DeepSeek 处理的分区门禁，三轮共删除 112 个非活跃 Codex archived sessions（最旧单个大归档 1 个、第二轮 66 个、余量轮 45 个），`/root/.codex/archived_sessions` 目录中的归档已清空；`/root/.codex/sessions`（包括 Reviewer 观察到的 2026-06-23 两个小文件）与 `memories` 属于不同的 active-session namespace，均未触碰。已删除归档历史不可恢复。
5. 下一次 Gradle 首次运行会重新下载 wrapper/dependencies，耗时和网络用量增加。

## 9. 对 C01 的接口与强约束

- C01 只从 `feat/m2-api-base-20260803` / `c49d148...` 创建任务 worktree。
- C01 不得从脏 `api/develop` 文件系统复制任何文件。
- C01 首个 commit trailer 必须记录最终控制面 manifest SHA-256。
- 所有 Gradle 命令继续持有 `/tmp/cyf-gradle.lock`，单 worker、低内存。
- `task_version` 与 `event_version` 必须遵循 ADR-006；不得在 C01 重解释。
- 不执行 B09/C01H 生产 DML，不部署、不重启服务。

## 10. Reviewer 结论

- 状态：ACCEPT；两项 P1 证据修订均已闭环，M2-00 可标记 `accepted`。
- Reviewer：Kant / `adversarial_reviewer / deepseek-v4-pro high`
- Agent ID：`019fc7ff-66f9-7182-a839-4138526e59cc`
- 审查时间：`2026-08-03T22:30+08:00`
- 审查方式：独立只读；未修改文件、未清理磁盘、未运行 Gradle/npm。
- 独立核验：基线 HEAD、44 dirty 数量及抽样 checksum、stash object/type/reflog、两个累计 base HEAD/clean、资源门禁、Gradle init mode/hash、M1 源码状态、协议日志、控制面关键 checksum 均通过。
- P1-1：claim-time `TASKS.yaml` hash 已明确标记为历史快照；最终 detached manifest 在 reviewer closure 后生成。
- P1-2：archive 删除清单已由原 DeepSeek disk worker 从执行记录只读恢复，并固化为 112 行最终合并清单；路径、大小和 SHA-256 见 5.3。
- 非阻塞风险：磁盘余量薄、首次构建需重下缓存、测试未由 Reviewer 重跑、host-level Gradle secret bootstrap 未进入 Git。


## 11. Reviewer closure

- `P1-1` 已关闭：claim-time checksum 明确为历史快照；最终权威清单固定为 `docs/implementation/preflight/M2-00-control-plane.sha256`。
- `P1-2` 已关闭：112 个 archived session 的最终合并删除清单已落盘并校验 SHA-256。
- 接受状态写入后，不再修改 manifest 覆盖的控制面文件；C01 必须引用 detached manifest 文件自身的 SHA-256。
