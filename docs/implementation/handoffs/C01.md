# Task Handoff: C01 task event schema and version allocation

## 1. Conclusion

- Status: `integrated`
- Writer: Cicero / `deepseek-v4-pro high` (`019fe2ae-883a-7203-abdb-ba71c2008f4a`)
- Independent Test Runner: Tesla / `deepseek-v4-flash medium` (`019fe2f6-f1c6-7563-9c63-05cadc928bf4`)
- Reviewer: Socrates / `release_guard / gpt-5.6-sol high` (`019fe302-4790-7b01-9d44-1a2ce28fc476`) — `REJECT`
- Branch: `feat/c01-task-event-schema`
- Worktree: `/home/isp/wsps/cyf/.worktrees/m2-c01-api`
- Base: `c49d148da40bc1f2998954cef27feee7c6b77b57`
- Final HEAD: `201a79391c0d70a194fc2aad063a25aab6fda20c`
- Production changes: none; no deployment, service restart, production MySQL connection or migration

## 2. Commits

1. `87b25df958f44aaa916ca86cb32407f652904853` — initial C01 implementation
2. `1e9bd9dfa8fce7229b128708c9b006f95360efb1` — transaction/schema/version/design hardening
3. `da9e6c5500b37cb33078e7d41356ddfd1dc7b638` — auto-increment validation, startup TOCTOU closure, actor whitelist and conditional MySQL drift tests
4. `2986b8b6bf2f33eb3ec6e7d0560fbabbfd7a7cff` — close engine/index/TOCTOU/REQUIRED/replay review blockers
5. `201a79391c0d70a194fc2aad063a25aab6fda20c` — verify composite MySQL index semantics correctly

The first commit contains the required trailer:

`M2-Control-Manifest-SHA256: f272dbb8b7e04ee8b2bd245f6544c6e89d8e743c135b811209d145a80df45a1a`

## 3. Delivered behavior

- Adds the binary-collated `agent_task_event` table aligned with collaboration design section 7.5.
- Keeps `task_version` exclusively for aggregate CAS and uses `event_version` / `current_event_version` for the persistent event sequence.
- Adds a production `AgentTaskEventWriter.append()` transaction boundary using Spring `TransactionTemplate` with REQUIRED semantics.
- Appends by scoped row lock, exact next-version allocation, event insertion and byte-exact CAS update in one transaction.
- Fails closed for missing/wrong scope, unknown event/actor/aggregate type, invalid field lengths, version gaps, CAS miss and `Long.MAX_VALUE` overflow.
- Provides Entity, write command/result DTOs, DAO, Mapper and replay-oriented scoped reads.
- Adds full initializer drift checks for table collation, all columns, primary key, auto-increment, two unique constraints and three secondary indexes.
- Adds the frozen section 7.5 event catalog plus `HISTORICAL_BASELINE_IMPORTED` for C01H.
- Does not wire B03-B08 business mutations; that remains C01B. It does not implement historical baseline, Broker, replay bridge or SSE.

## 4. Changed files

- `agent/jia-agent-api/src/main/java/cn/jia/agent/service/AgentTaskEventWriter.java`
- `agent/jia-agent-core/src/main/java/cn/jia/agent/common/TaskEventType.java`
- `agent/jia-agent-core/src/main/java/cn/jia/agent/entity/AgentTaskEventEntity.java`
- `agent/jia-agent-core/src/main/java/cn/jia/agent/entity/AgentTaskEventWriteCommand.java`
- `agent/jia-agent-core/src/main/java/cn/jia/agent/entity/AgentTaskEventWriteResult.java`
- `agent/jia-agent-mapper/src/main/java/cn/jia/agent/dao/AgentTaskEventDao.java`
- `agent/jia-agent-mapper/src/main/java/cn/jia/agent/dao/impl/AgentTaskEventDaoImpl.java`
- `agent/jia-agent-mapper/src/main/java/cn/jia/agent/mapper/AgentTaskEventMapper.java`
- `agent/jia-agent-mapper/src/main/resources/db/schema.sql`
- `agent/jia-agent-mapper/src/main/resources/db/task-event-schema.sql`
- `agent/jia-agent-mapper/src/test/java/cn/jia/agent/schema/AgentTaskEventSchemaTest.java`
- `agent/jia-agent-service/src/main/java/cn/jia/agent/config/AgentSchemaInitializer.java`
- `agent/jia-agent-service/src/main/java/cn/jia/agent/service/impl/AgentTaskEventWriterImpl.java`
- `agent/jia-agent-service/src/test/java/cn/jia/agent/config/AgentSchemaInitializerMySqlTest.java`
- `agent/jia-agent-service/src/test/java/cn/jia/agent/config/AgentSchemaInitializerTest.java`
- `agent/jia-agent-service/src/test/java/cn/jia/agent/service/impl/AgentTaskEventRealTransactionTest.java`

No changed path is outside `agent/**`; the worktree is clean and `git diff --check` passes.

## 5. Independent verification

Tesla reran all mapper/service tests from final HEAD with `/tmp/cyf-gradle.lock`, one worker, low memory and `--rerun-tasks`:

```text
jia-agent-mapper: 77 tests, 0 failures, 0 errors, 0 skipped
jia-agent-service: 370 tests, 0 failures, 0 errors, 22 skipped
combined: 447 tests, 0 failures, 0 errors, 22 skipped
BUILD SUCCESSFUL; 34 tasks executed
```

Key C01 suites:

- `AgentTaskEventSchemaTest`: 11/11 passed.
- `AgentTaskEventRealTransactionTest`: 30/30 passed.
- `AgentSchemaInitializerTest`: 33/33 passed.
- C01 MySQL drift tests: 7 conditional tests present but skipped because no approved `A08_MYSQL_URL` was supplied.

The Test Runner did not connect to a default/local/production MySQL instance and did not modify code.

## 6. Acceptance evidence

- Concurrent append: every attempt succeeds; versions are exactly `1..N`; DB event count and `current_event_version` equal `N`.
- Rollback: insert failure and explicit transaction rollback retain neither event nor version update.
- Version rules: only `expected + 1` is accepted; gap, stale CAS and `Long.MAX_VALUE` fail closed.
- Scope: lock, update and reads use byte-exact scope predicates; case/padding/control-character mismatches fail closed.
- ADR-006: event append does not change `task_version`.
- Schema: binary collation, exact field contract, BIGINT event version, stable unique constraints and indexes.
- Startup: repeated compatible initialization is stable; conditional MySQL tests cover column/collation/TOCTOU drift rejection.
- Contract: event type, actor type and aggregate type use explicit allowlists.

## 7. Resource work and irreversible effects

C01 claim and verification were twice blocked by the 5 GiB gate. The user-assigned DeepSeek V4 Pro resource Worker restored headroom without touching C01 source, dirty develop, stashes, active Codex state, databases or deployments.

This C01 cycle additionally removed only regenerable output from inactive A/B/M1 worktrees, expired `/tmp` build directories and an inactive frontend worktree's `node_modules`/`dist`, recovering about 1.79 GB. A tracked-file boundary was detected in that frontend worktree and immediately restored byte-exact from its HEAD; the worktree ended clean.

Earlier in the same C01 startup cycle, the resource Worker irreversibly removed an inactive StarAgent redo log and obsolete EL7 kernel/module files; the active kernel is AL8 and StarAgent was inactive. These effects are disclosed in `docs/implementation/preflight/C01.md`.

Final Test Runner resource snapshot: root available `6,342,078,464` bytes, above the 5 GiB gate.

## 8. Process deviations and environment debt

1. The initial Writer ran a small number of early Gradle commands without `/tmp/cyf-gradle.lock`. No other Gradle process was active. All final compilation and full test evidence was rerun under the lock; the violation remains disclosed.
2. The root Gradle build eagerly reads `repoUsername`/`repoPassword` during configuration. Test verification used non-secret dummy project properties; test tasks do not publish or connect to the publishing repository. This is baseline build configuration debt, not introduced by C01.
3. MySQL-specific C01 drift tests exist but were not executed because there was no approved isolated MySQL URL. They must run before integration/production release; production MySQL must not be used as a substitute.
4. Root disk headroom remains operationally constrained and must be checked before every later M2 build.

## 9. Reviewer checklist

- [ ] Independent GPT-5.6 Sol High reviewer confirms schema/design/ADR-006 alignment.
- [ ] Reviewer confirms transaction propagation participates in C01B outer transactions.
- [ ] Reviewer attacks lock/insert/CAS ordering, overflow, duplicate IDs, scope collisions and rollback.
- [ ] Reviewer checks initializer collation/auto-increment/index drift and startup TOCTOU behavior.
- [ ] Reviewer confirms C01 has not absorbed C01B/C01H/C02+ scope.
- [ ] Reviewer returns `ACCEPT` or `REJECT` without modifying the worktree.

## 10. Independent Reviewer rejection (2026-08-08 control-plane date)

Socrates, an independent read-only GPT-5.6 Sol High release guard, returned `REJECT` without modifying the worktree or running Gradle.

Required remediation:

1. Validate that both `agent_task_meta` and `agent_task_event` use InnoDB; existing non-transactional tables must fail startup.
2. Reject dangerous extra unique indexes/constraints that weaken scoped identity or create cross-scope denial of service.
3. Make post-create table absence fail closed; remove the startup TOCTOU fail-open path.
4. Correct the conditional MySQL tests so auto-increment, table-drop/TOCTOU and drift tests exercise the intended failure after setup; add real concurrent/outer-transaction coverage where feasible.
5. Lock and test REQUIRED participation in an outer business transaction, including outer rollback and inner failure behavior.
6. Remove the premature inclusive replay contract or freeze an explicit exclusive-after-version cursor before C03.

Non-blocking findings already confirmed: no C01B/C01H/C02 scope absorption, no `task_version` mutation, exact base/HEAD and clean worktree, and 447 existing tests remain green.

### Audit-time clarification

The execution host reports local time `2026-08-09` because its RTC is configured as local time in `Asia/Shanghai`; the control-plane authority for this run is `2026-08-08`. The host is synchronized but has a known RTC/local-time configuration warning. No system clock change is attempted inside C01 because changing host time can affect running services and audit logs.

### M2-00 manifest clarification

The detached M2-00 manifest remains immutable and its own SHA-256 is still the commit-trailer authority. `TASKS.yaml` is the live task ledger and necessarily changes under the standard claim/review/status protocol, so its entry is treated as a historical M2-00 snapshot. At this rejection point, all other manifest entries verify byte-exact; only `TASKS.yaml` differs intentionally. The old manifest is not regenerated or overwritten.

## 11. Remediation revision

The original DeepSeek V4 Pro Writer added commit `2986b8b6bf2f33eb3ec6e7d0560fbabbfd7a7cff` after the independent rejection. It validates InnoDB for both participating tables, rejects extra UNIQUE drift, makes missing post-create tables fail closed, repairs the conditional MySQL tests, pins a locally-created transaction template to `PROPAGATION_REQUIRED`, adds outer-transaction rollback/failure tests, and changes event reads to exclusive `findAfterVersion` semantics.

Writer verification under `/tmp/cyf-gradle.lock`: mapper 77 passed; service 383 tests with 0 failures and 27 conditional skips. The conditional MySQL suite has expanded to 16 tests and remains pending an independent isolated MySQL 8.0.21 run. The Git commit timestamp displays the host's known future local date; the authoritative control-plane date remains August 8, 2026.

## 12. First isolated MySQL gate

The independent Flash Test Runner created a disposable MySQL 8.0.21 instance on `127.0.0.1:33307` using a unique `/tmp` datadir and `--no-defaults`. Production PID `2958797`, port 3306 and production datadir were not connected to or modified, and the isolated instance was stopped and removed afterward.

Full mapper/service rerun passed (77 + 383 tests). The conditional MySQL suite ran without skips: 15/16 passed. The sole failure was a test assertion that expected one `information_schema.statistics` row for a four-column composite UNIQUE; MySQL correctly returned four rows. Engine, binary collation, auto-increment, extra UNIQUE rejection, MyISAM drift and the other tests passed. The original Writer was resumed for a narrow test-only correction; production initializer logic must not be relaxed.

## 13. Composite-index test correction

Commit `201a79391c0d70a194fc2aad063a25aab6fda20c` changes only `AgentSchemaInitializerMySqlTest`: it validates the exact UNIQUE index set, `non_unique` values, and ordered composite columns instead of incorrectly expecting one `information_schema.statistics` row per composite index. The production initializer is unchanged. Independent isolated MySQL and full reruns are pending.

## 14. Final independent test evidence

On the authoritative control-plane date August 8, 2026, Tesla independently reran the final HEAD. A disposable MySQL 8.0.21 instance using `--no-defaults`, a unique `/tmp` datadir and port 33307 ran all 16 conditional initializer tests with 0 failures and 0 skips. The instance was stopped and deleted afterward; production PID `2958797`, port 3306 and production data/config were not connected to, signaled or modified.

After isolated MySQL cleanup, mapper 77 and service 383 tests were forced with `--rerun-tasks` under `/tmp/cyf-gradle.lock`: 460 total, 0 failures/errors, 27 expected conditional skips. C01 suites: schema 11/11, initializer unit 39/39, real transaction 32/32, isolated MySQL 16/16. Final worktree is clean and `git diff --check` passes. Final disk free was `5,423,689,728` bytes, only about 52 MiB above the 5 GiB gate; this is a P0 operational constraint for the next build, not a C01 correctness failure.

## 15. Second Reviewer conclusion

Socrates returned `ACCEPT` on August 8, 2026. There are no P0/P1 or blocking findings. Two non-blocking P2 items remain: the writer API Javadoc describes `@Transactional REQUIRED` while the implementation uses an explicit REQUIRED `TransactionTemplate`, and the control-character validation lacks a direct regression input despite production validation being present. These do not block C01.

Required follow-ups are carried forward: C01B must add isolated-MySQL writer concurrency/rollback, a real Spring `@Transactional` proxy outer rollback test, and duplicate-event-id then successful-append no-gap coverage. C03 must paginate `findAfterVersion`.

## 16. Cumulative API base integration

Independent Sol High integration Writer Knuth created no-ff merge commit `76c857ea5f6c9cbf4fbe59d24d695cf75d0a0f4e` on `feat/m2-api-base-20260803`. Its parents are base `c49d148da40bc1f2998954cef27feee7c6b77b57` and accepted C01 tip `201a79391c0d70a194fc2aad063a25aab6fda20c`. The integration and source trees are byte-exact at `a9a67e2a9dcad1600feb42b8517f1a611afb67b9`; there were no conflicts or semantic edits.

Popper, an independent DeepSeek V4 Pro High reviewer (`019fe347-6fa9-72c0-8a4b-653a9290cfef`), returned `ACCEPT` with no blocking findings. The cumulative branch remains intentionally local: M2 policy does not push, merge into dirty develop, deploy, or restart services at this task. The review's suggestion to push/merge/deploy is deferred to the explicit final C08/handoff task and is not part of C01.

The integration review saw the later full-run XML where MySQL-gated tests are skipped by design; the authoritative isolated evidence remains Tesla's separate disposable MySQL 8.0.21 run with 16/16 passed and 0 skips.

