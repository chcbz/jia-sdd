# PWA API R19 R9 static Review root-cause matrix — 2026-08-30

Exact source remains unchanged and clean: `2599de3c2bdf5f1c3cf6e4faa8dcbb4048dffa4a` / tree `bad28da5c05d6448d805f88c4e88457f0f657a5e`.

Fresh independent `sol_reviewer` `01a050ee-d04d-7760-900d-888e4a407224` rejected the inactive R9 verifier package with `P0/P1/P2 = 0/1/2`. The R9 unit was never started; no Gradle, DB, Rabbit, Chromium, production access, or deployment occurred. Control-plane incident evidence is sealed at `/var/tmp/cyf-pwa-api-r19-r9-static-review-reject-20260830T124805+0800`, root SHA-256 `388cd6b38939a113f07b42285b42c09337bbeb85f6c018d1fd6d116b16b758eb`.

| Finding | Root cause | Required bounded remediation | Acceptance proof |
|---|---|---|---|
| P1: R9 attempt1 hard-linked eight private-plugin MavenLocal files from `/root/.m2`, then chmod'd the staging paths. The same inode is referenced by preserved R3/R4/R5 paths, so all eight historical modes changed from `0644` to `0444`. | The preparation generator treated source Maven artifacts as immutable bytes but used hardlinks before applying staged metadata. The retry fixed only the final R9 staging and could not undo the already-recorded cross-version metadata mutation. | Preserve R9 attempt1, R3/R4/R5, and the source paths exactly as current incident evidence; do not chmod/chown/unlink/relink or pretend historical ctime can be restored. R10 must use independent `O_CREAT|O_EXCL` byte copies only, sourced from the independent-nlink R9 final private-plugin projection or another byte-exact immutable copy, and prove source metadata before/after exact. R10 must declare R3/R4/R5 Maven evidence contaminated/non-authoritative and must not depend on their current manifests for acceptance. | Fresh R10 staging/final private-plugin files have `nlink=1`, distinct inodes, exact approved SHA-256 values and intended staged modes. Source metadata before/after/current is byte-exact. A negative static test rejects every `os.link`, `link`, `cp -l`, hardlink helper, or metadata operation on donor/source paths. |
| P2: final unit is renamed before `preparation.terminal`; Python exception cleanup cannot run after SIGKILL or host crash, so publication is fail-closed but not crash-atomic as documented. | Contract language and static test asserted a stronger atomicity property than the two-path publication protocol provides. | R10 must implement a crash-safe activation protocol with one explicit commit point, or narrow the contract to the exact proven fail-closed property. A partial publication must never produce a loadable/startable verifier and must be mechanically distinguishable from complete preparation. No exception-only cleanup may be the sole safety boundary. | Static fault-injection covers termination before and after each publication edge and proves every partial state is non-loadable/non-startable; complete state binds exact unit/package/terminal digests. Contract and tests use the same property name and semantics. |
| P2: the controller wrote `control-plane-sha256sum-package-20260830T1105+0800.log` inside already sealed R9 PREP, outside `SHA256SUMS.package`. | The replay command used the sealed package as its output directory instead of a separate control-plane evidence root. | Preserve R9 PREP unchanged as contaminated review input. Every R10 controller/reviewer replay log must be written outside R10 PREP. R10 package completeness must compare the full final path set against the manifest allowlist and reject any unlisted object except explicitly self-referential manifest files. | Fresh R10 PREP path-set check has zero unexpected/unlisted files; package checksum replays from `/` into a separate non-package evidence root; post-publication write probe against PREP fails. |

## Bounded R10 scope

1. Source candidate/tree remains unchanged; no product-source repair is authorized.
2. Create only fresh, non-overwriting R10 builder, wrapper, sealer, unit, preparation, evidence, workspace, and incident paths.
3. Do not start R9 or R10 during remediation; do not run Gradle, DB, Rabbit, Chromium, production access, or deployment.
4. Preserve all R2–R9 paths and the sealed rejection evidence. Do not repair the shared historical inode metadata in place.
5. Reuse the accepted R9 safety properties that the Reviewer passed: delegated UID/GID `61019:61019`, exact source/tree, direct staged Gradle, immutable R5 JDK, H06 donor independent copy and exact metadata preservation, online `9` / authoritative offline `8`, read-only source mount, bounded lifecycle, cgroup-quiescent ExecStopPost sealing, terminal → runtime sums → root digest, and no production access.
6. Require a fresh independent static Review before any R10 unit start.
