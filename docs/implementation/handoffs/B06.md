
## 首轮独立 Review（2026-07-23）

- Reviewer：Locke。
- 结论：**REJECT**（P2 两项；B04 lease/result CAS race 验证通过）。
- Request workItem 过滤、Artifact visibility ACL 在 DAO 取 500 行后才于 Java 过滤，超过边界时合法结果不可达；必须下推 SQL，在过滤后排序/limit。
- request description、storageUri、metadata 校验与 TEXT/VARCHAR 列边界不一致；所有 DataIntegrityViolation 被误判为唯一冲突。必须按 UTF-8 字节/字符精确校验，并仅映射真实 duplicate constraint。
- 已退回 Owner，要求补 500+ 行、多字节 UTF-8、超长 URI 和非 duplicate 持久异常测试。

## 最终独立 Review 与集成（2026-07-23）

- 修复 commits：`2fed7ae`、`ac4af12`。
- Reviewer：Locke，最终结论 **ACCEPT**。
- 定向 45 项测试通过：SQL ACL 下推后再 limit，第 501+ 合法行可达，artifact 可见性和稳定排序通过。
- UTF-8/code point、URI/TEXT 边界通过；仅 `DuplicateKeyException`、SQLState `23505`、MySQL `1062` 映射冲突，SQLState `22001` fail closed。
- B04 lease/result CAS 回归通过。
- 已在 B05 后集成到 `api/develop`：`0abe246..9a160ca`，联合模块测试通过并推送。
