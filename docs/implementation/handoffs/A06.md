# Task Handoff: A06 Agent commandId/ACK/幂等协议

## 1. 当前结论

- 状态：**rejected**（第二轮独立 Review 未通过，第三轮返修中）
- Branch：`feat/a06`
- Worktree：`/home/isp/wsps/cyf/.worktrees/a06-isp-install`
- Baseline：`isp-install/master@dd9a00a`
- 首版 Commit：`b1d0524d47ccda6e122a497ebfbb4544428599f8`
- Reviewer：Mill (`019f8d29-3425-77a2-b13c-1f52a0b0c1cb`)
- 禁止部署在线 `/home/isp/apps/codex-ws-agent/**`。

## 2. 首轮实现证据

- `npm test`：32/32 PASS，包含 A05 原 17 项回归。
- `OPENCLAW_API_KEY=test node agent-client.mjs --validate`：PASS。
- 以上自动测试未覆盖 Reviewer 的故障注入场景，不能据此验收。

## 3. 独立 Review REJECT 清单

### P0

1. `processing` 恢复为 pending 后仍再次 `runCommand()`，副作用已发生但 marker 未落盘的崩溃窗口会重复执行。
2. 同 commandId 不同 payload 会把原 `SUCCEEDED/FAILED` terminal ledger 覆盖成 `REJECTED`。
3. ACK 在 `sendFn` 成功前就标记 emitted；false/throw 时 ledger 与 outbox 状态矛盾。
4. 损坏 ledger 被当作不存在并覆盖，导致命令重新执行，未 fail closed。

### P1/P2

- fingerprint 遗漏嵌套业务 payload，且 transport messageId 会错误影响业务 fingerprint。
- 损坏 ACK 文件被静默忽略，未 quarantine/告警。
- ACK 复用 dispatch messageId；应使用独立 ACK messageId + correlationId。
- 首版把 handoff 错提交到 isp-install 的 `docs/implementation/**`；必须从产品 commit 删除，由本中央文件维护。
- “terminal ACK replay”测试实际只断言 RECEIVED，名称和覆盖不一致。

## 4. 返修门禁

- processing/terminal/corrupt ledger 的跨重启故障注入测试必须通过。
- terminal entry 对 fingerprint conflict 不可变；冲突独立记录并 REJECTED ACK。
- send 成功后才写 emitted；失败保留 outbox，成功但 marker 失败按 at-least-once 明确处理。
- ledger/ACK 损坏记录 durable quarantine 并 fail closed。
- business fingerprint 完整覆盖嵌套 payload，排除 messageId/timestamp/runtime 等 transport 元数据。
- ACK 每条独立 messageId，correlationId 指向 dispatch。
- 产品 commit 不包含 `docs/implementation/handoffs/A06.md`。

## 5. 第二轮独立 Review 新增阻断（2026-07-23）

上一轮 9 项已基本修复，但仍有两个 P0：

1. STARTED ACK outbox durable enqueue/fsync 失败时 `_emitAck()` 返回 false，但调用方仍进入 `runCommand()`；必须在 ACK 未持久化时禁止产生业务副作用。
2. ledger 已写 RECEIVED、inbox enqueue 前崩溃时，启动协调未识别 orphan RECEIVED/STARTED；重投只得到 duplicate，命令永久不执行。必须在启动时把无 inbox 对应项的非终态 ledger 转为 durable recovery-required/REJECTED。

第三轮测试必须新增上述两个故障窗口。

## 6. 第三轮独立 Review 新增阻断（2026-07-23）

前两项 P0 已通过，但 ACK outbox 仍可能越序：

- 文件名使用毫秒时间戳 + UUID，同毫秒内不保证 `RECEIVED → STARTED → terminal`。
- replay 当前项发送 false/throw 后继续后续项，可能先发送 STARTED/SUCCEEDED，RECEIVED 留待以后。

第四轮必须使用跨重启持久单调序列并严格 FIFO；任一项发送失败立即停止本轮重放，补同毫秒入队和中间失败测试。

## 7. 第四轮独立 Review 新增阻断（2026-07-23）

正常单实例 FIFO 已修复，44 tests/validate 通过，但 Mill 仍判定 **REJECT**：

1. 两个已初始化的 `AckOutbox` 共享目录时可同时分配相同 sequence；重启后按文件名兜底排序，能实际发送 `STARTED -> RECEIVED`。
2. `ack-sequence.json` 被合法格式回滚且 outbox 已清空时会重用旧 sequence，跨重启单调性不成立。
3. 自动测试缺少跨实例并发、重复 sequence、合法回滚、空 outbox 回滚窗口。

第五轮必须使用跨进程原子互斥/CAS 分配 sequence，保留不可随 dequeue 消失的 durable high-water 证据；duplicate sequence、状态回滚和 pending/high-water 不一致均须 quarantine/fail closed，禁止文件名兜底或静默修正。

## 8. 第五轮修复（2026-07-23）

- Commit：`46b36ac06b6a6e6e2351135e284e5cc4542b651c`
- 增加跨实例原子 sequence 分配锁、不可随 ACK dequeue 删除的 durable high-water、启动/replay duplicate 与回滚冲突 fail-closed。
- 新增锁竞争、合法回滚、重复 sequence、高序 pending 等故障测试。
- `npm test`：48/48 PASS；validate 与 diff check PASS。
- 状态保持 review，等待 Mill 第五轮独立复核；未集成、未部署在线副本。

## 9. 第五轮独立 Review 新增阻断（2026-07-23）

Mill 判定 **REJECT（P0）**：enqueue/replay 的 durable quarantine 健康检查发生在获取跨实例锁之前；等待锁的实例可能先通过健康检查，另一实例随后发现冲突并 quarantine，等待者获锁后却不复查，仍可继续入队或发送 ACK。

第六轮必须在持锁临界区内重新读取 durable quarantine/health，并把“检查健康 + 扫描冲突 + 分配/发送”置于同一互斥边界；补精确 TOCTOU 时序测试，确保一旦任一实例 durable quarantine，所有后续实例立即 fail closed。

## 10. 第六轮修复（2026-07-23）

- Commit：`bddc6a7162232885792b69dc8af2329de14fb53c`
- enqueue/replay 在持有跨实例锁后重新读取 durable quarantine/health，并把冲突扫描与关键操作置于同一互斥边界。
- 新增精确 TOCTOU barrier 测试，覆盖等待者在另一实例 quarantine 后不得 allocation/send。
- `npm test`：51/51 PASS；validate 与 diff check PASS。
- 状态保持 review，等待 Mill 第六轮独立复核；未集成、未部署。

## 11. 第六轮独立 Review 新增阻断（2026-07-23）

Mill 判定 **REJECT（P0）**：replay 路径已严格 FIFO，但 live `_emitAck()` 入队后仍直接尝试发送当前 ACK。若较低 sequence 的 RECEIVED 首次发送失败、网络瞬时恢复，后续 STARTED/SUCCEEDED 会直接成功，最后才 replay RECEIVED，形成 `STARTED -> SUCCEEDED -> RECEIVED`。

第七轮必须让 live emit 与 replay 共用唯一 FIFO drain：任何发送都从最低 pending sequence 开始；低序项失败立即停止，当前及后续 ACK 不得越过；STARTED 返回值必须只在其已按序完成 durable send/marker/dequeue 时为 true。

## 12. 第七轮修复（2026-07-23）

- Commit：`3a00b507383f3cf568d5740b06599bcd7c5d9df0`
- live emit 与 replay 改为共用锁内严格 FIFO drain；任何发送从最低 pending sequence 开始。
- STARTED 仅在自身按序 send、ledger marker durable、dequeue 完成后返回成功并允许执行命令。
- 新增真实 command path `false -> true` 恢复、旧 pending 阻断当前 ACK 等测试。
- `npm test`：54/54 PASS；validate 与 diff check PASS。
- 状态保持 review，等待 Mill 第七轮独立复核；未集成、未部署。

## 13. 第七轮独立 Review 与集成（2026-07-23）

- Mill 结论：**ACCEPT**；无 P0/P1，记录慢 sendFn 持锁的活性 P2 风险。
- `false -> true` 实际 command path 的成功发送顺序严格为 `RECEIVED -> STARTED -> SUCCEEDED`。
- 七项 commit 已集成至 `isp-install/master@bbf2c20` 并推送。
- `npm ci && npm test`：54/54 PASS；Node 20 validate 与 diff check PASS。
- 未同步或重启在线 `/home/isp/apps/codex-ws-agent/**`，等待 M1 MVP 整体部署窗口。
