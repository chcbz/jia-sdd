# Task Handoff: E12 HallScene 完整集成

## 状态
- 状态：accepted
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Base：`9e87f15f7b696ee4e58fc819cc90e2a60b2ffc9c`
- 日期：2026-08-12

## 冻结输入
- E7 atomic contract 及其测试；未经例外流程不得修改。
- E10B production TMX：32 canonical drawable fragments、5 existing props、37 non-drawable legacy bindings。
- E11 calibration：`zones=0`，report SHA-256 `cb9c70d67bb917d2efa51c52d0dc332efb5737341b107aa1be2bd8b74464455d`。
- Navigation/business semantics 保持 E1 基线；不得重新引入 `/agent/active`。

## 实施要求
1. HallScene 在 V2 staging 路径完整实例化 agents、props、fragments，并进入统一 world order。
2. lighting、world-ui、screen-ui 保持独立 render bands / layers，不混入 world order。
3. 每帧计算连续、安全整数 depths，通过 E7 atomic commit 一次提交；失败时不得出现半提交。
4. Pointer hit-test 按最终视觉顺序命中，不能按创建顺序或旧 depth。
5. 延迟 sprite 加载仅使用真实人物资源；不得使用临时人物形象。
6. V1 保留且只由 activation gate 控制；同一 Hall 实例不得同时激活 V1/V2 语义。
7. 不改变路径、移动、hotspot、任务分配、roster/map agent 分流等业务语义。
8. 生产 SpatialGrid provider 路径需有集成测试，不得仅依赖 `_trustTestProvider`。

## Exit Gate
- V2 staging 可完整实例化并稳定销毁/重建。
- agents + props + 32 fragments 使用同一确定性 world order。
- depths 连续且为安全整数，atomic rollback/error 路径有测试。
- pointer hit-test 与最终视觉顶层一致。
- lighting/world-ui/screen-ui 不受 world depth 重排。
- delayed sprite load 使用真实资源并在加载后原子重排。
- V1/V2 不混用，activation gate 行为可测试。
- 所有现有交互、navigation、map、occlusion tests 与 frontend build 通过。
- 未通过独立 Reviewer 不得进入 E13；不得部署或 push。


## 完成记录（2026-08-12）
- Final commit：`5308d7c`。
- Reviewer：`adversarial_reviewer` / DeepSeek V4 Pro，结论 `ACCEPT`。
- 初始 activation 的 32 fragment handles 均纳入统一 renderables/depth 提交。
- roster replacement 具备完整 `commitFrame`，删除 agent 不遗留 SpatialGrid entry。
- replacement 失败会恢复 controller、adapter、assembly、fragments、reader state 和 depth。
- 新增真实 HallScene 测试覆盖 fragment depth、roster 后 frame 更新、sort failure rollback。
- 定向测试：242 passing；迁移/校准测试：65 passing；`npm run build` 通过。
- `hall.tmx` SHA-256 保持 `4f94e3a52da71369d9c29d96e0ac0ceb2126a1a441b6cd63911701957e1ed49b`。
- 未 push、未 deploy；下一阶段 E13。
