# Task Handoff: E11 OcclusionConstraintZone 校准

## 状态
- 状态：accepted
- 最终 Reviewer verdict：ACCEPT
- Commits：`e364685dcf9bdc99ace8f07b8d5b9b788ff1dfe3`、`fdf39268c5f52ed8760ab568cc83af6d62055e2b`、`9e87f15f7b696ee4e58fc819cc90e2a60b2ffc9c`
- Calibration report SHA-256：`cb9c70d67bb917d2efa51c52d0dc332efb5737341b107aa1be2bd8b74464455d`
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Base：`72f05e7b07fcea9ac2187da6ccfe7d625cbf2b51`
- 日期：2026-08-12

## 冻结输入
- E10A migration ledger generationId：`fc855f90cbfc13c5ad8b24659825bc1dccaa03ec17866735fd923452dbdc7611`。
- E10B production TMX SHA-256：`4f94e3a52da71369d9c29d96e0ac0ceb2126a1a441b6cd63911701957e1ed49b`。
- 37 legacy bindings、32 canonical fragments、111 probes。
- E10A 对 37 项的 constraintDecision 全部为 `none`；该结论已通过 GPT V3 和技术审核。

## 校准原则
1. 只有“继续拆 fragment 仍无法表达”的局部关系才允许新增 zone。
2. 不得因旧 mask polygon 存在而机械转换为 zone；旧 mask 只是审计/迁移证据。
3. 不得添加 global/always-behind/always-front、共享 fragment 全局状态或人物 depth 缩放。
4. 任一 zone 必须具有唯一 stableId、targetFragmentId、scene/floor/chunk scope、behind/front relation、整数 priority、3px hysteresis 和有效 polygon。
5. target 必须为同 scene、同 floor、world-band canonical fragment；不得为 overhead。
6. 必须执行 opposite relation overlap、static/activation/runtime cycle 和多 Agent 两侧并存测试。
7. 如果完整证据表明 0 zones 即可表达当前地图，应交付机器可读的“零约束校准结果”，并证明所有 37 项仍依赖普通 fixed anchor ordering；不得为了产生代码而造 zone。
8. 不激活 V2、不改 HallScene runtime、不删除 V1。

## 预期交付物
- E11 machine-readable calibration report/manifest。
- validator 与 mutation tests。
- 若 zones=0：明确的零约束证明、32 target fragments coverage、111 probes 和 cycle/opposite-overlap 空集验证。
- 若 zones>0：TMX `v2-zones-*`、snapshot/debug evidence 和完整约束测试；任何偏离 E10A `none` 必须先停止并上报，不得自行推翻 accepted 规格。

## Exit Gate
- 不存在全局人物状态修改。
- 约束只影响当前 agent/target pair。
- target 不得是 overhead。
- E10A `none` 若保持成立，应以 zones=0 收口并由独立 Reviewer 接受。
- 未通过独立审核不得进入 E12。


## 完成证据
- 结论：`zones=0`；37/37 bindings 为 `constraintDecision=none`。
- 32/32 canonical fragments 覆盖；111/111 probes 通过。
- 108 agents + 32 fragments：mandatory edges=0、cycles=0，插入顺序不影响最终 total order。
- Generator 两次完整输出字节一致，并与 committed fixture 一致。
- Live ledger/manifest/TMX/contract SHA 漂移均 fail closed。
- Schema 与 polygon geometry 的 3px hysteresis 双源一致性及边界行为已测试。
- `npm run validate:constraint-calibration`：PASS。
- `npm run test:juyiting-occlusion-constraint-calibration`：55/55 passing。
- 独立 Reviewer 最终判定：ACCEPT，无 P0/P1/P2 遗留。
