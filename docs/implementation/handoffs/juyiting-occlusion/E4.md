# Task Handoff: E4 Polygon Geometry、3px Hysteresis 与 Validator

## 状态

- 状态：accepted（最终累计复核通过）
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Reviewer：独立 adversarial_reviewer / DeepSeek V4 Pro（只读，已启动）
- Base：`7fc2098`
- Commits：`1a96bd7`、`52fad93`、`9031ba8`
- Worktree：`/home/isp/wsps/cyf/.worktrees/juyiting-occlusion-v2`
- 日期：2026-08-09

## 范围

- 1/256 世界像素固定点规范化。
- AABB broad phase + even-odd containment + 精确边界。
- 点到 polygon 边线段的最小欧氏 signed distance。
- 固定 3px 的初始/outside→inside/inside→outside hysteresis。
- polygon validator：唯一点、零长度边、自相交、面积、3px erosion 非空。

## 禁止越界

- 不实现 E5 world ordering、constraint graph 或 spatial grid。
- 不接线 E6 renderer/E7 activation/Hall active scene。
- 不修改 TMX、assets、navigation 或 `/agent/map`/roster 数据流。

## Exit Gate

- 内/外/边界/AABB 假命中/凹角/顶点/±3px 临界测试通过。
- 退化边、自相交、面积 <1、3px erosion 空区域 structured fatal。
- XML/Document/preparsed polygon 的 fixed-point 结果确定一致。
- game/full tests、production build、E4 path typecheck、worktree clean。
- 独立 Reviewer ACCEPT 后解锁 E5。


## Writer 交付

- 新增 `polygonGeometry.ts`、`validation.ts` 与 70 项 E4 定向测试。
- `canonicalIr.ts` 在 zone parse 时调用 fixed compile/validator；CanonicalSceneIr 仍保存 world coordinates。
- Writer 报告：occlusion 248 passing；E4 path typecheck errors=0；diff-check/worktree clean。
- Writer 未完成 `npm test` / `npm run build` 的有效门禁证据，称环境 timeout，Reviewer 必须独立验证。
- 重点风险：concave polygon erosion 算法、inward normal方向、number叉积精度、fixed结果被丢弃导致canonical单位契约未闭合。


## 第一轮 Reviewer 结论

- 结论：**REJECT**（2 P0、1 P1、3 P2）。
- P0：AABB + inward half-plane clipping 对凹 polygon 计算的是 kernel，不是 disk erosion；U/C/L shape 有大于3px内部仍被误拒。
- P0：fixed compile 结果被丢弃，CanonicalSceneIr 仍保存未量化 world polygon/bounds；同一 fixed cell 的输入 serialized bytes 不一致。
- P1：orientation/area/intersection 的 Number 乘法可超过 2^53，所谓 exact predicate 不成立。
- P2：FixedPoint 未封装/运行时校验；containment 注释与浮点实现不符；少于3点使用错误码不准确。
- Reviewer 独立确认 `npm run test:game` 470 passing、production build PASS、E4 path typecheck clean、worktree clean。


## 第一轮修复

- 提交：`52fad93`。
- 凹 polygon erosion 改为基于 signed-distance 1-Lipschitz 上界的 Polylabel 式分支定界；凸 polygon 保留 inward offset clipping 快路。
- canonical polygon 保存量化后的世界坐标，bounds 从 fixed polygon 重算。
- orientation/intersection/area/even-odd 改为 BigInt exact predicates。
- public fixed inputs 加 safe-integer 断言；新增 `POLYGON_TOO_FEW_UNIQUE_POINTS`；boundary 使用 exact point-on-segment。
- Writer 证据：`npm run test:game` 479 passing；production build PASS；E4 path typecheck errors=0；worktree clean。
- 待 Reviewer 重点确认：concave branch-and-bound 不 false-pass、=3 与 >3 临界、fixed canonical bytes、量化后重复点契约。


## 最终验收

- 最终提交：`9031ba8`，validator 改为调用 `erodedInteriorNonEmpty`，按 convex exact clipping / concave branch-and-bound 分流。
- 最终 Reviewer：**ACCEPT**。
- 临界证据：宽 6px fatal；宽 `6+1/256` pass；incircle=3 fatal、≈3.001 pass；旋转凸细条临界一致。
- 门禁：`npm run test:game` 484 passing；production build 29.32s PASS；E4 path typecheck errors=0；worktree clean。
- 未接线 Hall v1，未越界 E5。
