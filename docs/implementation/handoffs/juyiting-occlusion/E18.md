# Task Handoff: E18 V5 Release Guard, Deploy and Push

## Status

- Status: `done`
- Candidate: `4127e7af8fe5c5325d0715fbfc826937f48f41bb`
- Visual reviewer: `vision_reviewer` / GPT-5.6 Sol High — `APPROVE`, P0/P1/P2 = 0
- Final gate: `release_guard` / GPT-5.6 Sol High — `GO`
- Release executor: main orchestrator
- Smoke operator: `test_runner` / DeepSeek V4 Flash Medium, with orchestrator supervision
- Dependency: E17 independently accepted on 2026-08-14; Release Guard is now authorized.

## First Release Guard result

The first independent Release Guard returned `REJECT — NO-GO` for three control-plane/release-process gaps, not for a newly discovered occlusion correctness defect:

1. E17/E18 were not accepted in the authoritative task ledger and had no handoffs.
2. The legacy deployment script performed `git pull` and `npm install`, so the deployed tree was not pinned atomically to the reviewed candidate.
3. `npm audit --omit=dev` findings lacked either remediation or a formal, bounded risk acceptance.

## Remediation prepared

Pinned release script:

- Path: `/home/isp/bin/cyf_web_kit_release_pinned.sh`
- SHA-256: `642bbd512f7a78a1b892920ddd839f85df06af272f6a3f5546237fcbe947c981`
- Requires `CYF_RELEASE_HEAD` and verifies exact HEAD, `develop`, clean tree, pre/post-build source identity, staging/dist hashes, same-filesystem rename, rollback trap and basic public smoke.
- Does not run `git pull` or dependency installation.
- Emits a release record containing commit, dist/deploy/live hashes, backup path and entry asset.

Rollback script:

- Path: `/home/isp/bin/cyf_web_kit_release_rollback.sh`
- SHA-256: `b7f64cf622800fc98ab2f679ad14b8f7a1188b5e360c10e56e11a08a8529e266`
- Syntax check: PASS.

Dependency risk disposition:

- Formal exception: `docs/implementation/JUYITING_E18_SECURITY_EXCEPTION.md`
- Scope is limited to the exact candidate; owner CYF web maintainers; expiry 2026-09-14.
- Release Guard must explicitly accept or reject this exception.

## Release prerequisites

Release Guard must verify all of the following before `GO`:

- E17 ledger and handoff are independently `accepted` with no unresolved P0/P1/P2.
- Candidate HEAD is still exactly `4127e7af8fe5c5325d0715fbfc826937f48f41bb` on clean local `develop`.
- Final build/test/performance/visual evidence is traceable to the candidate.
- The V5 manifest is clean and contains 12 screenshots with the frozen TMX SHA.
- The pinned release/rollback scripts match the recorded hashes and pass `bash -n`.
- The time-limited dependency exception is acceptable for this exact release path.
- Rollback and post-deploy smoke commands are explicit.

## Approved execution sequence after GO

```bash
CYF_RELEASE_HEAD=4127e7af8fe5c5325d0715fbfc826937f48f41bb \
  bash /home/isp/bin/cyf_web_kit_release_pinned.sh
```

Record `RELEASE_RECORD`, `BACKUP_PATH`, `DIST_SHA256`, `DEPLOY_SHA256` and `ENTRY` from the script output.

Then run public and deep Juyiting smoke checks:

- `/` and `/juyiting` return expected production content;
- entry JS and critical `hall.tmx`/Juyiting assets load without 404;
- no console fatal or resource failure attributable to the frontend release;
- V2 active and no pending activation/frame/roster flags;
- depth, fragment/overlay, selection halo, names/bubbles and deterministic remove/re-add order assertions pass;
- capture production smoke evidence tied to the deployed commit/artifact hashes.

If smoke fails, run:

```bash
bash /home/isp/bin/cyf_web_kit_release_rollback.sh <BACKUP_PATH>
```

and do not push. If smoke succeeds:

```bash
cd /home/isp/wsps/cyf/web
git push origin develop
```

Finally update this handoff and `JUYITING_OCCLUSION_TASKS.yaml` to accepted/done with the release record, smoke evidence and pushed commit.

## External limitation

The local API port `127.0.0.1:10018` is unavailable, and the public API root has returned 403/502. These conditions must be recorded separately from frontend smoke. Real backend E2E is not currently verified; CDP/CORS fixtures cover the frontend contract but do not replace that claim.

## Final Release Guard — 2026-08-14

GPT-5.6 Sol High `release_guard` returned **GO** with no blocking items.

- Candidate/branch/worktree/remote ancestry: PASS.
- E17 accepted; GPT code and visual reviews approved.
- E13/E14/V5/TMX provenance: PASS.
- Pinned release and rollback script identities: PASS.
- Temporary dependency exception: accepted for this exact candidate through 2026-09-14.
- E17's six P2 findings remain open non-blocking debt.

## Deployment result

The first pinned invocation was OOM-killed during Vite chunk rendering before staging or live-tree replacement. The script's error trap ran; the existing live tree remained unchanged and no push occurred.

A single operational retry used the same unmodified script and frozen candidate with bounded process settings:

```text
MALLOC_ARENA_MAX=2
NODE_OPTIONS=--max-old-space-size=384
```

The retry completed successfully:

- Release record: `/home/isp/hosts/cyf/web/bak/release-records/kit_release_20260815_003122_4127e7a.json`
- Backup: `/home/isp/hosts/cyf/web/bak/kit_bak_20260815_003122_4127e7a`
- Dist/deployed tree SHA-256: `15ef96f8acca26b09e086a8241f206c6b93d71d00b2a431be33a0abd962b89a7`
- Previous live tree SHA-256: `b51081349c33166e57dd3b5e6ee75f4e1b69e52b5bc275fa4cd4ea391d872636`
- Entry: `/static/index-BW4ZUe0h.js`
- Basic transactional public smoke: PASS.
- Full release logs: `/tmp/cyf-e18-release-4127e7a.log` and `/tmp/cyf-e18-release-4127e7a-retry.log`.

The release record filenames/timestamps carry the host-local post-midnight `20260815` stamp. The authoritative task/control date for this execution is 2026-08-14; neither source commit nor release ordering changed.

## Online smoke result

DeepSeek V4 Flash ran the required read-only, no-screenshot smoke and returned **PASS**:

- `/`, `/juyiting`, and entry JS: HTTP 200, non-empty, online hashes match the deployed tree.
- Online `hall.tmx` SHA-256: `885471a17ac080d4d766f3e86c69836bcac8ba66b9cab125a6ca3ac978d82d9f`.
- All eight TMX external image resources: HTTP 200, correct MIME, byte/hash match local deploy, no SPA fallback.
- V2 active; activation/frame/framePending/roster all false after settle.
- 林冲/吴用 depth assertions, world-ui overlay, halo band, names/bubble and remove/re-add identity/order: PASS.
- Runtime exceptions, console/log errors, non-cancel network failures and critical frontend resource failures: 0.
- Deploy tree SHA matches the release record and source tree remains clean at the frozen commit.
- Smoke JSON: `/tmp/cyf-e18-smoke-4127e7a.json`
- Smoke log: `/tmp/cyf-e18-smoke-4127e7a.log`

The local API port remained connection-refused and the public API root returned 403. They are recorded as external backend limitations; real backend E2E is not claimed.

## Push result

`develop` was pushed only after smoke passed:

- Before push: `origin/develop = 2424f51f375814f403ca70a9a6e9948728e595b1`
- After push: `origin/develop = 4127e7af8fe5c5325d0715fbfc826937f48f41bb`
- Push status: PASS
- Push log: `/tmp/cyf-e18-push-4127e7a.log`

## Final status

`done`

Production is serving the pinned artifact, smoke passed, rollback backup is retained, and local/remote `develop` both point to the accepted candidate.
