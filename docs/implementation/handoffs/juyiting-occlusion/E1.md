# Task Handoff: E1 生产等价基线与 GPT V0 证据

## 1. 结论

- 状态：accepted
- Writer：deepseek_flash_worker / DeepSeek V4 Flash
- Branch：codex/juyiting-occlusion-v2
- Worktree：/home/isp/wsps/cyf/.worktrees/juyiting-occlusion-v2
- Base commit：2424f51f375814f403ca70a9a6e9948728e595b1
- Initial commit：9bef6866c9b165fc343a0cec3880af942aef6a0f
- Review-fix commits：`76cb751329e3c8e61623d4e2aeffb6f8beae5e0b`、`52c098c07023ca304ae001ad8e5f3ca1674afeb8`、`584770a9414f718ab470088a0218d87ec87f0591`、`68b0ddb70faa61963cf59aaefc061348dcb7c644`、`3089af0c6dde2e76691fcd56e58dba1829f32166`、`b1ee51af14ddcd84a499917b83ec1a79141729b7`、`393c0386678fb726953f3a98454ced8ddb1961e9`、`fb1136438d49022915be7dbcc64f6422006fe23d`、`37d7263a7aa8a124b8150a710a91648d1007afcb`
- Visual Reviewer：vision_reviewer / GPT-5.6 Sol
- Technical Reviewer：adversarial_reviewer / DeepSeek V4 Pro

## 2. 实现内容

- 生成当前 TMX 的确定性机器清单和 37 mask ledger。
- 冻结 canonical/base/lighting/props/sprites SHA-256，并确认 duplicate occluder。
- 新增四种可重复 SVG 几何分层预览，带 commit/TMX hash/generation ID、ID 标签和图例。
- 新增构建体积、Juyi Hall runtime 网络资产体积和纹理解码估算报告。
- 新增 V0-CS01～CS09 证据状态报告，明确可完成项与依赖 E6/E14 的阻塞项。
- 新增 8 项 baseline validator，不修改运行时、TMX、图片资产、导航或业务接口。

## 3. 修改文件

- `package.json`
- `scripts/juyiting/lib/tmx-structure.mjs`
- `scripts/juyiting/inventory-juyiting-map.mjs`
- `scripts/juyiting/hash-juyiting-sources.mjs`
- `scripts/juyiting/render-occlusion-layers.mjs`
- `scripts/juyiting/asset-report-juyiting.mjs`
- `tests/juyiting-occlusion-baseline.test.js`
- `tests/fixtures/juyiting/occlusion-v0/**`

## 4. 关键事实

- mask=37、prop=5、image layers=3、collision=38、nav obstacles=38、hotspots=5、regions=8、routes=6。
- canonical mid 与 legacy foreground SHA-256 完全相同：`3e4f3f90b4d84411a844978237a7d3530bd481c37a62bcd73b9d694a7d2dd432`。
- TMX SHA-256：`e2b79085d2caf232801f9843bb1cfafa941fb5a7d38e16cede60ecb0ab3e8401`。
- 7 个 mask 的 centroid 与权威九宫区域不一致：49、54、57、74、76、80、83；只标记为 E10A 校准候选，不在 E1 改数据。
- V0-CS07 已完成；V0-CS02/03/04/05/08/09 因当前无可控角色摆位、完整 depth/mask debug 和浏览器 harness 而明确 blocked，未伪造截图或数值。

## 5. 测试证据

Writer 报告：

```text
npm run inventory:juyiting-map                PASS
npm run hash:juyiting-sources                 PASS
npm run preview:juyiting-occlusion-layers     PASS
npm run asset:juyiting-report                 PASS
npm run test:juyiting-occlusion-baseline      41 passing
npm run test                                  538 passing
npm run build                                 PASS
```

Reviewer 必须独立复现关键命令，不直接信任本节。

## 6. 验收条件逐项核对

- [x] 37 mask、5 prop、image/collision/nav/hotspot/region/route 清单可重复生成。
- [x] canonical source、重复资源和关键资产 hash 已冻结。
- [x] 四种 geometry 分层 preview 可重复验证。
- [x] 四个用户事实回归条目已冻结。
- [x] 资产和构建体积有机器报告；无法可靠获取的 draw call/perf 明确 blocked。
- [x] 未修改 v1 运行行为、TMX、现有图片或导航语义。
- [x] GPT Vision Reviewer：`ACCEPT-WITH-DEFERRED-EVIDENCE`；延后证据在 E6 后、E8A 前闭合。
- [x] 累计技术门禁通过；最后的任意非协作 filesystem writer 竞态由 GPT Architect 按串行执行契约裁定为 P2 known precondition，不阻塞 E1。

## 7. 未解决问题与风险

- 当前 E1 无法生成同坐标卢俊义/扈三娘 A/B、桌子三点矩阵、runtime depth/mask overlay、九宫 production screenshot 和 camera/mobile 矩阵；这些证据需要 E6 debug/harness 后补齐。
- 初始 V0 视觉事实仍是卢俊义 FAIL、扈三娘 PASS；E1 不宣称视觉问题已修复。
- asset report 的 decoded texture 为基于图像尺寸×4 BPP 的估算，不等同浏览器实际 GPU memory。
- SVG fixture 体积较大；Reviewer 应确认其确定性和审查价值足以支持提交。

## 8. 对后续任务的接口或假设

- E2 可消费 `tmx-structure.mjs`，但不得默认其已满足 v2 canonical IR 契约；E2 必须独立定义 schema/canonical serializer。
- E6 必须实现 V0 报告冻结的 debug manifest 字段，并补拍 blocked contact sheets。
- E10A 必须复核 7 个 region 漂移候选，不能自动移动 polygon。
- E14 才能生成正式 draw-call/108-agent 性能门禁。

## 9. Reviewer 结论

- GPT Vision Reviewer：`ACCEPT-WITH-DEFERRED-EVIDENCE`；允许推进 E2～E6，但 deferred V0 必须在 E8A 前闭合。
- DeepSeek V4 Pro Technical Reviewer：`REJECT`。
- 阻断问题：
  - P1-1：fixture 内嵌生成时 HEAD，导致提交后的 verify 模式必然不匹配；
  - P1-2：TMX parser 未保留 tileset `objectalignment`；
  - P1-3：TMX parser 未保留 `<ellipse/>` shape。
- 非阻断建议：重命名误导性的 `RUNTIME_CATEGORY`；明确 SVG generation ID 不是 final SVG self-hash；大型 inventory 后续可拆分。
- 修复状态：原 Writer 已提交 `76cb751329e3c8e61623d4e2aeffb6f8beae5e0b`：
  - 使用固定 `baselineCommit` + ancestor/`git show` 字节验证，最终 HEAD 上四个 verifier 连续两轮通过；
  - parser 已保留 `objectAlignment` 和 `ellipse/shape`；
  - `RUNTIME_CATEGORY` 已改名；generation ID 算法已显式标注并测试。
- 第二轮 Technical Reviewer：`REJECT`。
  - P1：重复 occluder 解码估算被算成三份，实际两路径各加载一次只能计两份；
  - P1：未被当前 runtime 引用的 legacy `public/juyiting/tiles/*` 被错误计入 `runtimeCoreBytes`；
  - P2：baseline test 未验证上述统计语义。
- 第二次修复状态：原 Writer 已提交 `52c098c07023ca304ae001ad8e5f3ca1674afeb8`：
  - runtimeCore 改为由实际资源引用扫描生成，legacy tiles 排除；
  - 解码统计拆为 loaded-path、unique-content、duplicate-overhead，重复 occluder 只产生一份额外开销；
  - baseline tests 增加统计语义断言。
- 第三轮 Technical Reviewer：`REJECT`。
  - P1：`asset-report-juyiting.mjs` 通过正则/字符串扫描源码推导启动资源和人物精灵；伪造注释可能抢占 `HALL_MAP_RESOURCE`，常量引用的合法 sprite `src` 会漏报。必须从可执行模块导出或真实 AST 获取。
  - P1：`source-hashes.json` 未把六张运行时人物精灵图纳入基线 provenance；精灵变化后可更新 fixture 并继续冒用旧 `baselineCommit`。必须纳入六张精灵的 hash 和 `git show <baselineCommit>` 校验。
- 第三次修复状态：原 Writer 已提交 `584770a9414f718ab470088a0218d87ec87f0591`：
  - runtime reference audit 直接执行 `HALL_BOOT_RESOURCES`、`buildHallMapResources`、`buildPersonaSpriteResource` 与 `PERSONA_SPRITE_MANIFEST` 导出，不再扫描源码；
  - 六张运行时 persona sprite sheet 已加入 source-hashes 和 baseline `git show` provenance；
  - 新增 fake comment、常量 sprite、sprite 字节篡改、未知/缺失 runtime reference 等 fail-closed 回归测试；
  - 四个 verifier 连续两轮通过，baseline 18 passing、全量 515 passing、build PASS。
- 第四轮 Technical Reviewer：`REJECT`。
  - P1：runtime/TMX/sprite 路径仅拒绝字面量 `..`，会接受 `%2e%2e`、`.%2e` 等编码点段；浏览器 URL 解析后可逃逸 `/juyiting/`，审计路径与实际请求路径不一致。
  - P2：sprite 被篡改后，`hash:juyiting-sources -- --update` 会先写入无效 fixture 并返回 0，后续 verify 才失败；建议 updater 在写入前先验证候选报告的 baseline provenance。
- 第四次修复状态：原 Writer 已提交 `68b0ddb70faa61963cf59aaefc061348dcb7c644`：
  - 新增共享严格路径 canonicalizer，拒绝 percent encoding、编码点段/分隔符、反斜杠、控制字符、origin/query/hash，并用 WHATWG URL 复核规范化结果；
  - runtime/TMX/hash 均使用唯一 canonical `public/juyiting/...` 路径；
  - hash/asset update 在写入前验证候选 provenance，并采用原子替换；篡改测试证明命令非 0、fixture 字节不变且无临时文件残留；
  - 四 verifier 两轮通过，baseline 20 passing、全量 517 passing、build PASS。
- 第五轮 Technical Reviewer：`REJECT`。
  - P1：删除 baseline 中的非 runtime `public/juyiting` 文件后，asset updater 只校验剩余候选集合，会成功更新 fixture；必须把完整 baseline public tree 集合作为不可缺项契约。
  - P1：baselineCommit 仍可通过修改待更新 fixture 重指向任意 HEAD ancestor；必须由代码中的冻结常量/独立不可自举契约锁定为 `2424f51f375814f403ca70a9a6e9948728e595b1`。
  - P1：本地路径词法边界不处理路径组件 symlink；必须 realpath 检查 public root 和最终文件，拒绝根外目标。
  - P2：atomic write 的 close/cleanup 双重失败可能覆盖主错误并遗留临时文件；应采用已验证的 AggregateError cleanup 模式。
- 第五次修复状态：原 Writer 已提交 `3089af0c6dde2e76691fcd56e58dba1829f32166`：
  - 代码常量锁定 E1 baseline commit，并要求所有 fixture 精确匹配；
  - 以 baseline `git ls-tree -r public/juyiting/` 的 27 个 regular blob 为完整权威集合，路径/mode/blob/current bytes 均必须一致；
  - public file 解析加入 realpath 边界，公共树遍历拒绝 symlink 和特殊条目；
  - 原子写采用可注入 operations 与 AggregateError 非遮蔽清理；
  - 四 verifier 两轮通过，baseline 27 passing、全量 524 passing、build PASS。
- 第六轮 Technical Reviewer：`REJECT`。
  - P1：Git replace refs 可重定义 `git show` 读取的 baseline 字节；所有 provenance Git 命令必须禁用 replace objects，并校验读取对象 ID 与 frozen tree blob 一致。
  - P1：公共树遍历存在 `lstat(path) → readFile(path)` 的 TOCTOU；必须对同一 no-follow 文件描述符执行 fstat/read，并验证打开文件的真实目标仍位于权威根内。
  - P2：inventory 与四 SVG preview 的多文件 update 仍可能部分写入；应使用带 staging/rollback 的批量原子替换。
- 第六次修复状态：原 Writer 已提交 `b1ee51af14ddcd84a499917b83ec1a79141729b7`：
  - 所有 provenance Git 子进程禁用 replace objects，baseline 按 frozen tree blob ID 经 `cat-file` 读取并复算 Git object hash；
  - 公共资产使用 `O_NOFOLLOW` descriptor-bound fstat/realpath/read，同一已打开实体完成边界与字节校验；
  - public tree、sprite ledger 和纹理统计共享审计后的内存字节；
  - inventory 两文件与 preview 四 SVG 改为 staging+rollback 批量事务更新；
  - 四 verifier 两轮通过，baseline 32 passing、全量 529 passing、build PASS。
- 第七轮 Technical Reviewer：`REJECT`。
  - P1：批量事务在移走原目标后用 POSIX `rename(temp,target)`，会静默覆盖事务期间被重新创建的目标；后续失败时又恢复旧 backup，导致期间内容丢失但未报告 rollback 异常。
- 第七次修复状态：原 Writer 已提交 `393c0386678fb726953f3a98454ced8ddb1961e9`：
  - batch commit 使用同目录 hard-link no-clobber 安装，目标存在即 fail closed；
  - staged/installed/backup 记录文件身份，rollback 只删除本事务实体；
  - 并发目标重建/替换时保留新内容和旧 backup，并以 AggregateError 明确 intentional recovery artifact；
  - 四 verifier 两轮通过，baseline 37 passing、全量 534 passing、build PASS。
- 第八轮 Technical Reviewer：`REJECT`。
  - P1：temp unlink 后重新采样 target 时，代码在验证前把候选 identity 写入 trusted `installedIdentity`；该窗口中的并发替换会污染 rollback token，导致 rollback 删除并发内容。
- 第八次修复状态：原 Writer 已提交 `fb1136438d49022915be7dbcc64f6422006fe23d`：
  - trusted installed identity 仅由 staging descriptor 创建并冻结，不再被 target 重采样覆盖；
  - hard-link 和 temp unlink 后均先验证局部 candidate；rollback 只删除匹配 trusted staging inode 的目标；
  - 精确窗口测试证明并发 target 与旧 backup 均被保留，并返回 intentional recovery AggregateError；
  - 四 verifier 两轮通过，baseline 38 passing、全量 535 passing、build PASS。
- 第九轮 Technical Reviewer：`REJECT`。
  - P1：trusted token 仅比较 `dev+ino`，hard-link 安装后的原地写会保持 inode 不变；事务可返回成功但最终字节已被并发改写，或 rollback 删除同 inode 的并发内容。
- 第九次修复状态：原 Writer 已提交 `37d7263a7aa8a124b8150a710a91648d1007afcb`：
  - trusted token 扩展为 inode/mode/length/SHA-256；target 使用 `O_NOFOLLOW` 同 fd 执行 fstat-before/read/fstat-after；
  - rollback 只有 inode 与内容均匹配时才删除事务文件；同 inode原地改写会保留 concurrent target 与 original backup；
  - backup cleanup 窗口变化可从内存原始字节重建 no-clobber recovery artifact，并有最终线性化验证；
  - 四 verifier 两轮通过，baseline 41 passing、全量 538 passing、build PASS。
- 第十轮 Technical Reviewer 继续在任意非协作 filesystem writer 模型下报告 pathname syscall 窗口。
- GPT Architect 最终裁决：`ARCHITECT DECISION: ACCEPT-SCOPE`。E1 是独占 Writer 的离线 fixture 工具，任意外部进程同时改写目标不属于 E1 exit gate；该项降为 P2 known precondition，禁止把 helper 复用到 E7/runtime activation。
- Coordinator 最终门禁：baseline 41 passing、全量 538 passing、validate-map/preview-map/inventory/hash/occlusion-preview/asset-report 全部 PASS、production build PASS、build 后 asset report PASS、无 temp/backup 残留、worktree clean。
- 当前状态：E1 ACCEPTED；V0 延迟视觉证据已于 2026-08-09 闭合，允许进入 E8A。


## 10. V0 延迟证据闭合

- 日期：2026-08-09
- Reviewer：`vision_reviewer` / GPT-5.6 Sol（只读多模态）
- 判定：`ACCEPT-V0-CLOSED`
- 历史事实：卢俊义右上桌遮挡 FAIL；扈三娘 PASS；历史样本不满足同坐标/同帧，禁止推断角色身份参与排序。
- 对象边界：prop、canonical occluder、mask geometry、hotspot 四类对象已严格分离。
- 新增强制复查：右上桌前 mask 58；E8A 五 prop 四方向探针和卢俊义/扈三娘同条件矩阵。
- 延期但不阻塞 E8A：fragment ownership→E9A/E9B；37 mask 视觉映射→E10A；全图/UI/camera/sprite edge→E13。
- 门禁：E8A 可开始，E8B 必须等待 E8A V1 视觉与技术双签字。
