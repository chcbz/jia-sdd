# Task Handoff: E7 Atomic Activation 最小模块

## 状态
- 状态：accepted
- Writer：critical_worker / GPT-5.6 Sol
- Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Base：`b46949b`
- Commit：`7144d9260b3905ce0335d037d3b1a3589d3a88a1`
- 日期：2026-08-09

## 实现范围
- 新增 `src/game/occlusion/sceneActivation.ts`。
- 新增 `tests/game/occlusion/scene-activation.test.ts`。
- 未接线 HallScene，未修改 TMX、prop、mask、atlas、navigation，未启用 active v2。

## 冻结事务
`parsed → canonicalized → validated → assetsReady → staging instantiated → constraint validated → atomic active commit`

## 已实现保证
- 任一阶段失败不展示 partial scene；staging 资源逆序、串行、exactly-once 销毁。
- 有 active 时保留上一完整 scene；无 active 时进入完整可诊断错误态。
- 单调 transaction ID、abort/stale 检查与 A/B 事务淘汰，慢事务不可覆盖新事务。
- v1/v2、sceneId、stableId 与 transaction ownership 在 commit 前完整校验。
- commit throw 同步回滚内部 active 和外部 swap；旧 active 仅在新 commit 成功后 dispose。
- frame transaction 先验证完整 order/depth/constraint，再做单次同步 swap；失败保留上一完整 frame。
- snapshot 与诊断输出深冻结；controller destroy 幂等。

## 验证证据
Coordinator 独立复现：

```text
E7 定向测试                                      42 passing
E7 文件 ESLint                                   PASS
E7 文件级 TypeScript                             0 errors
npm run test:game                                698 passing
npm run build                                    PASS（31.40s）
git diff --check b46949b..7144d92                PASS
worktree                                         clean
```

全项目 `tsconfig.game.json` 仍报告既有 sprite manifest readonly、movement fixtures、DOMParser 与 viewportTransform declaration 类型债；E7 两条新增路径单独 typecheck 为 0 errors，且 production build 通过。

## Reviewer 结论
- 最终判决：**ACCEPT**。
- 已核对原子性、旧 active 保存、无 active 错误态、v1/v2 整图隔离、abort/stale、exactly-once dispose、frame 完整性、snapshot immutability、ownership 和未提前接线。
- Reviewer 建议在 E15 真实接线时补充：标准 AbortSignal 注册边界、防循环 deep-freeze、commit hook 是否必须调用 `swap()`、destroy 后 API、无 active frame 和 depth-count 错误码测试。
- Coordinator 评估：标准浏览器 JS 事件循环中 abort 检查与 listener 注册之间无异步抢占；当前 snapshot 只由模块内部 POJO 投影构造，不接收 hook 循环对象。这两项不构成 E7 阻断，但 E15 集成测试仍需覆盖真实生命周期和 swap 契约。

## 后续接口与风险
- E15 必须使用同步 `context.swap(apply, rollback)` 更新 melonJS 可见状态，并提供不可抛出的同步 rollback。
- E12/E15 才进行真实 HallScene/melonJS 生命周期接线；E7 本阶段保持 standalone。
- 未经执行计划的 GPT contract exception 流程，不得修改 E7 已冻结事务语义。
