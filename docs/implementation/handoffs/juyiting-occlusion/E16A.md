# Task Handoff: E16A Legacy Runtime/Test Cleanup

## 状态
- 状态：accepted
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Final commit：`928e30a`
- 日期：2026-08-13

## 结果
- 删除生产 `behindMask` 双 depth、全局 `_sortByDepth`、`propDepth += 0.5` 和声明顺序 prop depth。
- 删除 V1 world snapshot collector；shadow/debug 改为 committed V2 adapter/handles 诊断。
- 正常生产默认启用 V2，`__JYT_V2_ENABLED=false` 仅保留为测试 opt-out。
- V2 fail-closed 使用固定错误态层级：prop=6、agent=7，位于 foreground=5 与 lighting=8 之间；不包含 mask、Y sort 或声明顺序语义。
- navigation、routes、slots、lighting、hotspot/world-ui、atomic error-state 均保留。

## 验证
```text
全遮挡单元测试                         567 passing
E15 tests                               17 passing
Shadow renderer tests                   41 passing
HallScene JS tests                       37 passing
Debug bridge tests                        2 passing
E14 Chromium: p95 1.60ms / p99 1.90ms   PASS
E14 strict provenance                     7 passing
production build                         PASS
```

## Reviewer
- 最终判定：**ACCEPT**，无 P0/P1/P2。

## E16B 签字候选
- 重复 foreground occluder：与 canonical mid occluder byte-identical；删除前必须同步清理 TMX/测试引用。
- `public/juyiting/tiles/hall-tileset.{json,png}`：unreferenced legacy tileset。
- `public/juyiting/images/modular/`：8 个 superseded v1 PNG + preview.html。
- 不得删除 canonical mid source、V2 atlases、lighting/base、cropped props、sprites 或迁移/门禁 fixtures。
