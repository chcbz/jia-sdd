# Task Handoff: E10A 37 Mask 视觉语义与迁移规格

## 状态
- 状态：accepted_with_findings
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Visual Reviewer：vision_reviewer / GPT（只读）
- Technical Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Base：`b8adb0988cd17f777e44064cf79c376cd9254b92`
- 日期：2026-08-12

## 强制输入
- Current TMX SHA-256：`291a38cc66ebd60c8577500a5afc18ce5398570fe4c35ca66d9eebe818826a97`。
- Legacy masks：37 项（TMX id 48–84），必须全部纳入 ledger。
- E9A fragment generationId：`7f8bbdd8f3ca49952d0bcfceadf60a50ad998fc7033e370cbef665ee331f3d3b`，32 stableId 视觉语义冻结。
- E9B atlas commit：`b8adb0988cd17f777e44064cf79c376cd9254b92`；不得改变 atlas/fragment ownership。
- 不修改 TMX/runtime/active renderer；不执行机械迁移（留给 E10B）。

## 交付物
- 37/37 machine-readable migration ledger；每项含 legacy TMX id/index、区域、polygon/AABB、目标视觉结构、target fragment stableId、future occluder stableId、scene/floor/chunk scope、sortAnchor、是否需要 constraint/recalibration。
- 每项 behind/boundary/front probes，包含角色脚点、预期 painter relation 和边界 tie 规则。
- 明确 mask 58（右上桌区域）及用户已指出的卢俊义/扈三娘回归事实。
- 九宫区域与 37 项映射的自包含 GPT V3 contact sheet/overlay。
- 完整性、映射唯一性、polygon/probe、mutation、reproducibility tests。

## Exit Gate
- 37/37 无语义空白，无“由 E10B Flash 自行判断”的字段。
- GPT V3 对九宫区域、37 mask→视觉结构映射和关键 probes `ACCEPT-V3`。
- Technical Reviewer ACCEPT 后才允许 E10B。


## 已接受结果
- 实现 commits：`3817a14`、`7404d361daba8f0af0dea98ab9db38cbbf01b286`。
- 最终 generationId：`fc855f90cbfc13c5ad8b24659825bc1dccaa03ec17866735fd923452dbdc7611`。
- 37/37 legacy masks（TMX id 48–84）均有唯一 target owner、fixed-point-y sortAnchor 和 behind/boundary/front probes。
- 约束决策：37/37 为 `none`；E10B 不得新增或猜测 constraint。
- GPT V3 视觉审核：`ACCEPT-V3`。
- 独立技术审核：`ACCEPT WITH FINDINGS`。
- 当前环境验证：`validate:mask-ledger` 0 errors/0 warnings；`test:mask-ledger` 36/36；确定性再生成通过；frontend build 通过；worktree clean。

## 非阻塞 Findings
1. P2：`3817a14` commit message 写“576 tests”，实际 Mocha test case 为 36；不影响测试真实性或代码行为，后续 squash/rebase 时可修正文案。
2. P3：受限 sandbox 内部分 legacy subprocess/git/Chromium suites 出现 `EPERM`、`SIGTRAP` 或 timeout；E10A 为 additive change，未修改既有 TMX/runtime/E9A/E9B 文件。

## E10B 冻结输入
- Base：`7404d361daba8f0af0dea98ab9db38cbbf01b286`。
- Ledger：`tests/fixtures/juyiting/occlusion-v2-masks/migration-ledger.json`。
- Contact sheet：`tests/fixtures/juyiting/occlusion-v2-masks/contact-sheet.svg`。
- TMX input SHA：`291a38cc66ebd60c8577500a5afc18ce5398570fe4c35ca66d9eebe818826a97`。
- Expected mask count：37；constraints：0。
- E10B 只允许机械迁移；ledger/TMX/manifest 任一不一致时必须停止，不得解释或修正语义。
