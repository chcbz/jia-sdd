# Task Handoff: E16B Legacy Resource/Reference Cleanup

## 状态
- 状态：accepted
- Writer：deepseek_flash_worker
- Reviewer：adversarial_reviewer（只读）
- Branch：`codex/juyiting-occlusion-v2`
- Worktree：`/home/isp/wsps/cyf/.worktrees/juyiting-occlusion-v2`
- Commits：`76afa82`、`2ed6020`

## 结果
- 生产 `hall.tmx` 不再引用重复的
  `liangshan-hall-foreground-occluders-v3.webp`，改为复用 canonical mid occluder。
- `foreground-occluders` 层名保留，用于 V1/error-state fallback depth contract；正常 V2
  运行时 legacy full-map occluder layers detached，不产生额外 V2 draw。
- E1 冻结 public tree 成员未删除；历史 E1/E8B/E10A provenance 未改写。
- `public/juyiting/images/modular/`、legacy `hall-tileset` 文件及重复 foreground WebP
  均因冻结 provenance 或工具链/历史证据约束保留。
- `2ed6020` 补充并关闭 fallback-layer review finding。

## Reviewer
- 最终判定：**ACCEPT**。
- P2：判定不适用（无需保留为未解决 finding）。

## 验证证据
- Map validation：PASS；sprite manifest validation：PASS。
- E10B current manifest/navigation validation：PASS（37 bindings、32 fragments、111 probes）。
- E11 calibration suite：55 passing。
- 当前 TMX/asset/runtime/E13 定向套件：88 passing。
- HallScene E12/E15 integration and rollback suites：131 passing。
- Production build：PASS；源码及构建产物中的 `hall.tmx` 均不再引用重复 foreground WebP。
- Restricted Chromium E14 gate：PASS，p95 1.5 ms、p99 1.8 ms、无 full-grid scan。

## 限制与未宣称事项
- Git-backed E1 baseline Mocha suite 在当前 Codex sandbox 中无法完成：Node 子进程调用
  收到 synthetic `EPERM`，且 `git cat-file --batch` 可能挂起。
- 因此不得将该 baseline Mocha 记为通过；等价的 frozen-tree comparison 使用 shell-level
  Git/archive evidence 完成，但不替代 Mocha 通过证据。

## 后续
- E17 负责 full verification and adversarial review。
- 本次未修改 web worktree 代码；无需 commit 本 docs 侧收尾。
