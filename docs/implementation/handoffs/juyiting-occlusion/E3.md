# Task Handoff: E3 Runtime Agent Adapter

## 状态

- 状态：accepted（最终累计复核通过）
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Reviewer：独立 adversarial_reviewer / DeepSeek V4 Pro（只读，已启动）
- Base：`245a07cb14420a0f2f8f0fc35a3752d1dc19cbfa`
- Commits：`b1afad3`、`6104101`、`7fc2098`
- Worktree：`/home/isp/wsps/cyf/.worktrees/juyiting-occlusion-v2`
- 日期：2026-08-08

## 范围

- 新增 Runtime Agent Adapter，将 `/agent/map` runtime identity/position 转换为 v2 `SceneObject`。
- 原始 `sourceEntityId` 不 trim、不改大小写、不做 Unicode normalization。
- `stableId` 使用原始 UTF-8 字节 SHA-256 + lowercase base32。
- trusted spawn/chunk resolver 决定 floor/elevation/chunk；普通 snapshot 不得覆盖冻结字段。
- 支持原子 batch、重复/碰撞检测、stableId→sourceEntityId 反查和安全 remove/update。

## 禁止越界

- 不接线 Hall active scene，不混用 v1/v2。
- 不修改 HallScene、tiledMap、TMX、assets、navigation。
- 不引入 `/agent/active`，不读取或合并 roster 数据。
- 不提前实现 E4 几何、E5 排序、E6 renderer 或 E7 activation。

## Exit Gate

- known vectors、Unicode/大小写、collision、原子 batch、可信 resolver、冻结字段更新测试通过。
- E3 定向、`npm run test:game`、`npm test`、production build 通过。
- E3 路径 typecheck 零新增错误，worktree clean。
- 独立 Reviewer ACCEPT 后才解锁 E4。


## Writer 交付

- 新增 `src/game/occlusion/runtimeAgentAdapter.ts`。
- `schema.ts` 新增 E3 structured fatal error codes。
- 新增 53 项 Runtime Agent Adapter 测试。
- Writer 报告：E3 occlusion tests 136 passing；production build PASS；E3 path typecheck errors=0；worktree clean。
- 待 Reviewer 重点攻击：外部可变引用、resolver raw throw、部分坐标更新、并发原子性、collision seam、Web Crypto 环境契约。


## 第一轮 Reviewer 结论

- 结论：**REJECT**。
- P0：公共 API 返回内部可变 SceneObject/sortAnchor 引用，可篡改冻结身份并破坏 reverse map。
- P0：部分坐标 update 将未提供轴错误重置为 0。
- P1：并发 create 存在 TOCTOU，重复 ID 可双成功并静默覆盖。
- P1：trusted resolver throw 与 crypto digest reject 泄漏 raw error。
- P1：无位置 update 的浅拷贝仍共享 sortAnchor。
- P2：collision 分支无真实 injectable seam；ill-formed surrogate 经 TextEncoder replacement 可能失去区分。
- 修复要求：统一 immutable copy boundary、线性化 mutation、structured resolver/hash failure、真实 collision 测试、严格坐标与 ill-formed UTF-16 处理。
- Reviewer 复核前后确认 worktree clean，未写入仓库。


## 第一轮修复

- 提交：`6104101`。
- 统一 clone/deep-freeze 返回边界，内部 Map 不再暴露。
- 部分坐标 update 保留未提供轴，并重新解析 chunk。
- mutation promise queue 线性化 create/update/remove。
- resolver/hash failure structured fatal；thenable fail closed。
- 新增真实 collision hash seam、ill-formed surrogate 拒绝和严格坐标校验。
- Writer 报告：occlusion 172 passing（E2 83 + E3 89）；production build PASS；E3 path typecheck errors=0；worktree clean。


## 最终验收

- 最终补充提交：`7fc2098`，关闭 resolver 返回对象 getter throw 泄漏 raw Error 的 P2。
- `safeSpawn` 的 then/floorId/elevation getter 与 `safeChunk` then getter 异常均转为 `AGENT_RESOLVER_THREW`；正常 thenable 保持 `AGENT_RESOLVER_TYPE_INVALID`。
- 最终 Reviewer：**ACCEPT**。
- 最终证据：`npm run test:game` 400 passing；E3 bounded suite 178 passing；production build PASS；E3 path typecheck errors=0；worktree clean。
- E3 未接线 Hall active scene，未引入 `/agent/active` 或 roster coupling。
