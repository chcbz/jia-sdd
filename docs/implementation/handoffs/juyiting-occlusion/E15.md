# Task Handoff: E15 Atomic V2 Switch

## 状态
- 状态：accepted
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Base：`703d9eb`
- Final commit：`0ed2c06`
- 日期：2026-08-13

## 实现结果
- staging 全部门禁通过后，整张场景一次发布 `renderSchemaVersion=2`。
- 首帧 pending/current agents 在首次 V2 commit 前进入 adapter、统一 handles/order/depth。
- 重复激活、地图刷新、roster replacement 均保留原子事务语义。
- map refresh 的新 mapData/shadow state 只在成功 commit 时发布；失败保留旧 active。
- asset、constraint cycle、sort、shadow publish、stale generation、destroy race 均 fail-closed。
- rollback 使用 transaction/controller ownership，destroyed scene 不会被旧事务复活，第三方新生命周期不被覆盖。
- roster 瞬时失败可重试，grid 通过可信快照重建恢复，诊断单次记录。
- E7 `sceneActivation.ts` frozen contract 未修改。

## 验证
```text
E7 + E12 + E15 定向测试                 119 passing
HallScene JS 测试                        36 passing
E14 provenance/performance report test    7 passing
production build                          PASS
E15 test 单文件 typecheck / ESLint         PASS
git diff --check                          PASS
```

## Reviewer
- 最终判定：**ACCEPT**。
- 无 P0/P1/P2。
- 已攻击 shadow publish failure、map/roster destroy race、ordinary stale、third-party controller takeover 和初始 activate destroy。

## 后续
- E16A 删除 legacy runtime/depth 语义，但不得删除 navigation、routes、slots、lighting、world-ui 或 atomic error-state 基础设施。
- 若 E16 修改 `src/game/occlusion/*.ts`，必须重跑 E14 Chromium benchmark 生成新 provenance 报告。
