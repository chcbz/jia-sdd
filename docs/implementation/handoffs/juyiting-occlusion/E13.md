# Task Handoff: E13 全地图视觉校准 V4

## 状态
- 状态：accepted
- Evidence Operator：test_runner / DeepSeek V4 Flash
- Fix Writer：deepseek_pro_worker / DeepSeek V4 Pro（仅视觉审核明确失败时启用）
- Visual Reviewer：vision_reviewer / GPT（只读，一次批量审核；必要时一次增量复审）
- Technical Reviewer：adversarial_reviewer / DeepSeek V4 Pro（只读）
- Base：`5308d7c`
- 日期：2026-08-12

## 成本约束
- 先以本地脚本和 Flash 完成全部截图、编号、坐标、角色、目标对象与机器检查。
- 首轮 evidence 不完整或机器门禁失败时，不调用 GPT。
- GPT 只读取压缩后的 contact sheets、截图索引和判定表；不提供历史对话或全仓代码。
- 默认仅一次批量 V4 审核；只有明确 FAIL 的受影响子集允许一次增量复审。
- 不调用 GPT Architect，不使用 gpt-image-2。

## 必须覆盖
- 九宫区域：西北、北中、东北、西中、中央、东中、西南、南中、东南。
- 六角色：宋江、卢俊义、扈三娘、李逵、林冲、吴用。
- 关系：behind、boundary、front；重点右上悬赏桌、栏杆、柱子、书架、前门结构。
- camera zoom/pan/resize、desktop/mobile/pinch、agent/hotspot pointer、labels/bubbles、lighting。

## Exit Gate
- 机器证据完整、可复现并绑定 world coordinate、角色、prop/fragment stableId。
- GPT V4 无 FAIL/UNCERTAIN；技术 Reviewer 无未解决 P0/P1/P2。
- 若有 FAIL，仅由 Pro 修复明确范围，并重跑受影响矩阵与公共回归。
- 不 push、不 deploy。

## V4 Round 1 / Round 2 Evidence Correction

- V4 round 1：15 张 contact sheet 中 12 PASS、3 FAIL。失败集中在 `bounty-board`、`scroll-table-front`、`worktable`。
- 根因复核：E13 的统一 `anchor ±34px` probe 对宽桌和相邻独立结构不成立；失败图包含不可导航点或第三方实体的正常遮挡，不能据此删除真实 prop/fragment。
- 不变项：production TMX、E7 frozen contract、E9 canonical ownership、navigation/hotspot 语义均未修改。
- Round 2：
  - bounty-board 使用可导航桌边 in-context probes：`(1380,370)`、`(1390,379)`、`(1400,385)`；
  - scroll-table-front 使用 E10 mask-73 probes，并在该 target 的 isolated evidence 中仅省略独立 companion bounty-board；
  - worktable 使用 E10 mask-74 probes，并在该 target 的 isolated evidence 中仅省略独立 companion library-shelf；两 companion 均有独立 in-context sheet。
- 机器门禁（2026-08-12）：production TS oracle `2160/2160`；Python validator `21/21`；matrix gate `20/20`；E13 tests `23 passing`。
- DeepSeek Pro/Flash Writer 与 Reviewer 当日均因 provider `503 auth_unavailable` 无法启动；未用 GPT 技术 Reviewer 替代。
- GPT 增量 V4 复审结果：`PASS 3/3`；无整人吞没、反向遮挡、白边或透明裂缝。
- camera/interaction/movement 的真实 Chromium smoke 随 E17 统一执行；E13 matrix 与 V4 视觉门禁已完成。

## 最终闭环（2026-08-14）

E13 原先延后到 E17/E18 的真实 Chromium camera/interaction/movement 验证已经完成：

- E17 全量证据与独立 DeepSeek V4 Pro 对抗审核：`ACCEPT`，无 P0/P1；
- 最终 V5 production-equivalent GPT 多模态审核：`APPROVE`，12/12 截图，无 P0/P1/P2；
- E18 线上无截图 CDP smoke：`PASS`，V2 active、camera zoom/pan、desktop/mobile 视口、agent/hotspot/world-ui、depth、halo、姓名/气泡及 remove/re-add 稳定顺序全部通过；
- 线上 TMX 与八个外部结构资源均和已验收候选 byte/hash 一致，无关键资源失败或 console/runtime fatal。

因此 `accepted_with_deferred_browser_smoke` 的 deferred 条件已关闭，E13 最终状态为 `accepted`。历史 Round 1/2 纠正记录保留，不改写。
