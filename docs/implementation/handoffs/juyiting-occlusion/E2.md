# Task Handoff: E2 Scene Schema 与 Canonical IR

## 状态

- 状态：accepted（第三轮最终累计复核通过）
- Writer：deepseek_pro_worker / DeepSeek V4 Pro
- Base：`37d7263a7aa8a124b8150a710a91648d1007afcb`
- Commits：`0ca3693`、`9923de7`、`31bca0d5b2b75a040c9c346792ef61f5d8ef81cd`、`245a07cb14420a0f2f8f0fc35a3752d1dc19cbfa`
- Worktree：`/home/isp/wsps/cyf/.worktrees/juyiting-occlusion-v2`
- Reviewer：独立 adversarial_reviewer / DeepSeek V4 Pro（只读）

## 交付物

- `src/game/occlusion/schema.ts`
- `src/game/occlusion/canonicalIr.ts`
- `tests/game/occlusion/schema-canonical.test.ts`

## 实现摘要

- 冻结 render schema v2、六个 render band、SceneObject/SceneRender/Fragment/ConstraintZone/CanonicalSceneIr 与 structured fatal。
- 提供 XML Document 与 melonJS/preparsed data 两条 canonical IR 入口和 deterministic serializer。
- 展开默认值、ASCII stableId 排序、floor registry、render union、zone reference/scope 基础校验。
- 当前 hall v1 未接入 active scene；E3 agent adapter、E4 几何、E5 排序、E6 renderer、E7 activation 均未提前实现。

## Writer 验证

- E2 定向测试：69 passing。
- 全量测试：607 passing。
- Production build：PASS。
- `typecheck:game`：E2 新增路径错误为 0；仓库既存 persona readonly frames、patrolRoutes fixture 和 viewportTransform declaration 错误仍存在。
- Worktree clean。

## Reviewer 必查

- XML 与预解析 adapter 是否真实覆盖同一语义，而非测试共用实现造成假等价。
- raw XML/Document API 契约是否符合设计；属性/对象组嵌套、typed properties、melonJS shape、offset/rotation 是否不会误解析。
- canonical serializer 字符串转义、数字（NaN/-0/指数）、字段顺序、ASCII 排序是否确定。
- stableId 全局唯一、floor ID/order 唯一、unknown floor、render discriminated union、partial IR fatal。
- fragment 与 SceneObject 是否存在双重身份/重复渲染风险；zone target world/same scene/same floor。
- 不能把 E3/E4/E7 责任提前或弱化；当前 v1 hall 行为必须不变。

## 第一轮 Reviewer 结论

- 结论：REJECT。
- P1：`hasRenderSchemaV2(null/undefined)` 抛 raw TypeError。
- P1：zone priority 错用 finite number 校验，接受 3.5，违反 signed integer 契约。
- P1：floor registry 未拒绝不同 floorId 使用相同 floorOrder。
- P2：canonicalNumber 以 10 位小数四舍五入，会让不同 IEEE-754 值串行化碰撞。
- P2：E2 tests 依赖全局 tests/setup，`npm run test:game` 下 56 项失败。
- P2：XML/preparsed objectgroup offsetx/offsety 被静默忽略。
- 修复要求：原 Writer 关闭全部 P1/P2，并补 raw XML/Document API 和 group transform 契约测试。

## 第一轮修复

- `hasRenderSchemaV2(unknown)` null-safe。
- priority/floorOrder uniqueness/number serialization修复。
- raw XML 使用 saxes；Document/raw/preparsed 三路径支持 group offset。
- 非零 rotation/nested group fail closed。
- 测试可由 `npm run test:game` 独立运行。
- E2定向 80、test:game 302、全量 618、build PASS、E2 typecheck errors=0。


## 第二轮 Reviewer 结论

- 结论：REJECT（第一轮 12 项均已关闭；新增 1 P1 + 1 P2）。
- P1：preparsed/melonJS `obj.polygon` 数组未在 parse 阶段拒绝 `NaN`/`Infinity`，错误会延迟成 serializer 的 `CANONICAL_NUMBER_INVALID`。
- P2：三点闭环退化 polygon `[A,B,A]` 未去尾并拒绝，违反“至少三个唯一点”的 fail-closed 契约。
- 修复要求：统一 polygon normalize/validate 路径，覆盖 raw XML、Document、preparsed 与 `props.polygon`，但不提前实现 E4 完整几何算法。
- Reviewer 确认复核前后 worktree clean，未写入仓库。


## 第二轮修复与最终验收

- 修复提交：`245a07cb14420a0f2f8f0fc35a3752d1dc19cbfa`。
- 统一 zone polygon 基础 normalize/validate；非有限坐标在 parse 阶段返回 `ZONE_POLYGON_INVALID`。
- `[A,B,A]`、`[A,A,A]` 等少于三个唯一点的闭环退化 polygon fail closed。
- raw XML、Document、preparsed、`props.polygon` 四入口回归覆盖；合法闭环 canonical bytes 一致。
- 最终 Reviewer 结论：**ACCEPT**；此前 12 项累计回归无复发，未越界至 E3/E4/active hall。
- 最终证据：E2 定向 83 passing；`npm run test:game` 305 passing；`npm test` 621 passing；production build PASS；E2 typecheck errors=0；worktree clean。
- 非阻断延后项：preparsed `null` 坐标的 coercion 加固可在后续 validator 边界复核；相邻重复点/零长度边归 E4 完整几何校验。
