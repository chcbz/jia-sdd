# Task Handoff: E17 Full Verification and Independent Review

## Status

- Status: `accepted`
- Evidence operator: `test_runner` / DeepSeek V4 Flash Medium
- Independent reviewer: `adversarial_reviewer` / DeepSeek V4 Pro High
- Integration/code reviewer already completed: GPT-5.6 Sol High, `APPROVE`, P0/P1/P2 = 0
- Repository: `/home/isp/wsps/cyf/web`
- Branch: `develop`
- Base: `origin/develop`
- Candidate commit: `4127e7af8fe5c5325d0715fbfc826937f48f41bb`
- Candidate state at handoff creation: clean; 91 commits ahead of `origin/develop`
- Started/completed evidence date: 2026-08-14

## Scope closed by E17

E17 closes the cumulative V2 implementation and the final P1 remediation series:

- `9a379e0` — move agent name/bubble UI to the world-ui overlay
- `e9139eb` — stabilize overlay bounds, bob and ordering
- `57f4610` — preserve world-ui identity across renderer modes
- `5d3f1a0` — fail closed invalid refresh/world-ui inputs
- `7d26b30` — enforce real source agent identity and hotspot gates
- `f2af916` — publish ready only after successful scene build
- `5a7f384` — preserve the frozen occlusion schema hash
- `4127e7a` — refresh final E14 benchmark provenance/evidence

The cumulative implementation also retains the accepted E0–E16 commits recorded in `JUYITING_OCCLUSION_TASKS.yaml`.

## Functional and build evidence

All evidence below is for the final candidate commit unless explicitly noted:

- Targeted occlusion/Juyiting suites: 173 passing.
- Runtime adapter + isolated E11: 151 passing.
- E9B isolated RGBA/provenance coverage: 49 passing.
- `npm run test:game`: 790 passing.
- Full `npm run test`: 1482 passing, 0 failing.
- `npm run validate:juyiting-map`: PASS.
- `npm run preview:juyiting-map`: PASS.
- `npm run validate:juyiting-sprites`: PASS.
- `npm run typecheck:game`: PASS.
- `npm run build`: PASS.
- `git diff --check`: PASS.
- Independent final fast-gate rerun by DeepSeek V4 Flash on 2026-08-14: map validator, sprite validator, TypeScript check and two production builds all PASS; E14 freshness test 7/7 PASS; build log contains no warning/error match.

E13 aggregate evidence is 48/48 PASS with matrix and live flags true; directed E13 tests are 23 passing. Its report keeps `releasePass=false` only because `independent release_guard pending` is an intentional final-release sentinel.

## Performance evidence

Formal report:

`web/tests/fixtures/juyiting/occlusion-e14/benchmark-report.json`

Final fixed Chromium run:

- Production build, Chromium 133, 1664×928 at DPR 1.
- 108 agents, 50 fragments, 37 zones, cell size 256.
- 10 seconds warmup plus 60 seconds sample; 3,595 frames.
- p95 1.8 ms; p99 2.3 ms.
- Full-grid scan count 0; all fixed performance gates PASS.
- Report freshness/provenance tests: 7 passing.

## Visual evidence

Final V5 production-equivalent evidence:

- Manifest: `/tmp/cyf-v5-4127e7a-final/manifest.json`
- 12 Chromium screenshots across desktop, portrait and landscape.
- Manifest commit: `4127e7af8fe5c5325d0715fbfc826937f48f41bb`.
- TMX SHA-256: `885471a17ac080d4d766f3e86c69836bcac8ba66b9cab125a6ca3ac978d82d9f`.
- GPT multimodal review: `APPROVE`; P0/P1/P2 = 0.
- V2 activation, depth relation, world-ui overlay, selection halo, names/bubbles and remove/re-add deterministic order all passed.

## Required independent review

The DeepSeek V4 Pro adversarial reviewer must independently inspect the cumulative candidate, acceptance contract and evidence. At minimum it must attack:

- source/stable identity ambiguity and duplicate/empty/oversized IDs;
- malformed/duplicate hotspot, unknown floor and invalid/degenerate geometry fail-closed behavior;
- initial build failure, refresh drift and partial-scene publication;
- world/world-ui/postDraw band separation and stable ordering;
- static/runtime cycle, cross-floor/scene edges and opposite constraints;
- spatial-index/full-grid scan and benchmark provenance;
- frozen TMX/schema/asset identity and E13/E14 evidence freshness.

The reviewer is read-only and must return `ACCEPT` or `REJECT` with P0/P1/P2 findings. E17 is not accepted until that verdict is recorded.

## Residual/environment limitations

- The managed host requires the documented Chromium restricted-host transport; E14 records this explicitly.
- `tests/fixtures/juyiting/occlusion-e14/environment-blocker.json` intentionally preserves the historical 2026-08-13 accepted run (p99 2.1 ms). It is not the current freshness authority; the final committed 2026-08-14 `benchmark-report.json` and its 7/7 provenance gate are authoritative (p99 2.3 ms).
- `127.0.0.1:10018` is currently connection refused.
- `api.chaoyoufan.cn` root access has returned 403 and previously 502. These external backend conditions are not, alone, evidence of an E18 frontend regression.
- Frontend behavior uses CDP/CORS fixtures where the real backend is unavailable; real backend end-to-end interaction remains unverified and must not be claimed.

## Next dependency unlocked

An independent `ACCEPT` with no unresolved P0/P1/P2 updates this handoff and the authoritative ledger to `accepted`, unlocking E18 Release Guard.

## Independent Reviewer verdict — 2026-08-14

DeepSeek V4 Pro High `adversarial_reviewer` returned **ACCEPT** for candidate
`4127e7af8fe5c5325d0715fbfc826937f48f41bb`.

- P0: 0
- P1: 0
- P2: 6 non-blocking findings
- E14 provenance inputs: 18/18 match current source.
- E13 V6 bindings: all recomputed bindings match.
- V5 manifest: final commit bound; 12 screenshots present.

Accepted non-blocking P2 follow-up list:

1. Ready reflects a successful complete scene build, not successful V2 activation; a complete V1 fallback can therefore be ready without a separate UI warning.
2. `_sceneBuilt` is set before final `_fullSyncAgents`/callback steps; malformed runtime failure after that point is only partially guarded.
3. Non-hotspot build exceptions can remain pending and retry per frame instead of latching a fatal state.
4. Historical E14 `environment-blocker.json` preserves the older accepted p99 while the final benchmark report is authoritative.
5. Invalid optional `geometryFootprint`/`visualClip` JSON is omitted rather than fatal; ordering polygon validation remains fail closed.
6. V1 fallback hit-test tie-break uses JavaScript string ordering instead of the V2 stable ASCII comparator.

These findings do not invalidate V2 active behavior or the frozen candidate and are accepted as post-release hardening debt. They must not be silently reclassified as fully closed.

## Final E17 status

`accepted`

E18 Release Guard is unlocked.
