# Task Handoff: A07 编码 Agent 独立 worktree 管理

## 1. 最终结论

- 状态：**ACCEPT**（2026-07-29）。
- Writer：Russell (`019f8d4a-52e8-7962-a2f0-853ccd5ac7c0`) / `critical_worker` / GPT-5.6 Sol High。
- Reviewer：`adversarial_reviewer` / DeepSeek V4 Pro High；只读独立审查，未参与返修写入。
- Source branch/worktree：`feat/a07` / `/home/isp/wsps/cyf/.worktrees/a07-isp-install`。
- 最终 source commit：`cb408a8408652532fd630ad7c3f67462e9763ad5`。
- M1 isp-install integration：`d1a71ccd46809fdc70716fab1f847ca8a24222ad`。
- 未部署 `/home/isp/apps/codex-ws-agent/**`，未删除或 prune 任何 Git worktree。

## 2. Commit series

1. `a7ff539bc090e246f45f02ae03a31783fa95501c` — isolate coding agents in task worktrees。
2. `8f6309756006383a355f1adeb613b5c097cbda38` — harden workspace archive trust checks。
3. `e23cbdc69aa0d5942bcbd7b1ba3d9f351105083a` — isolate archive verification from Git config。
4. `bc8f89499517192f2385346c0ba4690174caf1db` — close archive deletion races。
5. `c3859a5fd6b1912cd925b6e82742cf67ccb37cbb` — harden final archive Git state。
6. `c934f407c585ca213645de727ad612391829282c` — retain archived quarantine worktrees。
7. `cb408a8408652532fd630ad7c3f67462e9763ad5` — validate retained archived worktrees。

## 3. 验收语义

- 每个 Agent profile 获得独立、受 policy 限定的 Git worktree，不共享可写目录。
- archive 前后均执行 Git 状态、引用和 remote 隔离验证；迟到 dirty/unpushed commit、配置注入、旧 policy 升级失败均 fail closed。
- archived/quarantined worktree 保留注册，供审计和人工恢复；生产代码不包含 `git worktree remove` 或 `git worktree prune`。
- M1 集成冲突保留 base `168ad60` 已有的每个 profile `codexModel`，不会把多 Agent 模型配置折叠为全局默认值。

## 4. 变更文件

- `bin/codex_ws_agent.sh`
- `conf/codex-ws-agent/README.md`
- `conf/codex-ws-agent/agent-client.mjs`
- `conf/codex-ws-agent/codex-profiles.conf`
- `conf/codex-ws-agent/env.example`
- `conf/codex-ws-agent/install-policy-check.mjs`
- `conf/codex-ws-agent/test/agent-client.test.mjs`
- `conf/codex-ws-agent/test/workspace-manager.test.mjs`
- `conf/codex-ws-agent/workspace-manager.mjs`
- `conf/codex-ws-agent/workspace-policies.example.json`
- `shell/codex_ws_agent_install.sh`
- `skills/codex-ws-agent-install/SKILL.md`

## 5. 测试证据

- `cd conf/codex-ws-agent && npm test`：**92/92 PASS**，包含完整 A05/A06/A07 attack、workspace、archive 回归。
- `OPENCLAW_API_KEY=test node agent-client.mjs --validate`：PASS。
- workspace policy/installer validation、shell/node syntax：PASS。
- per-Agent `codexModel` profile 与 retained archive/quarantine 验证：PASS。
- 生产代码扫描：无 `git worktree remove/prune`。
- `git diff --check`：PASS。
- 独立 Reviewer 最终结论：ACCEPT，无 P0/P1。

## 6. 残余风险

1. archive/quarantine 只提供保留和隔离，不自动判断业务工作是否可丢弃；后续删除必须走单独人工审批。
2. Git remote、凭据和文件系统权限仍需由部署环境正确配置；policy 不替代主机级权限隔离。
3. 升级时禁止新旧 agent 实例同时操作同一 profile/workspace。
