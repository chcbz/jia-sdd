# 聚义厅轻量工作台 V3.1：覆盖审计与 UI 详设

2026-09-23；本目录仅记录 **V3.1 静态原型审计当时** 的快照：该审计当时未改原型/生产代码。
**另见 `../../specs/juyiting-lightweight-workbench/`：随后正式 Vue 接入的实现态详设与尚未完成的验收；本页 63 项结论只针对静态 Demo，不是正式系统的验收结论。**

**复核结论：未覆盖当前全部功能，不能作为无损替换方案。** 63个审计条目中，5项限定UI覆盖、25项部分覆盖、24项缺失、6项条件缺失、3项语义偏差。此计数不是全站功能覆盖率。

## 阅读顺序

1. `feature-coverage.md`：源码逐项对照、语义差异、开关与全站范围边界、补齐优先级。
2. `ui-detail.md`：字体、颜色、排版、尺寸、距离、响应式、状态、链接/动作、聊天空间专项。
3. `component-attributes.md`：桌面/移动代表组件的实际像素及计算样式。
4. `control-targets.md`：控件与目标属性登记，180种去重记录。
5. `evidence/`：可复核的JSON、源快照、测试报告与哈希；`tools/`为采样及文档生成脚本。

## 必须注意

- “典籍阁”底部入口已改名/直达，但正式阅读器与案卷检索未被两篇演示说明替代实现。
- 正式任务创建语义有偏差：当前源码创建未指派任务，Demo却要求选人并模拟assigned。
- 本轮浏览器检查的是本地Demo；源码基线是含未提交修改的本地工作树。未重新核验线上部署，不含真实后端联调。
- 所有操作仅本地样例；不要把保存草稿、假回复、TXT下载、收入案卷视为账号持久化、AI执行、真实交付或正式验收。
- 本轮不改已存在的`specs/juyiting-unified-experience/ui-detail.md`等用户脏文件；此目录是独立补充详设。
- 完整原型：`deliverables/ui-workbench-demo/`；原V3.1 zip保持不变。`evidence/source/`不包含图片字体，不能独立运行。
